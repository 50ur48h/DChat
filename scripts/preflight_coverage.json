{
  "covered": {
    "api/DAL suite and its coverage gate": "test.dal",
    "api/Lint, typecheck, test": "lint typecheck test",
    "api/Tenant isolation proof (may never be skipped)": "test.rls",
    "hygiene/.env.example and compose agree about what the container gets": "check.env",
    "hygiene/BACKLOG.md keeps every row it had": "check.backlog",
    "hygiene/Every TODO has a backlog ID": "check.todos",
    "hygiene/Lint the workflows, with shellcheck over every `run:` block": "lint.workflows",
    "hygiene/STATUS.md keeps its checklist and its signed-off work": "check.status",
    "hygiene/Seed truths match the generator": "check.truths",
    "hygiene/The BACKLOG guard still catches a lost row": "check.backlog",
    "hygiene/The STATUS guard still catches a gutted file": "check.status",
    "hygiene/The environment guard still catches a variable that reaches nothing": "check.env",
    "hygiene/The role ids and the secret-scanner allowlist agree": "check.roles",
    "hygiene/preflight still covers every step this file runs": "check.preflight",
    "infra/Compile every module and both parameter files": "build.infra",
    "infra/No secret value is written into a template": "check.infra",
    "infra/The infra secret guard still catches one": "check.infra",
    "web/Browser smoke": "test.web.e2e",
    "web/Build": "compile.web",
    "web/Lint, typecheck, test": "lint typecheck test"
  },
  "excluded": {
    "api/Install": "environment setup for a CI runner, not a check: a developer's machine is already installed",
    "api/Nothing to do": "the path-filter's no-op branch; it runs when the job has nothing to do",
    "coverage/Combine the shards and apply the floor": "combines the coverage data from two CI jobs; neither shard exists on a developer's machine, where `make test` measures everything in one pass",
    "docker/Build the API image": "builds a production image; minutes, and CI has the layer cache",
    "docker/Build the web image": "builds a production image; minutes, and CI has the layer cache",
    "evals/A key for the local secrets store": "needs a seeded platform database and the pizza fixture; `make evals` exists and is run against a stack rather than as part of a push check",
    "evals/An organization, a registered source and its catalog": "needs a seeded platform database and the pizza fixture; `make evals` exists and is run against a stack rather than as part of a push check",
    "evals/Install": "environment setup for a CI runner, not a check: a developer's machine is already installed",
    "evals/Nothing to do": "the path-filter's no-op branch; it runs when the job has nothing to do",
    "evals/The pizza fixture": "needs a seeded platform database and the pizza fixture; `make evals` exists and is run against a stack rather than as part of a push check",
    "evals/The platform database": "needs a seeded platform database and the pizza fixture; `make evals` exists and is run against a stack rather than as part of a push check",
    "evals/The twenty golden questions": "needs a seeded platform database and the pizza fixture; `make evals` exists and is run against a stack rather than as part of a push check",
    "hygiene/Scan for secrets": "gitleaks runs as a GitHub Action. No local equivalent is wired up, and adding one would be a second scanner to keep in step with .gitleaks.toml",
    "infra/Nothing to do": "the path-filter's no-op branch; it runs when the job has nothing to do",
    "mssql/Connector tests (may not skip here)": "the SQL Server container is heavy and started on demand (`make up.mssql`), which CLAUDE.md records as the deliberate arrangement",
    "mssql/Install": "environment setup for a CI runner, not a check: a developer's machine is already installed",
    "mssql/Install the SQL Server ODBC driver": "environment setup for a CI runner, not a check: a developer's machine is already installed",
    "mssql/Seed the SQL Server fixture": "the SQL Server container is heavy and started on demand (`make up.mssql`), which CLAUDE.md records as the deliberate arrangement",
    "web-e2e/A .env for the stack": "the compose smoke builds images and seeds a database — minutes rather than a minute — and has always been deliberately out of preflight",
    "web-e2e/Install": "environment setup for a CI runner, not a check: a developer's machine is already installed",
    "web-e2e/Install Chromium": "environment setup for a CI runner, not a check: a developer's machine is already installed",
    "web-e2e/Nothing to do": "the path-filter's no-op branch; it runs when the job has nothing to do",
    "web-e2e/The walk": "the compose smoke builds images and seeds a database — minutes rather than a minute — and has always been deliberately out of preflight",
    "web/Install": "environment setup for a CI runner, not a check: a developer's machine is already installed",
    "web/Install Chromium": "environment setup for a CI runner, not a check: a developer's machine is already installed",
    "web/Nothing to do": "the path-filter's no-op branch; it runs when the job has nothing to do"
  }
}
