# BACKLOG — deferred work (append-only)

Rules (plan §1.5 / §2.3): IDs are `B-001, B-002, …`, append-only, **never renumbered,
never deleted** — a dead item becomes `dropped (reason)`, not a gap.
`Prio` ∈ P1 (blocks V1) / P2 (should fix before V1 ships) / P3 (V1.1+).
`Status` ∈ open / planned / in progress (WP) / done (PR#) / dropped (reason) /
accepted (reason — the owner has read it and the risk stands).
In-code TODOs must cite an ID here: `TODO(B-017): …`. FIXME/HACK/XXX are banned.
A literal `|` inside a cell must be written `\|`, or it splits the row into
columns the header never declared and GitHub renders the rest of it away.
`scripts/check_backlog.sh` enforces every line of this in CI (B-081).

| ID | Date | Found during | Title & detail | Suggested phase | Prio | Status |
|----|------|--------------|----------------|-----------------|------|--------|
| B-001 | 2026-08-10 | P0.1 | **Choose a license.** Repo is public with no LICENSE file, so the default is "all rights reserved" — which is a poor fit for a public repo and blocks outside contribution. Decide MIT / Apache-2.0 / proprietary-with-notice, then add `LICENSE`, a README badge, and the SPDX line in `pyproject.toml`. Owner decision, not a code task. | P0 | P3 | open |
| B-002 | 2026-08-10 | P0.2 | **GNU make is absent on the Windows dev host.** CLAUDE.md and the plan use `make` as the developer interface, and CI (ubuntu) has it, but the local host does not — every target has to be run as its raw `uv run --directory apps/api …` equivalent. Fix by installing make (`winget install ezwinports.make`) and noting the prerequisite in README, or by adding a thin `make.ps1`/`Taskfile` shim. Decide before WP0.4, when `make up`/`make seed` become the documented quickstart. | P0 | P2 | done (#2) — installed GNU make 4.4.1 via `winget install ezwinports.make`; all umbrella targets verified from Git Bash; prerequisite documented in README and CLAUDE.md |
| B-003 | 2026-08-10 | P0.3 | **Generate the TypeScript API client from OpenAPI.** `apps/web/src/lib/api-client/` is hand-written with a runtime type guard. Replace with `openapi-typescript` output generated from the FastAPI schema, plus a CI drift check, so a contract change breaks the build instead of production (arch Part 3.1). Pre-seeded by plan WP0.3. | P7 | P2 | open |
| B-004 | 2026-08-10 | P0.3 | **Decide the styling approach before the chat UI.** WP0.3 ships CSS Modules (the Next default) with a small token set in `globals.css`; Tailwind was declined to keep the skeleton minimal. Phase 7 starts real UI, and Phase 11 is a polish pass — pick one then, while there are still only a handful of components to migrate. | P7 | P3 | done (#13) — CSS Modules plus a token set, written up in docs/design.md; no component library, and adding one now needs a DECISIONS entry |
| B-005 | 2026-08-10 | P0.4 | **The seed dataset's fixed end date will go stale.** `seed_pizza.py` pins `END_DATE = 2026-07-31` because reproducibility forbids reading the clock, so "last full month" stops meaning July as real time moves on. Golden eval #2 ("revenue last full month") and the Phase 8 demo both phrase questions relatively. Resolve by pinning every eval question to absolute dates, or by adding a documented `SEED_END_DATE` override that CI fixes while local demos track today. **Blocks the start of Phase 9** — the eval harness must not be built on a window that silently drifts. | P9 | **P1** | done (#57) — **and it was a product defect, not an eval chore.** Checking before deciding found that nothing in the prompt said what the current date was, so the model chose an anchor per question and chose differently: one live run resolved *"revenue last full month"* with `CURRENT_DATE` and another, minutes later, resolved *"recently"* with `MAX(order_date)`. Both were **right on the day they were measured**, because the wall clock happened to sit two weeks past the fixture's last row — a right answer for a reason nobody chose, which is B-051's shape and B-060's. The fix is **D-027**: `as_of` on the context and the tool context, defaulted to the wall clock, rendered at L0 where no budget can drop it, naming both escapes. The seed's `END_DATE` stays frozen and `truths.json` is untouched. Verified live — the same question at the same `as_of` gives byte-identical SQL with literal dates, and at `as_of = 2027-03-09` it says *"the orders data ends on 2026-07-31, so it does not cover the required month"* instead of silently returning nothing. The eval harness pins it; the UI override is **B-062** |
| B-006 | 2026-08-10 | P0.4 | **The pizza demo database has no read-only role.** It ships with only the owner login `pizza`. WP3.2 must register it with credentials whose write attempts fail (`readonly_verified`), so the seed needs a `pizza_readonly` role with `CONNECT`/`USAGE`/`SELECT` and nothing else — plus `ALTER DEFAULT PRIVILEGES` so future tables stay covered. Small, but it belongs to the fixture rather than to connector code. | P3 | P2 | done (#17) — `make seed` creates `pizza_readonly` with CONNECT, USAGE and SELECT and nothing else, plus ALTER DEFAULT PRIVILEGES so a reseed keeps it covered; registering the demo database with it reports `readonly_verified=true`, and registering it with the owner login is refused with the six privileges that disqualify it |
| B-007 | 2026-08-11 | P0.5 | **Every CI action still targets the Node 20 runtime.** The first green run warned on `actions/checkout@v4`, `actions/setup-node@v4`, `pnpm/action-setup@v4`, `dorny/paths-filter@v3`, `astral-sh/setup-uv@v5` and `gitleaks/gitleaks-action@v2` — GitHub currently forces them onto Node 24, but that shim will be removed. Bump each to a major that targets Node 24 once published, in one PR, and re-run CI. Warning today, broken pipeline later. | P12 | P2 | open |
| B-008 | 2026-08-12 | P2.3 gate | **The UI offers controls the caller may not use.** A Reader sees Change role, Remove and Send invite on the members page; clicking any of them earns a 403 the API correctly refuses and audits. Functionally safe — the guard is server-side and layered — but it teaches people the product is broken rather than that they lack permission. `role` already arrives on `/v1/me` and on the members list, so the screen can hide or disable admin-only controls and say why in one line. | P3 | P3 | done (#20) — both screens read the caller's role from `/v1/me` and hide what the API would refuse, failing closed while the role is still unknown. A Reader sees the data sources and the members list and no buttons, with one line saying why rather than a screen that looks broken. Server-side guards are unchanged: this decides what to render, not what is allowed |
| B-009 | 2026-08-12 | P2.3 gate | **`users.email` is NOT NULL, so a missing claim becomes a fabricated address.** An Entra access token carries `email` only when the API registration asks for it as an optional claim; without it `ensure_user` writes `<subject>@unknown.invalid` — a lie stored in a column later code will trust. Make the column nullable, keep whichever identity claim did arrive, and let the UI show the name or subject and say the email is unknown. | P2 | P2 | done (#16) — revision 0005 makes the column nullable and nulls out any `@unknown.invalid` address already written; `ensure_user` stores only claims that arrived and fills a blank later if one turns up; `/v1/me` and the members list carry `email: null`, and the UI falls back to name, then subject, saying once that no address was sent |
| B-010 | 2026-08-12 | P2.3 gate | **The Phase 2 gate ran without exercising the audited 403.** On the signed-off org (`ebfe8139-…`) no 403 ever reached the API: `audit_log` holds only org.created / invitation.created / invitation.accepted, and `security_events` is empty. The behaviour is covered by 8 integration tests plus the role-matrix snapshot, so this is missing *demo evidence*, not a missing control. Close it by clicking Send invite once as a Reader and confirming the `auth.denied` row. | P2 | P2 | done (#15) — exercised 2026-08-12: `auth.denied` / `insufficient_role`, actor sourabhkumrawat02@gmail.com (reader), object `POST /v1/orgs/ebfe8139-…/invitations`, in that org's own audit_log; security_events still empty, which is D-008 behaving |
| B-011 | 2026-08-12 | P3.1 | **Credentials are re-read from the store on every use.** Architecture Part 7.3 specifies "cached in memory with short TTL"; WP3.1 stores and fetches with no cache, which is correct but will mean one secrets-backend round trip per connector operation. Against a local file that is free; against Key Vault it is a network call on a request path, with a throttling limit behind it. Add a small TTL cache inside the provider (not the callers) once WP3.2 has a real read pattern to size it against, and make sure eviction on rotation is part of it — a stale credential outliving a rotation is worse than a slow one. | P3 | P3 | open |
| B-012 | 2026-08-12 | P3.1 | **A data source can only be registered with curl.** WP3.1 ships the full CRUD API and the Phase 3 gate is explicitly API-level, but no plan phase names a browser screen for it: WP4.3 builds catalog UI, and Phase 11 polishes "history/catalog/members". So the first thing a new organization must do has no UI at all. Add an Admin-only data sources screen — list, register, test, rotate, remove — reusing the design-system primitives. Small, and it removes the last reason to hand someone a curl command. | P3 | P2 | done (#20) — `/orgs/{orgId}/data-sources` lists, registers, tests, rotates and removes, built from the existing primitives. The Phase 3 gate demo is a browser flow; the last reason to hand someone a curl command is gone |
| B-013 | 2026-08-12 | P3.2 | **TLS to a customer database is negotiated, not required.** The Postgres connector connects with `ssl="prefer"`, which uses TLS when the server offers it and plaintext when it does not — right for a local container, wrong for a managed database over a network the customer does not control, and invisible either way. Add per-data-source TLS settings (`disable\|require\|verify-ca\|verify-full` plus an optional CA), default to `require` for anything that is not a loopback or compose host, record the negotiated mode in the verification evidence, and surface it on the data sources screen. Phase 12 is the natural home because that is when a real cloud database is first registered. | P12 | P2 | done (#18) — pulled forward to Phase 3 by the owner on 2026-08-12. `TLS_MODE` (encrypted modes only) applies to every address that is not loopback or named in `TLS_LOCAL_HOSTS`; a data source may tighten it and never loosen it, and in prod nothing counts as local. `tls_mode` is on every data-source response and in the audit trail, and a test asks the server — `pg_stat_ssl` — whether the session was actually encrypted rather than trusting the driver. See DECISIONS D-011; per-source CA material is **B-015** |
| B-014 | 2026-08-12 | P3.2 | **Nothing removes a secret when its organization goes away.** Deleting a data source deletes its credential; deleting an *organization* cascades the `data_sources` rows in the database and leaves the secrets orphaned in the store. There is no organization-deletion API yet, so this is not reachable today — but off-boarding a tenant is a V1 requirement and it must not leave credentials behind. Give the secrets provider a scoped delete (`ds/{org_id}/…`) and call it from whatever off-boards a tenant. **Measured on the dev host 2026-08-23, and it is no longer hypothetical: 14 of 17 refs in `ops/.secrets/secrets.json` are orphaned** — their organization does not exist in the platform database. Two came from deleting a throwaway org by hand minutes earlier, which is the exact sequence this entry predicts; the other twelve are from a previous machine's organizations, carried across in the copied `ops/.secrets/` while the Docker volumes were not. **That second group is the more interesting one**, because no deletion happened at all — the *database* was replaced and the credential store was not, so the store is not merely un-swept but has no way to know what it should contain. **The entry's own premise has expired too**: it says *"there is no organization-deletion API yet, so this is not reachable today"*, and reachability was never the barrier — a `DELETE FROM organizations` cascade produces it, and that is what a developer resetting state actually does. Locally this is contained: the ciphertext is useless without `LOCAL_SECRETS_KEY`, and the passwords are the demo fixtures'. Against Key Vault in Phase 12 the same ratio is a bill and an audit finding, and the sweep has nothing to enumerate from unless it is written before the orgs are gone. | P12 | P2 | open |
| B-015 | 2026-08-12 | B-013 | **A private CA can only be configured deployment-wide.** `TLS_CA_FILE` names one PEM bundle for the whole API, which covers the common case (a managed cloud database chaining to a public root) and not the one a self-hosted customer database presents: its own CA, per data source. `verify-ca`/`verify-full` against such a server currently needs that CA added to the deployment's bundle, which is an operator task rather than a tenant one. Give a data source optional certificate material — CA, and eventually a client certificate for mTLS — stored through the `SecretsProvider` like every other connection secret, not in `settings`. Wanted by the first customer who cannot use a public CA; not before. | P12 | P3 | open |
| B-016 | 2026-08-12 | P3.3 | **Coverage is measured in a job that cannot reach SQL Server.** The `api` job has no mssql service, so `connectors/sqlserver.py` reports ~27% there and the total falls from 94% to 88% — while the `mssql` job runs that suite and measures nothing. Nothing breaks today because there is no `--cov-fail-under` yet, but plan §4.4 ratchets coverage and Phase 5 adds a hard 90% gate on `dal/`, at which point a number that is 6 points wrong for structural reasons becomes a blocked merge. Fix by combining `.coverage` files across jobs (`coverage combine` on artifacts) before the gate exists, not after. | P5 | P2 | done (#27) — taken first in Phase 5, before any DAL code. Each job writes its own `.coverage.<shard>`, uploads it, and a `coverage` job combines them; §4.4's overall floor of 70 is applied there, on the whole measurement, rather than in a job that can see only half the suite. A run without a SQL Server shard prints what is therefore missing instead of reporting a total six points low as if it were the truth |
| B-017 | 2026-08-12 | P3 gate | **An organization whose only Admin loses their identity has no way back.** Roles live in `org_memberships` and are changed through an Admin-only route, so when the identity provider stopped recognising `sourabh@rereed.com` — the account that created the demo org — nobody who could sign in held Admin, and the Phase 3 gate was blocked on a database edit (`ops/scripts/set_role.sh`, added then). A tenant will hit this: people leave, accounts are deleted, a directory is migrated. Options worth weighing: a break-glass platform-operator role that can reassign an org's Admin (audited, and itself a privileged surface); an ownership-transfer invitation an Admin can arm in advance; or making an org's last Admin non-deletable at the IdP level, which we cannot enforce. Owner's call 2026-08-12: this is a product gap, not a demo inconvenience, and it is scheduled **before Phase 12** — **WP11.2**, which is already the members-and-polish work package, so the recovery path lands beside the screens that show membership. | **P11** | P2 | done (WP11.2a) — **ownership transfer, not a break-glass operator role**, per the plan's own preference: an Admin arms a grant in advance, keeps the token outside the product, and whoever holds it claims Admin of that one organization. The platform gains **no new privilege** — the alternative was a cross-tenant power that must be defended forever and is worth attacking precisely because it exists. **Not an ordinary invitation, and the difference is the whole feature**: `accept_invitation` adds a membership only where there is none, so a Reader redeeming an Admin invitation stays a Reader — and the locked-out person is usually already a member; invitations also expire in seven days, useless for a credential whose purpose is to be waiting years later. Treated as the bearer credential it is: hashed at rest, shown once, single-use, revocable, listed on the members screen with its expiry so renewing is visible rather than remembered, and audited on arm, claim and revoke. Table `org_recovery_grants` (revision **0027**) with its RLS policy, its `TENANT_TABLES` line and its rls_proof extension in the same PR; **no DELETE grant**, because that a recovery happened is exactly what an organization must be able to show afterwards. The test that carries it reproduces the locked-out state rather than asserting a 200 — every Admin demoted, the product refusing an invitation to the Reader who is left, and the grant as the only way out. `ops/scripts/set_role.sh` now says in its own header that it is no longer the answer |
| B-018 | 2026-08-13 | P4.3 | **Card search is lexical only; nothing is embedded.** Plan §6 Phase 4 makes the embeddings key optional and says search runs on the text index without one, so WP4.3 ships `websearch_to_tsquery` over `card_tsv` and no `embedding` column at all — a vector column nothing can fill is one that is never exercised, and a rerank over cards nobody embedded is a code path that is never right. Every card carries `flags.embedding = "queued"`, so the backfill has its work list already. When `EMBEDDINGS_PROVIDER/ENDPOINT/KEY/MODEL` exist: add the column, an idempotent backfill over queued cards, and the rerank seam `search_cards` already leaves (rank, then reorder). Lexical search finds `orders` from "revenue" today; what it cannot do is find it from "how much did we make". **Update 2026-08-17 (WP10.1b):** WP10.1 was expected to close this and does not. The embeddings, the vector column and hybrid retrieval all exist now (WP10.1a), but reranking *card* search needs a **query** embedding computed inside `build_context` — the agent's own path — which is the spending-capability-in-the-loop question filed as **B-073**. Take the two together; until then golden eval **#14** stays red live. **Update 2026-08-17 (B-073 merged):** the blocker is gone. An embedder now reaches a run through `ToolContext`, metered against the run and checked against D-019's ceiling, and `get_embedder` is the guarded door it comes out of — so a **query** embedding inside `build_context` is now an ordinary spend rather than an unwatched one. What is still owed is B-018's own half: a vector column on `catalog_tables`, an idempotent backfill over cards, and a rerank in `search_cards`. | P10 | P2 | done (#68) — **golden eval #14 passes live**, which is the evidence this entry has been asking for since WP4.3. Revision **0018** adds the column; `cards.embed_cards` is the idempotent backfill and runs from discovery, from profiling and from the eval provisioner, so a deployment that configured a key after its first crawl catches up by refreshing; and `search_cards` is hybrid, merged by **RRF on rank** exactly as `knowledge/retrieve.py` merges its two arms. Measured live on the eval catalog: *"Which day of the week is busiest?"* returns **0 cards lexically and 5 hybrid**, all `found_by=vector`, `public.orders` among them — the planner used to be handed an empty catalog for that question. **A refresh that changes nothing re-embeds nothing** (D-012's rule applied to the half that costs money): `refresh_cards` clears a vector only when the card's text actually changed. Two failures degrade rather than break, matching D-031 — a provider failure during the backfill leaves the cards lexically searchable and `queued`, and a failed *query* embedding falls back to wording, because this is the context stage of every run and raising here would turn a busy provider into an unanswerable question. One honest limit: the vector arm decides *candidacy*, not perfect ranking — on that question `stores` ranks above `orders`, and both reach the prompt, which is what the planner needs. |
| B-019 | 2026-08-13 | P5 | **Nothing guards STATUS.md, and it was gutted once already.** It is the single source of truth for position (plan §1.1) and every PR edits it, including PRs about something else — which is how #24 dropped the entire phase checklist, twelve signed-off work packages and four gate sign-offs, and stayed green until #25 restored it by hand. A file that only a careful human reads is one that eventually nobody reads carefully. Add a `hygiene` check: header fields present, one heading per phase 0–12 each carrying a GATE line, no more than a fifth of the lines lost against the base branch, and no item already `[x]` — WP, backlog ID or GATE — coming back as anything else or disappearing. Growth and rewording stay free. | P5 | P2 | done (#26) — `scripts/check_status.sh`, run by `hygiene` before anything else. Replayed against #24 it reports 43 findings where CI reported success. It carries a `--selftest` of nine crafted files, run first in CI, so a guard that has stopped matching fails the build rather than passing every gutted file |
| B-020 | 2026-08-13 | P5.1 | **An unaliased projection comes back named `_col_1`.** Canonicalising a statement runs sqlglot's qualifier, which names every output column — so `SELECT COUNT(*) FROM orders` executes as `SELECT COUNT(*) AS "_col_1"`, and that is the name the result frame, the artifact and eventually the chart axis will carry. Stable and unambiguous, which is what the qualifier is for, and meaningless to a person reading an answer. The fix is a naming pass over unaliased projections before generation (`count`, `sum_total`, or the expression's own text), and it needs a rule that survives duplicates and collisions with real column names. Not urgent while nothing renders results; visible the moment Phase 7 shows a table to a user. | P7 | P3 | open — **decided in WP7.3a, and deliberately not fixed there.** The evidence panel does put this in front of a person, so the choice the entry demanded was made rather than deferred by omission: the fix is a naming pass inside `dal/validator.py`, and a security-boundary change carrying a collision rule does not belong in the same PR as a migration, a new route and the phase's e2e. What WP7.3a does instead is cheaper and unrelated to the DAL — the planner's prompt asks for every projection to be aliased, so a model that complies produces `order_count` and a better name than anything we could synthesise. That is a mitigation and **not** the fix: a model may forget, and then a person sees `_col_1`. The deterministic pass is still owed, in its own PR, with human review |
| B-021 | 2026-08-13 | P5.2b | **Nothing deletes an expired result.** `result_artifacts.expires_at` is written on every row (30 days by default, `ARTIFACT_RETENTION_DAYS`) and no sweep reads it, so retention is currently a promise with a date attached and no enforcement. The files under `ARTIFACTS_PATH` are in the same position. Needed: a sweep that deletes expired rows and their stored payloads, running as the owner across organizations (`ix_result_artifacts_expires_at` exists for exactly this and is deliberately not org-prefixed), plus a test that a row past its date is gone. Phase 12 is the natural home because that is when there is a scheduler and a real Blob container to delete from — but note the promise is live from today, so if a customer asks before then, the honest answer is "it expires when the sweep exists". | P12 | P2 | open |
| B-022 | 2026-08-13 | P5.2a | **Execution caps are deployment-wide, not per data source.** Plan §6 WP5.2 says "hard cap per policy", and `SourcePolicy.limits` is built from `DAL_MAX_ROWS` / `DAL_TIMEOUT_SECONDS` — one pair of numbers for every source in every organization. That is right for one demo and wrong for a customer with a small reporting replica beside a large warehouse, or for an admin who wants a stricter ceiling on a production database than on a copy. The shape is already there: `limits_for()` clamps whatever it is given, so this is a column on `data_sources.settings` (the non-secret half of a connection, as B-013 used) read into `SourcePolicy`, plus UI to set it. Wanted by the first tenant with two sources of different sizes. | P11 | P3 | open |
| B-023 | 2026-08-13 | P5.2b | **The artifact store does file I/O on the event loop.** `LocalArtifactStore.put`/`get` are `async` and call `Path.write_bytes` / `read_bytes` directly, so a large result blocks every other request in the process for the duration. Invisible at demo sizes (a 1 000-row JSON payload) and a real stall at the cap. Fix with `asyncio.to_thread`, the same way `connectors/sqlserver.py` handles pyodbc's synchronous driver — or let it disappear when the Azure Blob backend arrives with a genuinely async client, which is why this is filed against Phase 12 rather than done now. Whichever comes first should also cover the `_resolve` path check. | P12 | P3 | open |
| B-024 | 2026-08-13 | P5.1 | **The first legitimate engine function will be refused.** D-015 refuses any function sqlglot cannot type, and `_ALLOWED_UNTYPED_FUNCTIONS` is empty by design. sqlglot knows the common ones — `count`, `date_trunc`, `lower`, `cast`, `generate_series` — so ordinary questions work today, but Phase 7 puts a real model in front of real schemas and something like `to_tsvector`, `age()` or a customer's own UDF will come up. This is not a bug to pre-empt: the entry is a standing decision about one specific function, and it belongs in a PR that says why that function is safe. Filed so that when it happens the response is "add the name, with a reason" rather than "widen the rule", which is the failure mode D-015 exists to prevent. | P7 | P3 | open |
| B-025 | 2026-08-14 | P6.1 | **Quotas are recorded but not enforced.** `usage_ledger` now holds every call with tokens, model, role, tier and a cost estimate, and `ix_usage_ledger_org_id_created_at` exists for exactly the window query a quota needs — but nothing reads it. Architecture 8.3 asks for per-org daily/monthly token and query quotas checked **at run start and at each LLM call**, soft warn at 80%, hard stop at 100% with a clear user message. Not built here because there is no run to stop and no screen to warn on: the check belongs with the Phase 8 controller that owns BudgetState, and it must treat `cost_usd IS NULL` (an unpriced model) as unknown rather than free. The data it needs exists from today. | P8 | P2 | open |
| B-026 | 2026-08-14 | P6.1 | **Nothing sweeps the usage ledger.** One row per LLM call, kept forever: at V1 volumes that is small, and by the time it is not, the table will be the one every cost query scans. Wanted: a retention policy (roll rows older than N months into a per-org monthly aggregate, or delete them), running as the owner across organizations the way B-021's artifact sweep will. Filed beside B-021 rather than folded into it because the answer differs — an artifact is deleted, a cost row is more likely rolled up, since "what did last year cost" outlives "what did that query return". | P12 | P3 | open |
| B-027 | 2026-08-14 | P6.1 | **A model id is deployment configuration with no validation until it is used.** `LLM_MODELS` must name a model per tier per provider, and this build deliberately ships no defaults (D-017/D-018 reasoning: a stale default 404s or bills for the wrong tier). `registry.resolve` fails clearly when an entry is missing, but nothing checks the ids are real until a call is made — so a typo in the strong-tier id is discovered by the first planning call in production. Wanted once WP6.2 has real providers: a startup or health check that asks each configured provider whether it knows the models it has been given, reported like `readonly_verified` is for a data source — a claim that has been earned rather than assumed. | P6 | P2 | open |
| B-028 | 2026-08-14 | P6.1 | **DB-backed tests fail intermittently on the local host, never in CI.** Four sightings across four unrelated suites: `tests/datasources` twice (WP6.1), then `tests/orgs::test_each_orgs_audit_log_holds_only_its_own_events`, two `tests/llm/test_front_door.py` repair tests, and `tests/auth/test_guards.py::test_the_role_matrix_holds` during WP6.2. Every one passes in isolation, in its own directory, and on immediate re-run; five subsequent full runs were clean, including two with `--tb=long` teed to a file specifically to catch a traceback — so the failure text has still never been captured. **The distinction that matters: this has not been seen once in CI**, whose `api` job runs the same suite against a service Postgres. That points at the local environment — Windows host, Docker Desktop Postgres — rather than at the tests or the product, which is why it is P2 and not P1. The common factor is the fixture chain that creates a temporary database per test and opens several engines against it; suspects are connection pressure over a long run and `DROP DATABASE ... WITH (FORCE)` racing an engine still disposing. Next step is instrumentation, not another re-run: log `pg_stat_activity` counts around the temp-database fixtures, and raise `max_connections` on the compose Postgres to see whether the frequency changes. Until then, treat a single red test in a local full run as unproven either way — re-run it alone before believing it, and believe CI over the laptop. **The failure text was finally captured on 2026-08-20** (during B-095, on a full run that went red in `tests/semantic/test_semantic_routes.py`), and it names the second suspect rather than the first: `OSError: [WinError 64] The specified network name is no longer available`, raised through `ConnectionResetError` on the asyncio proactor socket, at `tests/conftest.py` in `_temporary_database` — on the very line the entry guessed at, `asyncio.run(_run('DROP DATABASE IF EXISTS … WITH (FORCE)'))`. So it is the **teardown connection being reset**, not the test's own queries, and the symptom lands as a fixture ERROR beside the FAILED it drags down with it. Two further observations from the same session. **The failing subset differs every run** — three full runs produced three disjoint sets, all inside the file being torn down at the time — which is what a connection-pressure fault looks like and not what a product defect looks like. And it **reproduces with the session's changes stashed**, which is the cheap way to prove innocence: `git stash && pytest <file>` on clean code, before spending any time on the diff. Frequency rises sharply with host load: the sightings clustered after a container rebuild and several concurrent suite runs. Worth checking `max_connections` and adding a retry around the DROP before instrumenting further. | P7 | P2 | open |
| B-029 | 2026-08-14 | P6.2 | **The abstraction is not yet proven on a second provider.** WP6.2 ships `llm/openai.py` and the whole chain machinery — ordered resolution, retries with jitter, fallback on retryable failures, per-attempt metering — but only one provider exists, so the Phase 6 gate's *"same suite passes on both providers"* is **partially met**: proven on OpenAI, unproven on anything else. That is a deliberate owner's call on 2026-08-14 (the Anthropic key was not to hand and OpenAI credits expire soonest), **not a redefinition of the criterion** — the gate line is unchanged and stays unticked until a second provider passes the same suite. What is already proven with fakes: an injected 429 walks the chain, a 400 does not, and both attempts are metered. What is not: that a *real* second API agrees with our `LLMProvider` shape, which is the only claim two providers actually make. Close it by adding `llm/anthropic.py` (system prompt is a top-level parameter, so `ProviderCaps.supports_system_message` earns its keep there), verifying its model ids against the live account the way `.env.example` documents, and re-running the contract suite against both. | P6 | **P1** | open |
| B-030 | 2026-08-14 | P6.2 | **Provider error text is assumed not to quote the prompt back.** `llm/openai.py` puts a failure's message into the `LLMError`, which reaches `usage_ledger.error` and the logs. Provider errors describe the *request* — a bad parameter, a rate limit, an unknown model — in every case observed, and the text is truncated to 300 characters for exactly this reason. But that is an assumption about somebody else's API rather than a guarantee this code makes, and prompts carry table cards built from a customer's catalog. It is the one path by which customer-derived text could reach a log. Options: drop the message entirely and keep only status/type/code (loses the detail that makes a 400 debuggable), or pass it through a sanitizer that strips anything matching the request that was sent. Worth doing before a real tenant's data is in a prompt, which is Phase 7. | P7 | P2 | open |
| B-031 | 2026-08-14 | P6.2 | **The cost estimate ignores cached-input pricing.** `LLM_PRICES` models a flat input and output rate, and both providers charge roughly a tenth of the input rate for cached tokens (OpenAI $0.02/MTok cached vs $0.20 for `gpt-5.6-luna`; Anthropic $0.50 vs $5 for Opus 5). Nothing caches today, so the figure in `usage_ledger.cost_usd` is accurate — but Phase 7's context bundle is exactly the stable prefix that should be cached, and from the moment it is, the ledger over-reports every run and the per-run ceiling trips early. Needs: a third price per model, the cached-token count read from the provider's usage object (both report it), and a `cached_input_tokens` column. Do it in the same PR that turns caching on, not before — a column nothing fills is a column nobody trusts. | P7 | P2 | open |
| B-032 | 2026-08-14 | P6.2 | **The shared test `settings` fixture has the same non-hermeticity that just bit the LLM suite.** `tests/conftest.py` builds `Settings(env="ci", build_env="dev", …)` without `_env_file=None`, so every field it does not pass explicitly comes from the developer's `.env` — and for dict-typed fields pydantic-settings *deep-merges* rather than replaces, so even passing an empty dict does not isolate them. This is exactly what made six `tests/llm` tests change their answers the moment real model configuration landed in `.env`; that file was fixed, this one was not. Harmless today because no suite outside `tests/llm` asserts on a dict-typed setting, and latent for as long as that stays true by luck. Fix: `_env_file=None` on the fixture plus explicit values for anything asserted, and consider a session-scoped guard that fails if `.env` is readable during a test run. Cheap, and it removes a whole class of "passes on my machine, fails in CI" (and its more dangerous inverse). | P7 | P3 | done (#40) — `_env_file=None` on the shared fixture, taken alongside B-040 because they share a root cause. The guard B-040 adds is what covers the half this cannot: the route path never consults this fixture, it calls `get_settings()` |
| B-033 | 2026-08-14 | P6.2 | **Agent schemas do not opt into natively-constrained decoding.** `llm/openai.py` asks for `strict` only when a schema can carry it — every object closed with `additionalProperties: false` and every property required — because rewriting a caller's schema to comply would silently make optional fields mandatory (a contract change decided in the provider, invisible at the call site). A plain pydantic model satisfies neither condition, so today every structured call is schema-*guided* and relies on validate-then-repair rather than being schema-*enforced*. That works, and it costs a second billed call whenever the model wanders. Phase 7's planner, critic and composer schemas can opt in with `model_config = ConfigDict(extra="forbid")` and no optional fields, at which point the provider notices automatically (`strict_safe`) and the repair path becomes the exception rather than the routine. Measure the repair rate from `usage_ledger.repaired` before and after — the column exists for exactly this. | P7 | P3 | open — **half done, and measured rather than assumed (2026-08-15).** Every agent schema now sets `extra="forbid"`, and `strict_safe` reports: `DescribeTableIn` **true**, `RunSqlIn` **true**, `Plan` false, `FinalizeIn` false, `SearchTablesIn` false. The three that fail do so only because they carry fields with defaults — `Plan.reason`, `FinalizeIn.supported_by`/`confidence`, `SearchTablesIn.limit` — and each default is deliberate: a plan that can answer usually has no `reason`, and a required `supported_by` would force a refusal to invent citations. So what is left is a real decision, not an oversight: drop the defaults and make callers pass empties, or accept validate-then-repair on those three. Decide it with the repair rate from `usage_ledger.repaired` in hand |
| B-034 | 2026-08-15 | P7.1 | **`GET /v1/orgs/{o}/runs/{r}/executions/{q}` is in the contract and not built.** Architecture 10.2 lists it — "sanitized SQL + masked artifact" — and it is the route that turns a citation into something a person can check: a finding carries `query_executions.id` values in `support`, and there is currently no way to resolve one over HTTP. WP7.1 built the routes plan §6 names for it (conversations, messages, run, events) and stopped there. The data is all present from WP5.2b, including `result_artifacts.sample_rows`, already masked. Wanted by **WP7.3**, whose answer card is specified as "expandable evidence (SQL + rows preview + execution link)" — filed rather than assumed, because the plan's WP7.3 bullet describes the UI and never names the endpoint it needs. Watch the ownership rule when building it: an execution belongs to a run, and a run to one person's conversation. | P7 | P2 | done (WP7.3a) — `GET /v1/orgs/{o}/runs/{r}/executions/{q}` returns the canonical SQL, the tables and columns it touched, its row count and duration, and up to 50 **already-masked** sample rows from `result_artifacts`. A refused execution has no artifact and answers with its violation code and the statement that earned it, rather than an empty result that would read as "your data has no answer". The ownership warning was the right one: the execution is read **through** the run — `run_id` is in the WHERE clause, not just the path — so one belonging to another run is not found rather than refused, and there is no second access rule that could drift from the run's own |
| B-035 | 2026-08-15 | P7.1 | **Event payloads are typed as "some JSON".** `EventWriter` enforces the *type* against architecture 10.3's closed list and checks the payload serialises, and that is all it can do — `payload` is `Mapping[str, object]`. 10.3's actual rule is that payloads are **built for eyes**: short public strings from structured tool output, never raw model reasoning. Nothing checks that, and the failure it invites is not a crash but a trace that quietly starts carrying a model's chain of thought, or an unmasked value, in front of a user. Phases 8 and 9 add most of the twenty types, which is when the cost of getting it wrong multiplies. Wanted: a pydantic payload model per event type (a tagged union), so a writer cannot invent a field and a UI cannot read one that was never promised — and so review has something to look at. | P8 | P2 | open |
| B-036 | 2026-08-15 | P7.1 | **A conversation cannot be renamed, archived or deleted.** WP7.1 ships create, list, read and post; there is no PATCH and no DELETE, so a title auto-generated from a first question is permanent and a conversation started by mistake stays in the sidebar forever. Small, and worth doing with care rather than quickly, because of what a delete would take with it: `messages`, `agent_runs`, `agent_events` and `findings` all cascade from a conversation, while `query_executions.run_id` and `usage_ledger.run_id` are SET NULL (D-016). So deleting a conversation erases its whole trace but leaves the record that queries ran and money was spent — which is the right split, and needs to be a deliberate one with a confirmation that says so. Probably archive-not-delete for V1. | P11 | P2 | open |
| B-037 | 2026-08-15 | P7.1 | **A conversation is private to its creator, with no oversight path at all.** Architecture 6.2 grants every role "view **own** conversations & traces" and grants nobody anyone else's, so WP7.1 enforces exactly that: an Admin cannot read a colleague's conversation, and the refusal is a 404. That is the safe reading and the right default — questions are the most personal thing in the product — but it has consequences an owner should decide on rather than discover: nobody can help a user debug a bad answer without asking them to screenshot it, an org cannot review what its agent is being asked to do, and a departing employee's conversations become unreadable when their user row is removed (`user_id` goes NULL and ownership matches nobody). Options: leave as is; an Admin oversight view that is itself audited; explicit conversation sharing. Owner's call, not a code task. | P11 | P3 | open |
| B-038 | 2026-08-15 | P7.2a | **Three instruction layers have no storage and no owning phase.** Architecture 4.8 specifies six layers — L0 platform, L1 org instructions, L2 agent config, L3 skills, L4 reference data, L5 question — and 10.1 gives `agent_configs(org_id, instructions, budget_overrides, model_overrides, tool_toggles)` and `skills(...)` the tables to hold L1–L3. Neither table appears in any migration, and neither word appears anywhere in the implementation plan; 10.2's `GET/PATCH /v1/orgs/{o}/agent-config [admin]` is likewise unowned. `agent/context.py` builds all six slots and renders L1–L3 as nothing, so adding the store is one function each rather than a reshape — but until then an org cannot say "revenue excludes cancelled orders" and have it reach the model, which is the single most-requested thing a customer will ask for. Also carries `budget_overrides` (Phase 8 wants per-org caps) and `tool_toggles` (4.6 says the registry filters per org config, and today it filters only by role). Needs a phase: naturally P8 with the budgets, or P10 beside the semantic layer. | P8 | P2 | open |
| B-039 | 2026-08-15 | P7.2a | **A table is often not findable by its own name.** A card's headline renders the table as `public.shops`, and PostgreSQL's English parser reads `word.word` as one *host* token: `to_tsvector('english','public.shops')` is `'public.shops':1` while `websearch_to_tsquery('english','shops')` is `'shop'`, and they do not match. So a table is findable by its own name only by accident — when that name also appears as a plain English word elsewhere in its prose. Measured on the two live demo catalogs: **6 rows of 13 fail**, including `menu_items` on both sources, `payments` on both, `staff` on both, and `busy_stores`. `orders`, `customers` and `stores` pass, which is why nobody noticed. This matters more than a search-quality nit because `search_tables` is the agent's primary way to find anything (WP7.2a) and `menu_items` is the table **Phase 8's flagship refusal demo** is about — "which menu items sell best?" would fail to find the table at all and refuse for the wrong reason, hiding the capability check the M8 gate is meant to show. Fix in `catalog/cards.py` (emit the bare table name, and its underscore-separated parts, as plain words in the card) or in `search_cards` (also match `table_name` directly); either way rebuild `card_tsv`, which is generated, so it is a data migration over existing cards. `tests/agent/test_tools_live.py::test_a_table_is_not_yet_findable_by_its_own_name` pins the current behaviour and will fail when this is fixed. | P7 | **P1** | done (#41) — `build_card` now opens `shops (public.shops) is a table …`, and revision **0013** rewrites cards written before it. The fix needed no underscore-splitting: measured against PostgreSQL, a *bare* `menu_items` already tokenises to `'menu'`+`'item'` — only the qualified form makes a host token, so putting the bare name first is the whole change. Live demo catalogs went from **6 of 13** findable by their own name to **13 of 13**, and "menu items" now returns exactly the two `menu_items` tables, so Phase 8's demo will refuse for the right reason |
| B-040 | 2026-08-15 | P7.2c | **A test can call a real provider and bill for it, and one did.** The route path has no settings seam: `POST …/messages` calls `schedule_run` -> `execute_run` -> `llm.complete` with `settings=None`, which resolves through `get_settings()` and therefore through the developer's `.env`. On a machine with a working `OPENAI_API_KEY` — which is every machine that has run `make llm.smoke` — an HTTP-level agent test calls **OpenAI for real**. This was not theoretical: `test_asking_over_http_answers_202_and_the_run_completes_behind_it` did exactly that on its first run and failed with a genuine model's answer ("The supplied reference data contains no cataloged table or column representing shops") instead of the scripted one. Two model calls, a fraction of a cent, and no CI exposure — CI has no key, so it would have passed there while quietly charging every developer who ran the suite. Worked around in that one test by injecting `build_settings()` through the monkeypatched `schedule_run`. Proper fixes, in order of preference: (a) a session-scoped guard that fails any test whose provider is not a stub unless it opts in — `ProviderCaps.is_stub` already exists and the registry already refuses non-stubs in prod, so the machinery is there; (b) `_env_file=None` on the shared `settings` fixture, which is **B-032** and would not be enough on its own because the route never consults that fixture. This is the sharper, demonstrated version of B-032 and should be fixed with it. | P7 | **P1** | done (#40) — a session-scoped guard wraps `registry.get_provider` for the whole run: a non-stub provider **raises**, so nothing leaves the machine, *and* is recorded so a per-test check fails the test afterwards. The second half is the load-bearing one — replaying the original failure with the workaround removed shows the agent runner swallowing the raise into a `failed` run, so a guard that only raised would have gone green having proved nothing. Opt-out is `@pytest.mark.live_provider`, carried today by exactly one test that wants a non-stub on purpose. Four tests exercise the guard itself, because a guard that has never fired is one nobody has tested |
| B-041 | 2026-08-15 | P7.3b | **A whole question finds no table at all, so the agent is handed an empty catalog.** `search_cards` uses `websearch_to_tsquery`, which joins bare words with **AND** — right for a person typing `orders july` into the catalog search box, and wrong for `search_tables`, whose caller is the agent passing the user's whole question through. *"How many orders were placed in July 2026?"* becomes `'mani' & 'order' & 'place' & 'juli' & '2026'`, which no card can satisfy, so the **M7 gate question returned zero tables** and the model was asked to write SQL against nothing. Found by running the live smoke rather than by reading: the trace said `context_selected {"tables": []}` and the model then failed to produce a valid `Plan` twice, which looked like a model problem and was not. The same shape as **B-039** — a search that worked for the queries anyone tested by hand and not for the way the product actually calls it. | P7 | **P1** | done (WP7.3b) — two passes: the strict `websearch_to_tsquery` runs first and keeps every promise it made (quoted phrases, `or`, no 500 on a stray operator), and **only when it matches nothing** the words are retried joined by OR and ranked by `ts_rank_cd`. Nothing that matched before stops matching. Live on the demo catalog: the gate question now returns `orders` first at rank 0.8, and Phase 8's flagship *"Which menu items sell best?"* returns `menu_items` first at 0.6. Stemming and stopword removal are left to PostgreSQL so the query cannot drift from the index it searches |
| B-042 | 2026-08-15 | P7.3b | **The API container had no model configuration, so no question asked in the browser could ever be answered.** `ops/docker-compose.yml` forwards the database, auth, secrets and TLS settings to `api` and forwarded **none** of `LLM_PROVIDERS`, `LLM_MODELS`, `LLM_ROLE_MAP`, `LLM_PRICES`, `LLM_RUN_COST_LIMIT_USD`, `OPENAI_API_KEY` or `ANTHROPIC_API_KEY`. Every run scheduled by the API therefore died at its first model call with *"LLM_MODELS names no models for provider 'openai'"*. It went unnoticed for a whole phase because runs happen **inside the API process** (D-021) while every test of the provider — `make llm.smoke`, `make agent.smoke` — runs on the *host*, where the developer's `.env` is loaded automatically. The host worked and the product did not, which is the most expensive shape a configuration bug has. | P7 | **P1** | done (WP7.3b) — the seven variables are forwarded, each defaulted to empty rather than required, for the same reason `SECRETS_BACKEND` is: the API must still boot and answer `/healthz` on a machine with no key, and what refuses without one is the run, saying so. No secret is written into the compose file — the values come from the gitignored `.env` and are documented in `.env.example` |
| B-043 | 2026-08-15 | P7.3b | **A successful query died writing its result, after it had already read the customer's data.** `ARTIFACTS_PATH` defaults to `ops/artifacts`, which in the container resolves to `/app/ops/artifacts` — and only `/app/ops/.secrets` was mounted, so the directory was not writable and `dal.run` raised `[Errno 13] Permission denied` *after* the statement had run against the pizza database. The run ended `failed` with `llm_calls: 1` and a trace that stopped at `tool_called`, which reads like the query failing rather than the recording of it. Same root as **B-042** — the host has a writable `ops/` and the container did not, so nothing that ran outside a container could see it. | P7 | P2 | done (WP7.3b) — `../ops/artifacts` is bind-mounted beside `../ops/.secrets`, and on the host on purpose: WP5.2b's central claim is that no unmasked value reaches a stored file, and that is only checkable if the files are somewhere a person can grep |
| B-044 | 2026-08-15 | P7.3b gate | **Every reply rendered one message behind, so an answer arrived with no words.** Found by the owner walking the Phase 7 gate script in a browser. `POST …/messages` answers 202 with a run id and no answer — the reply becomes a `messages` row only when the run finishes — so the screen must re-read the thread at that moment. It did not. The poll effect depended on the whole `run` object, so `setRun(latest)` inside a tick triggered a re-render whose cleanup set `cancelled`, and the guard after the next `await` returned **before** the reload; the effect then re-ran, saw a terminal status and stopped. The answer text therefore arrived only on the next fetch something else happened to trigger, which in practice was the user sending another message. The backend was correct throughout — 3,718, correct SQL, correct refusal — which is what made it look like a rendering nicety rather than the gate failing. **A race, not a logic error:** the first regression test written for it passed against the broken code, because a stub that resolves in a microtask beats React's commit; only a stub with real latency orders the two the way a browser does. | P7 | **P1** | done (WP7.3b) — the poll is keyed on the run's **id and liveness**, never on the run object, and the reload is a separate effect that cannot be cancelled by the write that triggers it, guarded by a ref so it runs once per run. `setRun` moved after every `await` in the tick. Plus a belt: the answer card falls back to `run.answer` when the thread has no reply for that run, so a card is **never wordless** even if a reload is slow or fails. **Two false starts are the lesson.** The first regression test *passed against the broken code* (a stubbed `fetch` resolving in a microtask beats React's commit); the second only bit once given artificial latency, which is a guess about timing rather than a measurement of it. And the fix was then reported as working while the browser was still running the **old bundle** (B-045). Settled by a real browser: `apps/web/e2e/` drives Chromium against a stub API over real HTTP — **4 tests, all failing against the pre-fix component and all passing against the fix**, verified by swapping the file and rebuilding |
| B-045 | 2026-08-16 | P7.3b gate | **`next dev` in the web container does not see host edits, so a fix can be reviewed, shipped and tested against stale code.** File-watch events do not cross the Windows bind mount: the container had the corrected `conversation.tsx`, and the chunk actually served to the browser still contained the old one. The owner re-ran the gate script, saw the identical symptom, and reasonably concluded the fix did not work — it had never run. CLAUDE.md documented this only for **new route directories** (a 404, obvious); an edit to an **existing** file is far worse, because the page keeps working and silently executes the previous version. Cost: one wasted gate cycle and a fix wrongly believed broken. | P7 | **P1** | done (WP7.3b) — CLAUDE.md's quirk now covers edits as well as new directories and carries a one-liner that greps the *served* chunks for a token from your change, so "is my code running" is answerable rather than assumed. The manual test script starts by rebuilding rather than trusting hot reload |
| B-046 | 2026-08-16 | P7 gate | **The answer's status and its evidence live in a separate box below it.** WP7.3b renders the reply as a message bubble and then, underneath, a second card carrying the `answered` badge, the confidence badge, "no supporting query" and the link that opens the SQL. Two boxes for one answer: the reader's eye has to join them up, and the card reads as a footnote rather than as part of the reply. Owner's request at the Phase 7 sign-off: **fold them into the answer bubble itself** — a small colour-coded indicator in a corner that expands on hover or click to reveal confidence, status and the query link. Worth doing carefully rather than quickly, because the badges are load-bearing: colour never carries meaning alone (docs/design.md), a refusal must stay visibly different from a failure without becoming alarming, and the citation must remain reachable by keyboard. Note it interacts with **B-047** and **B-048** — all three are about making one answer read as one object. | P11 | P3 | open |
| B-047 | 2026-08-16 | P7 gate | **An answer is a wall of text, so its numbers are not scannable.** *"3,718 orders were placed in July 2026."* is short; a multi-sentence answer with several figures, dates and entity names is not, and today every word is styled identically. Owner's request at the Phase 7 sign-off: **highlight the key details** — numbers, dates, entity names — in bold or colour, so an answer can be read at a glance. Two constraints make this less trivial than it looks. The answer is **model-written prose**, so either the composer returns structure (spans, or the figures separately) rather than a string — the honest option, and a change to `FinalizeIn` — or the UI pattern-matches text a model produced, which is guessing about content the model did not mark up. Prefer the first. And whatever is emphasised is a **claim about what matters**, so it must not disagree with the findings and their citations: highlighting a number the run never computed would be worse than highlighting nothing. | P11 | P3 | open |
| B-048 | 2026-08-16 | P7 gate | **A chart must live inside the answer card, and its spec must be inspectable like the SQL.** WP11.1 adds the chart tool (validated Vega-Lite, client-rendered, no code execution). Owner's confirmation at the Phase 7 sign-off: the chart belongs **in the same answer card** as the prose and the citation, not in a panel beside it — and the **spec must be openable the way the SQL is** (B-034's evidence panel), so a picture is as checkable as a number. That is the same claim Phase 7 made for answers, extended to charts: a chart nobody can trace back to the query behind it is decoration that looks like evidence. Filed against WP11.1 so the requirement is in hand before the tool is designed, rather than retrofitted after. | P11 | P2 | done (WP11.1) — the chart renders inside the answer card, above the limitations and beside the findings, and its **Chart spec** button opens the document the browser rendered, exactly as B-034's panel opens the SQL. The spec is built server-side from a closed vocabulary, so what opens is what ran |
| B-049 | 2026-08-16 | P8.1b | **The duplicate-query rule compares proposals, not canonical statements, so one question written two ways runs twice.** 4.4's rule is a duplicate-query *hash*, and WP8.1a's docstring promised the hash of the **validated** statement — which would recognise `SELECT count(*) FROM orders` and `SELECT COUNT(1) FROM public.orders` as the same question. Contact with the DAL made that impossible as written: the canonical form only exists *after* validation, by which point the query has already been sent and the budget spent, and a duplicate rule that answers after the cost is not a rule. So the check runs on the model's own text, normalised for whitespace and case, which catches the failure it exists for — a loop out of ideas re-proposing what it just ran — and misses semantically identical rewrites. Closing it needs the validator to expose a canonicalise step that does not execute, so the loop can ask "what would this become" before spending a query; that is a `dal/` change and needs human review. `tests/agent/test_observe.py::test_the_same_question_written_two_ways_is_not_caught` pins the current behaviour and will fail when it is fixed. | P8 | P3 | open |
| B-050 | 2026-08-16 | P8.3a | **The SSE tail finds new events by re-reading the table, not by `LISTEN`/`NOTIFY`.** `event_stream` polls `agent_events` once a second per open stream. That is a deliberate first cut and it has real merits — the delivery path and the replay path are the *same query*, so a streamed event and a polled one cannot diverge, and there is no in-memory buffer that a subscriber can miss. What it costs is a steady indexed query per open stream, and up to a second of latency on a step that has already happened. Postgres `LISTEN`/`NOTIFY` is the obvious upgrade: the writer notifies on the run's channel and the stream wakes rather than polls, with the table still the source of truth and the poll kept as the fallback for a connection that missed a notification. The seam is one function (`event_stream`'s loop body). Worth doing when concurrent open traces become measurable, not before — the same trigger D-021 sets for the worker. | P11 | P3 | open |
| B-051 | 2026-08-16 | P8.3b | **A column's min/max came from the profiler's sample, so every card understated its range — and the agent refused answerable questions on the strength of it.** `pg_sample` returns the *first* n rows by design (it must not sort a customer's production table), so the extremes it sees are the extremes of the oldest rows. On the live demo catalog `orders.order_date` read **2025-02-01 → 2025-03-11** from a 5,000-row sample while the column runs to **2026-07-31** over 71,798 rows. Asked *"Why did revenue decrease in July 2026 compared with June 2026?"*, the agent replied that the catalog shows orders only to 2025-03-11 — a **correct inference from a card that lied**, and a wrong refusal wearing an honest one's clothes. Second time this class has bitten: WP4.3 fixed it for row counts (*"about 5,000 rows" about a 71,798-row table*). | P8 | **P1** | done (B-051 PR) — min/max now come from **one `MIN/MAX` aggregate per table**, not per column, so the cost is the same order as the sample beside it; bounded by the connector's timeout, and **any failure omits the range rather than falling back to the sample** (**D-025**). Only numeric and temporal columns are asked for one; free text carries **no range at all** rather than a sampled one. The guard is structural: `profile_column` has no code path from sampled values to a range, proved by reintroducing the old line and watching the test fail on the exact wrong dates the demo catalog held. A second test states the general rule over the card builder — a figure stated as a fact must be the engine's; one that can only come from the sample must say so, as `distinct in sample` and `examples:` already did, which is why only the range was wrong. Demo catalog re-profiled; the M8 scenario now answers |
| B-052 | 2026-08-16 | B-051 | **A structured call's output ceiling can be smaller than the schema it must fill.** `CallLimits` defaults to 1,024 output tokens — about 4,000 characters — while `Plan.sql` permits 20,000, and nothing connects the two. A model asked for a long statement has its reply truncated **mid-string**, and a truncated JSON object is not something the repair path can fix: the second attempt is cut off in the same place, so it costs two billed calls to fail. Found by the M8 revenue-decline scenario, which died with `did not produce valid Plan in two attempts` on a genuinely complex query. Fixed for the planner by sizing its limit to its own schema (`PLAN_OUTPUT_TOKENS`), but **the general defect stands** — every other structured call has the same mismatch waiting, and `FinalizeIn.answer` at 4,000 characters is the next to hit it. Wanted: derive the ceiling from the schema's declared maxima, or assert at startup that each structured call's limit can hold its schema, so this is a build error rather than a billed round trip. | P9 | P2 | open |
| B-053 | 2026-08-16 | P8.3b gate | **The duplicate-query block cannot be demonstrated on demand.** 4.4's rule works and is asserted twice in CI — `test_the_same_statement_is_never_sent_twice` and `test_a_repeated_query_counts_as_no_progress_and_is_never_sent`, both proving **zero extra `query_executions` rows** — but the M8 gate asks for it to be *visible in the trace*, and provoking it live needs a model that repeats itself, which a competent one does not. Asked a question deliberately containing the same sub-question twice, the real model answered it in a single query: correct behaviour, and no demo at all. When it does fire the trace shows a `reflection` reading *"That query has already run."* — honest, but not a dedicated marker, because 10.3's vocabulary is closed and inventing a type would put a word in the trace no UI was designed to render. | P11 | P3 | **accepted (owner, 2026-08-16) — covered by test, not demonstrated.** A developer-only switch replaying a scripted plan against the real stack was considered and **refused**: *a rigged demo is worse than a recorded gap.* The suite is the evidence; the Phase 8 GATE line records the gap in plain words, and the manual script says so rather than staging a step nobody can perform. Not a code task, and not to be reopened by building the harness — if this ever needs demonstrating, the honest route is a real question that genuinely makes a model repeat itself, which nobody has found |
| B-054 | 2026-08-16 | F&B source trial | **The profiler's sample is the first rows on disk, so on a real table it describes the wrong data.** `pg_sample` is a `LIMIT n` with no `ORDER BY` — deliberate, because sorting a customer's production table is not acceptable — and a warehouse table is physically ordered by load, so the first 5,000 rows of a year of sales are the first three weeks of January at one outlet. On the F&B source the card for `fact_sale` says `outlet_key` has **1 distinct** value (there are 5), `item_key` **18** (there are 211), and worst, the column that classifies a sale line reports **two** of its three values — omitting the one holding **80% of the table**. Nothing here is a lie: every figure is labelled `in sample`, which is D-025 behaving exactly as designed. But a planner reading a value set that omits the majority value writes a `WHERE` clause excluding four rows in five. The pizza fixture never showed this because it is generated in interleaved order; real data never is. Fix with a spread sample rather than a bigger one — `TABLESAMPLE SYSTEM (n)`, or a strided read over the primary key — and keep the `in sample` labelling either way. Note that the **range** on the same cards is correct, because B-051 moved it to the engine: the fix works, and this is the same disease in the part of the card it did not reach. | P10 | **P2** | open |
| B-055 | 2026-08-16 | F&B source trial | **A view never gets a row count, so its card cannot say how big it is.** `pg_row_estimates` reads `pg_class.reltuples`, which is `-1` for a view for the same reason it is `-1` for a never-analysed table: nothing has counted it. The catalog correctly declines to print a number it does not have, so all eight of the F&B source's `v_*` views are described as "is a view" with no size at all — and that source's own data dictionary says *"Runtime code should prefer `v_*` views"*, so the objects the customer intends to be used are the ones described least. A base table has a cheap honest estimate; a view has either an expensive exact count or nothing. Worth a bounded `COUNT(*)` under the profiler's existing per-query timeout, recorded as exact rather than estimated, with the timeout leaving the field absent as it is today. Related: freshly loaded tables are `-1` too until something analyses them, which is a property of the customer's database rather than of this code — `ops/seed/load_sqlite.py` ends with `ANALYZE` for that reason. | P10 | P3 | open |
| B-056 | 2026-08-16 | F&B source trial | **The capability gaps handed to the planner are truncated alphabetically, so it is told about the wrong tables.** Architecture 4.3 gives the planner the unreachable pairs up front *"so a well-behaved model avoids the dead end rather than being caught in it"*. `runner.py` takes `gaps[:20]`, and `unreachable_pairs()` returns them sorted by table name — so on the F&B source, all 20 the planner sees concern `bridge_item_ingredient` and `dim_business`, and **none of the 14 involving `fact_sale`** make the cut out of 385 total. The up-front warning is therefore noise, and the check catches the model afterwards instead of steering it: asked *"do we sell more on weekends?"* the planner reached for a view with no date column, was refused, and the refusal named a pair nobody was warned about. Never seen on the pizza fixture, which has fewer than 20 gaps in total. Fix by selecting the 20 that involve the tables `context_selected` just chose, not the 20 that sort first — the selected tables are already in hand at that point in `_investigate`. | P9 | **P2** | done (#55) — taken with B-057, since it is the same few lines. `runner.relevant_pairs` ranks by how much of the pair the question selected — both endpoints beats one — and keeps twenty of those rather than twenty of the alphabet. On a sales question the planner now hears about `fact_sale`, `dim_calendar`, `dim_outlet` and `v_daily_outlet_kpi`; it used to hear twenty facts about `bridge_item_ingredient` and none about any table in the question |
| B-057 | 2026-08-16 | F&B source trial | **Reachability through a one-row hub is treated as joinability, which is how a cartesian product gets called answerable.** The join graph is built from declared foreign keys and walked with BFS, so any two tables sharing a link to a common parent are "answerable". In a star schema every fact and dimension carries `business_key` referencing a single-row `dim_business`, giving that node degree 15 and connecting almost everything to almost everything: `fact_sale → dim_business → fact_purchase` is a legal two-hop path between 112,327 sales rows and 13,660 purchase rows whose only shared value is the one business key, and joining on it produces 1.5 billion rows. This is precisely the failure `capability.py`'s own docstring opens with — *"a query joining unrelated tables does not error, it returns a cartesian product, and a confident answer computed from one is indistinguishable from a real one"* — arriving through the check rather than around it. `dim_supplier → dim_business → dim_item` and a four-hop `fact_sale → … → meta_metric` are the same shape. The DAL's row cap bounds the rows returned and does nothing for an aggregate, which is the number a person reads. **Owner 2026-08-16: raised to P1 and scheduled before Phase 9** — the honest-refusal claim is the product's core promise, and the pizza fixture simply never had a hub table to expose this. **Proposed fix (WP8.4, spec in STATUS): direction, not degree.** A foreign key is many-to-one *by construction* — its target is a unique key — so every edge already carries a direction that the undirected adjacency throws away. Walking child→parent narrows (each row matches at most one); walking parent→child fans out. A path is a safe join exactly when it never turns **up then down** at an intermediate node, which is the textbook *chasm trap*: `fact_sale →up dim_business →down fact_purchase`. This is preferred over the three obvious alternatives because each of those fixes a symptom — a **degree threshold** is arbitrary and would trip a legitimate date dimension referenced by twelve facts while letting a two-row hub through; a **one-row-dimension exclusion** fixes this instance only, since a five-row hub still yields a fifth of a cartesian product just as confidently; and a **path-length cap** cannot separate them at all, because `payments → orders → customers` is two hops and safe while `fact → dim → fact` is two hops and fatal. The direction rule is structural, needs no threshold and no statistics, stays deterministic as 4.3 requires, and reads data `catalog_relationships` already stores. **Crucially it must not become a refusal**: two facts over a shared dimension *are* comparable, by aggregating each to a common grain first, so the verdict goes three-valued — `joinable` / `comparable` (aggregate each side, never join directly) / `unreachable` (refuse, as today). Turning the middle case into a refusal would trade this defect for a worse **B-058**. | **P8.4** | **P1** | done (#55) — built as specified. `JoinGraph` keeps the direction the foreign keys declare, `safe_path` searches over *(node, arrival direction)* refusing the up-then-down turn, and the verdict is three-valued. On the F&B catalog **143 of the 210 pairs the check used to call joinable were false** — two in three — while all 385 genuine refusals and every ordinary star join are unchanged. Proved live: *"how does our ingredient purchase spend compare with our net sales revenue?"* now returns a **CTE that aggregates each fact to `business_key` and joins the two aggregates**, answering RM 1,735,835.05 against RM 3,625,180.34 — both verified against the database to the cent — where before the guidance it had no reason not to join 112,327 detail rows to 13,660. See **D-026** for why direction beat the three threshold proposals |
| B-058 | 2026-08-16 | F&B source trial | **A join that exists in the data but not in the constraints is refused, and the refusal reads like a fact about the business.** The graph is `catalog_relationships`, which is declared foreign keys only — nothing is inferred, by design, because `MIN_CONFIDENCE` exists to keep a guessed edge from turning an honest refusal into a wrong answer. The cost lands on a schema that keys by convention: `dim_calendar.cal_date` and `fact_sale.business_date` are both `date`, they join perfectly, and *"do we sell more on weekends than weekdays?"* is refused because no constraint says so. **A false refusal, on a question the database answers** — the outcome the module's docstring calls worse than no check at all, *"because it teaches people the product is broken."* Fourteen of the F&B source's 35 objects have no declared edge in either direction, including every one of its eight views (a view can never have a foreign key) and both `map_*` tables, whose entire purpose is to link. Worth weighing: inferred edges above `MIN_CONFIDENCE` from name-and-type-and-value overlap, which is what the threshold was built for and nothing yet produces; a view inheriting the edges of the tables it selects from; and an admin-declared relationship, which needs no inference to be trusted. | P10 | **P2** | open |
| B-059 | 2026-08-16 | F&B source trial | **The customer shipped their own semantic layer and the agent cannot see it.** The F&B database carries a metric table (18 metrics, each with a definition and the tables it requires), a data-quality gate table (8 open questions, one of which is *precisely* the trap below), an assumptions table, a capability table, and a view listing every gate as open. Every one of these is in the catalog as an ordinary table, indistinguishable from a fact table, and nothing tells the agent that these rows are *authority* rather than more data to aggregate. The consequences are not theoretical: asked how many units of the top-selling set were sold, the agent answered **0** — correct SQL, and business nonsense, because that item's sale lines are all parent rows carrying money with a zero quantity, while the units sit on differently-named component rows. The database's own gate table asks that exact question, in English, one table away from where the agent got it wrong. This is **B-038**'s missing store seen from the customer's side: L1–L3 render as nothing, so there is nowhere to put a definition even when the customer has written one. What this adds to B-038 is the discovery that a mature source may already contain its semantic layer, so the feature is partly an *import* and not only an authoring UI. **Owner 2026-08-16: taken directly after B-057.** The point stands generally and is now written into the plan's WP10.2 — a semantic layer must be able to **import definitions a database already carries**, not only host ones somebody types in. A mature warehouse tends to arrive with its metric definitions, its assumptions and its open data-quality questions already written down somewhere, and a product that can only accept them retyped will mostly be given nothing. | P10 | **P1** | done (#72) — **service, routes, screen, and a live walk on the customer's own warehouse.** **Before**: *“which item brings in the most sales revenue, and how many units of it did we sell?”* answered **“Ayam Penyet Set … 0.00 units sold”** with `limitations: []` — the finding above reproduced exactly, and uncaveated. **Import**: `meta_metric` became **18 proposals**, each carrying its provenance and binding nothing. **Accept**: `prep_quantity` blessed by an Admin with `fact_sale.row_role not_in (parent_zero_qty)` — the rule this database's own `meta_gate` states in English as **G_ROWROLE** and records as `enforced = 0`. **After**: **“Ayam Penyet Combo … 157,258.26 … 21.48, 20.37, 18.42 … units”**, and the executed SQL carries `row_role <> 'parent_zero_qty'` inside a CTE. **A definition an Admin accepted changed the SQL a model generated, on a customer's warehouse** — which is the claim this entry was filed to make, and is no longer an assertion. The walk found **B-085** on the way: the import was **inert** until `accept` could give a metric the words people actually use, so this step would have proved nothing an hour earlier |
| B-060 | 2026-08-16 | F&B source trial | **Asked the same question twice, the agent chose different tables and gave answers two orders of magnitude apart.** *"Which raw ingredients cost us the most to buy?"* has three defensible sources in this schema: a purchase ledger (13,660 rows, RM 1,919,692 of invoiced spend), a stock-movement table filtered to its inbound code (10,464 rows, RM 1,522,521 of stock in), and that same table filtered to any of its seven other codes. Two live runs picked two different codes and answered **RM 642,930** and **RM 4,707** — the second from a filter matching **7 rows in 51,356**. Both were returned as answered findings, in prose, with no hedge and no mention that another table would say something else. The movement-type column has eight codes and no lookup table anywhere in the database. The failure is not the SQL, which ran and was cited correctly; it is that a genuinely ambiguous choice was made silently and differently each time, so the trace shows *what* was chosen and never that a choice existed. Bears directly on Phase 9: the deterministic critic can check that a filter on an undocumented code column is not left unexplained, and the composer can be required to name the table an answer came from when a comparable one exists. Related to **B-059** — this database's own metric table names the required tables for exactly this metric, and the agent cannot read it. | P9 | **P1** | open — **reproduced 2026-08-18 on live runs; diagnosis recorded, no fix attempted** (owner: *don't fix it silently*). Four runs of the exact wording now all choose the purchase ledger and agree to the cent, so the run-to-run divergence of 2026-08-16 did not recur — Phase 9 and 10 changed the prompt and the retrieval beneath it. **The instability moved rather than went away**: a paraphrase of the same question switched to the movement table and invented a *third* reading, a sign-of-quantity filter nobody has ever defined, then spent its whole iteration budget and refused. Four defensible readings of one question span a factor of **538**. **Three causes, each fixable alone.** (1) The card drops the counts the profiler already measured — `ColumnProfile.top_values` is `{value, count}` sorted by frequency and the card renders a bare list — so a code covering 0.01% of a table reads exactly like one covering 78%, which is how a filter matching 7 rows in 51,356 was answered without an objection (**B-092**). (2) The profile is the *first* rows rather than a random sample, so the card advertised five codes for a column that has eight and the third-largest never appeared — an incomplete vocabulary presented as a complete one (**B-092**). (3) Nothing says a choice existed: every run's `context_selected` names **both** candidate tables, the answer names neither, the critic passes and the answer carries zero limitations (**B-093**). Also confirmed: the customer's own metric table has **no** metric for this question and its two nearest metrics name *different* tables for ingredient cost, so the ambiguity is real in their model too and the semantic layer matches nothing — B-087's *“no definition matched”* is all that stands between the reader and a silent choice |
| B-061 | 2026-08-16 | WP8.4 live check | **Internal identifiers and the wrong currency symbol reach the reader in prose.** Two instances, both seen in live answers on the F&B source. A composed answer opened *"For B_AYAMPENYET in 2025…"* — the raw business key from the dimension table, meaningless to anyone outside the warehouse, where the business *name* sits one column away in the same row the query already touched. And every money figure in that answer carried **`$`** against a dataset whose own `dim_business` row records its currency as **MYR**; the catalog knows the currency and the composer does not ask. Group with **B-020** (`_col_1` in the evidence panel): the same family, which is the system's internal representation surfacing where a person reads, and it is why they should be fixed together rather than one at a time. None of these is wrong — the number is right, the key does identify the business — which is exactly what makes them a credibility tax at a demo rather than a bug report. Worth deciding as one rule: an answer names things the way the business names them, and a figure carries the unit the source declares. The currency half has a wrinkle worth stating now — it cannot come from the model's guess, and there is no currency column on `data_sources`; the honest routes are a per-source setting an Admin sets once, or a semantic definition once **B-059** lands, and the second is better because it is the same mechanism as every other fact about what a column means. | **P11** | P3 | open |
| B-062 | 2026-08-16 | B-005 | **A user cannot ask a question as of a past date.** **D-027** gives every run an `as_of` and defaults it to the wall clock, which is right for someone typing a question now and wrong for the two cases that will come up: reproducing a number from a report issued last quarter, and asking *"how did this look before the migration"*. The plumbing is already there — `execute_run(as_of=…)` and `ToolContext.as_of` — so what is missing is a way to say it and a way to see it: a date control on the conversation, the value carried on the run rather than inferred at execution, and the answer card showing the anchor whenever it is not today, because a historical answer that looks like a current one is worse than no answer. Two things to settle when it is built: whether the anchor belongs to a conversation or to a single question (a conversation, most likely — a follow-up should not silently move the window), and whether an `as_of` before a data source was registered should refuse rather than return an empty result that looks like a business fact. Not urgent: every question anyone has asked so far means *now*. | P11 | P3 | open |
| B-063 | 2026-08-16 | WP9.2a | **`test_a_retried_send_returns_the_run_that_already_exists` fails intermittently in a full run.** It asserts the conversation holds exactly one message after a duplicate send, and the assertion is racing the run it just started: when the background run gets far enough to compose, `record_answer` writes the assistant's reply and the count is two. It passes alone, passes with `-p no:randomly`, and has failed twice in randomly-ordered full runs — ordering changes the timing, not the logic. **Pre-existing**, and visible now only because two full runs happened close together; nothing in WP9.1 or WP9.2a touches idempotency. The fix is for the test to assert what it means — one message *from the user* — rather than a total that a completed run legitimately changes; scoping the count to `role = 'user'` is probably the whole change. Worth doing properly rather than by adding a sleep: a test that waits is a test that will fail again on a slower machine. | P9 | P2 | done (#60) — fixed in WP9.2b at the owner's direction, because a known order-dependent flake undermines every "CI is green" claim after it. The assertion now counts the **user's** messages: idempotency is a claim about what the sender created, so that is what it asserts, and the assistant's reply arriving or not is no longer part of it. No sleep, no retry, no ordering pin |
| B-064 | 2026-08-16 | P9 gate demo | **A conversation is not a conversation: every question is answered in isolation.** The owner asked a question, then typed *"check again"*, and the run replied that no business question had been given — because nothing about the previous turn was ever in front of the model. Traced end to end: `_question_of` reads `agent_runs.question`, one string for this run; `ContextBundle` has no field for prior turns; L5 renders that single question and nothing else. **No message other than the current one reaches any prompt, ever.** This is a specification gap and not only an implementation one — architecture 4.8's six layers have no slot for the thread, and the only place "conversation history" appears in the plan is WP11.2's *list* of past conversations, which is navigation rather than context. What *was* anticipated is the half D-022 built: 10.1 says a conversation carries its `data_source_id` "because a follow-up must reach the same source as the question it follows" — so follow-ups were foreseen as a concept and only their routing was built. Every real user hits this within a few messages: "and by store?", "why?", "same for June". Design questions worth settling before building: **which** layer it renders at (L5 with the question is the honest place — it is user-supplied text and must not sit above the platform rules); how much of it (a turn budget, since 4.4 already refuses to let a prompt grow with the length of an investigation); whether an *answer* goes back in or only the question (the answer's numbers are already durable and re-stating them invites the model to re-cite what it did not run); and whether a follow-up may reuse the previous run's executions as citations or must re-query. | **P10** | **P1** | done (#62) — **D-029** settles all four questions and the code follows in the same PR. The thread renders at **L5**, inside the question turn and above the question, framed as records rather than instructions exactly as a retrieved chunk is (owner's direction). **Three turns**, clipped, and a *truncation candidate*: dropped oldest-first before any table card is dropped, because a follow-up read without its thread is a question misunderstood while a question with no cards cannot be answered at all. The **answer goes back in**, since *"why?"* is meaningless without it — the frame says a number in an earlier answer is not a result this run obtained, and `_verified_citations` is the half that actually enforces it. A follow-up **re-queries**: a citation resolves through its own run, so a cross-run one would open onto nothing, and evidence that 404s is worse than none. One function renders the thread for all three prompts that need it — the layered one, the loop's reflection and the critic's rubric, the last because a critic asked whether a draft answers *"check again"* with no idea what was being checked will **falsely block a correct answer**. No migration; a first question's prompt is byte-for-byte unchanged. Found and fixed on the way: **B-067** |
| B-065 | 2026-08-16 | P9 gate demo | **A refusal reads as a broken product when the real problem is the wrong database.** The conversation screen does show which source a thread is bound to — a badge beside the title — but it is at the top of the thread and scrolls away, and the place a person needs it is beside the refusal that confused them. The owner asked *"how many orders were placed in July 2026?"* against the F&B source by mistake and got a fluent, correct refusal about there being no orders table; nothing on that message said which database it had looked in. Fix so the binding is visible **where the failure is read**, not only where the thread began: the strongest version is that a capability or grounding refusal **names the source in its own sentence** — "the F&B demo database has no orders table" instead of "the reference data has no orders table" — because that survives scrolling, screenshots and copy-paste, which a sticky badge does not. A persistent header badge is worth having as well and is the cheaper half. Related to **B-061** (internal identifiers reaching the reader) in that both are the system knowing something the person on the other side does not. | P11 | **P2** | open |
| B-066 | 2026-08-16 | P9 live eval run | **The eval harness's value checks assume the model's column names.** The first live run scored **12/20**, and five of the eight failures are one defect: `expect.value_of` names a result column — `store_id`, `cancelled_rate`, `repeat_rate`, `with_email` — and a real model aliases its output however it likes. `SELECT ... AS cancellation_rate` is a correct answer that this harness reads as a missing column. In FakeLLM mode the SQL comes from `golden.yaml`, so the alias always matches and the check looks sound; it is **only wrong in the mode that matters**, which is the mode a nightly job runs. Fix by comparing the single returned **value** rather than a named column when the result has one row and one numeric column, and falling back to a named column only for multi-column results — or by letting a case list acceptable aliases. Two of the remaining three failures are the harness expecting the wrong thing rather than the product misbehaving, and are worth fixing in the same pass: **#19** refused *because the data ends 2026-07-31*, which is exactly the last clause of D-027's rule working, while the case expects `answered=true` with a zero; and **#17** (*"how are we doing?"*) refused as too vague, which is defensible, while the case expects an answer. The eighth, **#14**, is a real gap and belongs to **B-018**: the model was not shown the `orders` card for *"which day of the week is busiest"*, because card search is lexical and nothing embeds. | P10 | **P2** | done (#63) — fixed, and **verified live**, which is the only mode that could show it. `match_value` now tries three rules in order: the named column (case-insensitively), then the value of a **1x1** result, then any cell of a **single row** of at most four columns. **The order is what keeps CI honest** — a fallback runs only when the named column is *absent*, so a result carrying that column with the wrong number still fails and nothing rescues it; `test_a_named_column_with_the_wrong_value_is_never_rescued` holds that. `may_refuse` was added for **#17** and **#19**, both of which have two right answers, and it is not a free pass: a refusal must still say why. Two things the live re-run corrected in this entry's own taxonomy. **#4 was never an aliasing failure**: the model answered *"Northgate"* rather than `3`, which is the **better** answer, since an internal key reaching a reader is itself a defect (B-061, B-020) — so `truth_any` now lets a case name two spellings of one fact, and `top_store_by_name` joins `decline.store_name`, which the generator had carried all along. And **#10 is not a harness problem at all**: it is **B-070**, a metric with two defensible denominators and nothing saying which is authoritative. The harness also gained the thing whose absence let this exist — **its own unit tests** (`apps/api/tests/evals/test_value_checks.py`, 21 of them, no database, no model, no dollar). It had only ever been exercised by running it, in the one mode where the defect could not appear. Live evidence on the affected cases: **#4, #8, #12, #16 pass**, #17 and #19 accepted as honest refusals, and the report prints *which* rule matched so a weaker pass is never mistaken for an exact one. #10 fails for B-070's reason and #14 still fails for **B-018**'s |
| B-067 | 2026-08-17 | B-064 | **A follow-up sees the previous answer's prose, never the SQL behind it.** D-029 puts the last three turns in front of the model as question-and-answer text, which is what makes *"why?"* and *"and by store?"* work. What it does not carry is the *statement* the earlier answer rested on, so *"check again"* re-derives a query from the catalog and may check something subtly different from what was originally run — the same silent choice **B-060** was filed for, now spread across turns instead of within one. Two runs of *"which raw ingredients cost us the most"* already picked two tables; a follow-up has strictly less to go on than the run it follows, because the run at least had its own executions. The fix is not simply to paste the SQL in: an earlier statement in the prompt is an invitation to re-run it verbatim against a period that has moved on, so this needs a decision about what a follow-up *inherits* — the canonical statement, the tables it named, or only the fact that a particular grain was used. Belongs with WP10.2's verified queries, which is the other place a blessed statement is handed to a planner. | P10 | P2 | open |
| B-068 | 2026-08-17 | B-064 | **Nothing tells anyone the thread was truncated.** D-029 caps context at three turns and drops oldest-first when the budget bites; `context_selected` records `history_turns` and that is the whole of the visibility. So the eleventh question in a conversation is answered from turns 8–10 with no sign, to the person asking or in the trace, that turns 1–7 were not considered — and a question like *"so what changed since I started?"* gets a confident answer about the wrong span. Two parts: WP8.3's timeline should say in words how much of the thread a run was given, and a run whose thread was clipped by the *budget* rather than by the cap should say so as a **limitation** (WP9.2's assembled kind, not a hedge the model writes). The composer already builds limitations from what the run knows and this is one more such fact. | P11 | P3 | open |
| B-069 | 2026-08-17 | B-064 live smoke | **`make agent.smoke` runs on the host, where the demo databases do not resolve — and the fix is already written next door.** Every demo source is registered by its **compose service name** (`seed-pizza-pg:5432/pizza`, `seed-fnb-pg`, `mssql`), which resolves only inside the compose network, so the M7 gate question fails on the host with `gaierror: [Errno 11001] getaddrinfo failed`, surfacing as `engine_error` / `sql_rejected` and composing into *"the query failed to execute"* — a DNS lookup wearing a product defect's clothes. **`ops/scripts/evals.sh` already solves exactly this** for the eval harness: it copies the files into the container and runs them there, its header explains why in four lines, and it even claims *"`scripts/agent_smoke.py` is run the same way and for the same reason"* — which is **not true**, because `make agent.smoke` still invokes it with `$(UV_API) python` on the host. So this is a documented decision that one of its two subjects never received. Fix: an `agent.smoke.docker` target mirroring `evals.sh`, and either point `make agent.smoke` at it or have the host path **say** the name cannot be resolved from here instead of letting it arrive as a failed query. One detail worth keeping from the B-064 evidence run: copy into **`/tmp`**, not `/app` — a write under `/app` trips uvicorn's reloader, whose startup sweep marks any in-flight run `interrupted`, which is what happened on the first attempt and looked for a moment like a product bug. `evals.sh` already uses `/tmp/evals` and this is presumably why. Same family as **B-042** with the sign reversed: there the host had the model configuration and the container did not. | P11 | P2 | open |
| B-070 | 2026-08-17 | B-066 live re-run | **A metric with two defensible definitions, and nothing says which is authoritative.** Golden eval **#10** — *"what proportion of customers ordered more than once?"* — has `customers.repeat_rate = 0.984471`, which is **7861/7985**: the denominator is customers who have *placed an order*. A live model wrote a `LEFT JOIN` from `customers` and computed **7861/8000 = 0.982625**, the proportion of *all* customers. Both are correct readings of the English, and the model's is arguably the better one, since "customers" plainly means all of them and the truth silently excludes the 15 who never ordered. The gap is 0.0018 against a tolerance of 0.001, so the eval fails a run that did nothing wrong. **This is not a harness defect and was deliberately not fixed in the harness** (B-066): widening the tolerance would accept genuinely wrong numbers, and rewriting the question to match the answer is the self-deception that whole backlog entry is about. It is the **semantic layer's** job — the same claim **B-059** makes from the F&B side, arriving here on the fixture this project designed, which is the more damning place for it to appear. **WP10.2 has a concrete before-and-after on the pizza data now, not only on the customer's**: define `repeat_rate` with its denominator, ground the planner in it, and #10 stops depending on which reading a model happens to pick. Until then #10 is a **known live failure with a known cause** and is recorded as such rather than hidden. | **P10** | **P2** | done (#72) — **closed with a live before-and-after: same question, same model, one definition apart.** Before: `FROM customers AS c LEFT JOIN orders o ON o.customer_id = c.id` — denominator 8000, `[FAIL] 0.984471 was in no result`, 5,960 tokens. After: `FROM orders GROUP BY customer_id` — denominator 7985, `[PASS]`, 6,102 tokens. **The definition changed the generated SQL**, which is this phase's whole claim, demonstrated on the fixture this project designed rather than on a customer's warehouse. `repeat_rate` is provisioned idempotently by `ops/evals/provision.py` and states in words which denominator is authoritative. It carries **no `required_filters` and does not pretend to**: the ambiguity is in which rows are counted at all, and no `{table, column, op, values}` predicate expresses *“customers that appear in orders”* — so it informs and does not bind (D-033), which is the honest shape. **Reachability is the fragile part**, so that is what the tests hold: nobody types `repeat_rate`, and the definition is reachable only through a synonym that appears in the question as asked — reword golden #10, or trim a synonym that looked redundant, and the grounding disappears while every scripted run stays green, because FakeLLM mode takes its SQL from `golden.yaml` and passes either way. `tests/evals/test_eval_definitions.py` reads the question out of `golden.yaml` and asserts it still reaches the definition, with two negatives proving the synonyms are narrow enough not to attach it to a different proportion. **The order matters**: this only works because **B-083** was fixed first — before that a matched definition reached the critic and never the model, so this definition would have changed nothing and the failure would have looked like a model ignoring guidance rather than a platform never giving it |
| B-071 | 2026-08-17 | P10.1 | **`knowledge_chunks` has no vector index, deliberately, and the decision needs a measurement.** Revision 0016 creates the column and the GIN index over `tsv`, and **no** IVFFlat or HNSW index over `embedding`. The reasons are real: an IVFFlat index must be built against rows that already exist — built on an empty table its lists describe nothing and searches miss — and HNSW's build cost is money on a B-series server. At V1 volumes an exact scan over **one organization's** chunks is fast, and unlike an approximate index it cannot silently fail to return the best match, which matters more here than latency: a missed chunk is a wrong answer with no symptom. What is missing is the *number* at which that stops being true. Measure retrieval latency against a realistic corpus (the F&B operator's dictionary is the honest test, not a fixture this project wrote), find the row count where an exact scan stops being acceptable, and only then choose. Whatever is chosen, build it **after** the backfill rather than before, and record the measurement rather than the conclusion. | P12 | P3 | open |
| B-072 | 2026-08-17 | P10.1a | **Two object stores that should be one.** `dal/artifacts.py` holds query results and `knowledge/store.py` holds uploaded documents, and both implement the same two properties by hand: a key prefixed with the organization, refused when it does not match the caller's, and a re-resolve afterwards so a reference carrying `..` cannot climb out of the root. They were kept apart deliberately rather than by accident — the DAL's interface is inside the security boundary, needs human review on every change (CLAUDE.md), and its `put(org_id, execution_id, payload)` signature is a *query result's* shape that the executor depends on; widening it so a knowledge feature could save a `.md` was the wrong trade in a WP that was already large. But the duplication is real and the copied half is the **safety** half, which is the worst kind to have two of: a fix to one is a fix nobody applies to the other. **WP12.2** already has to give both a Blob backend (`{org_id}/…`, arch 9.1), and that is the moment to converge them — one interface with a `key` rather than an `execution_id`, one prefix check, one path-escape test suite, and the DAL change reviewed on its own merits rather than as a side effect of a retrieval feature. Until then the check lives in two files and both have tests. | P12 | P3 | open |
| B-073 | 2026-08-17 | P10.1b | **`search_knowledge` runs on the lexical arm alone, because the agent has no embedder.** `knowledge/retrieve.py` is hybrid and its vector arm works — the ingest and retrieval tests exercise it — but the *tool* calls `search_knowledge` without an embedder, so a run gets Postgres full-text and nothing else. That is a degradation rather than a break, and a deliberate one: an embedder on `ToolContext` is a **spending capability reaching the agent loop**, and every guard around that today is built for the chat path — D-019's per-run cost ceiling reads `usage_ledger` rows for the run, and B-040's test guard wraps `registry.get_provider`, neither of which sees an embedding call made from inside a tool. Wiring it inside a retrieval feature would put an unmetered-against-the-run spend into the loop as a side effect. What is needed: `ToolContext` carries an embedder (or a factory), the run's cost ceiling counts embedding tokens, the B-040 guard refuses a non-stub embedder in tests exactly as it does a non-stub provider, and the trace says which arm answered. Until then the tool's docstring says so, and the practical cost is precisely **B-018**'s: a question worded differently from the document finds nothing. | P10 | **P2** | done (#67) — all four, plus the two things looking for the fourth turned up. **`get_embedder` is now the one door an embedder comes out of**, which is what makes the B-040 guard possible at all: the session fixture wraps that function exactly as it wraps `registry.get_provider`, refuses anything whose `is_stub` is false, in the same words, recording into the same list so the per-test check covers both. Proved by tampering — disabling the check makes the two guard tests fail with `DID NOT RAISE`. **`embed_texts` takes a `run_id`**, writes it on the ledger row and calls `budget.assert_within_limit` **before each batch**; both halves were tampered with and the right tests failed. A refusal **degrades rather than breaks** (**D-031**): the lexical arm still answers and `Retrieval.degraded` says which of the three ordinary reasons stopped the other one, with the tool putting that sentence *before* "nothing is written down about that" — the two together are the difference between a corpus that does not cover a question and a search that quietly halved itself. Two things this found on the way. **Nothing had ever built an embedder**: `OpenAIEmbedder` was constructed nowhere in the application, so the upload route ingested every document with `embedder=None` and the vector arm was dead in production as well as in the tool — the routes now use the factory, and re-index is the backfill for anything uploaded before a key. And **the query embedding bypassed the meter entirely**, because `retrieve.py` called `embedder.embed` rather than `embed_texts`; that is the same defect one layer below where it was noticed. Raised **B-075**, which is the reason this closes without changing what any run does yet |
| B-074 | 2026-08-17 | P10.1b | **Running two test directories together fails to collect, because every `conftest.py` is the module `conftest`.** `pytest tests/runs tests/agent` errors on four files with *"cannot import name 'Tenant' from 'conftest'"* — the agent suite's conftest won the name and the runs suite's imports resolved into it. **Pre-existing and confirmed as such**: the same command fails with this branch's changes stashed, so WP10.1b only met it rather than caused it. It does **not** affect CI, which runs the whole suite, nor a single directory, which is how these are usually run — so nothing is red today. What it costs is a developer narrowing a run to the two suites they are working on, which is exactly what someone does when a change spans `agent/` and `runs/`, and the error names an import rather than the collision so the first guess is always wrong. Fix by giving the shared helpers modules of their own — `tests/runs/tenants.py`, `tests/agent/…`, `tests/knowledge/…` — leaving `conftest.py` holding only fixtures, which pytest resolves by path rather than by module name. `llm_fixture.py` and `customer_db.py` at the tests root already work this way and never collide, so the pattern is established. | P11 | P3 | open |
| B-075 | 2026-08-17 | B-073 | **The research loop dispatches `run_sql` and nothing else, so `search_knowledge` can never actually be called.** Found while wiring B-073's embedder into a tool and asking what invokes it. The answer is: no code path in `agent/`. `loop.research` calls `plan_query`, which returns a `Plan` — one SQL statement, a purpose and an `answerable` flag — and the loop then calls `tools.call(context, "run_sql", …)` by name. `search_tables`, `describe_table` and `search_knowledge` are **registered, described to the model in the prompt, and unreachable**: a model that "asks" for one has nowhere to put the request, because the only structured reply the planner accepts is a statement. So WP10.1b shipped a tool the agent is told about and cannot use, and the corpus an organization uploads reaches no prompt. Nothing is broken in the sense of erroring — it is worse than that, because the tool list makes the capability look present. Two candidate shapes, and the choice is a real one. **(a) Retrieve into the context**, as table cards already are: `build_context` searches the corpus for the question and renders the passages at L4 with 7.4's framing — deterministic, no extra model call, and exactly what architecture 4.4's *Context* stage already describes (*"semantic definitions by name/synonym; knowledge chunks"*) and what plan WP10.2 asks for when it says definitions arrive in the prompt "when entities match". **(b) A genuine tool-choosing loop**, which is what 4.4's *Execute* stage says (*"pick next plan step → choose tool"*) and which D-024's call arithmetic was written against — a larger change, and one that puts a second model decision in every iteration. **(a) is almost certainly right for V1 and is WP10.2's business**, since that WP has to put definitions in front of the planner anyway and the two are the same mechanism; whichever is chosen, the tool list must stop advertising what cannot be reached. **Owner's direction 2026-08-18: this is a gate criterion, not a side item** — *"an agent that's told it can search documents but can't dispatch the tool means Phase 10 ships a feature the product can't reach"* — so it is **WP10.2a**, taken first, and the Phase 10 gate demo must show the agent **consulting a document mid-run**, not only the documents page working. That direction also settles the choice this entry left open in favour of **(b)**: the agent decides to look something up, rather than the context stage retrieving on its behalf. See **D-032**. | **P10** | **P1** | done (#69) — **WP10.2a**. `Plan` gained `define`: the planner names a term it needs explained and the loop dispatches `search_knowledge`, puts the passages at L4 and records both, so the tool the prompt advertises can finally be reached. **D-032** settles the shape and its load-bearing detail — a lookup **costs an iteration, not a model call**, because the lookup iteration runs no statement and so has nothing to reflect on, which leaves D-024's and D-028's worst case untouched. Bounded by `MAX_LOOKUPS` and by refusing a term already asked. Revision 0019 adds `knowledge_consulted`, a twenty-first event type, because `tool_called` records the asking and a lookup leaves no execution row to carry the answer. Two defects came out of running it live and neither was reachable from the suite: a model that needs a definition says so by **refusing**, and checking `answerable` first turned the one state the feature exists for into a dead run; and a duplicate lookup was refused **in silence**, so the model asked again and hedged an answer it had already computed. Raised **B-077** and **B-078** |
| B-077 | 2026-08-18 | WP10.2a | **`search_tables` and `describe_table` are offered to the model and the loop cannot dispatch them.** B-075's other half, left open deliberately and now visible in a test rather than invisible in a prompt. WP10.2a made `search_knowledge` reachable, because that is what the Phase 10 gate turns on; the two catalog tools are still described in every planning prompt and still unreachable, so a model that asks to look at a table gets silence and a worse answer for a reason nobody can see. They are **redundant** today rather than merely unwired — `build_context` selects the cards and renders them up front, which is why nothing has missed them — but redundancy is an argument for removing them from the prompt, not for leaving a promise the product cannot keep. Two options and they differ in cost. **(a) Deregister them**: one line, the prompt gets shorter and honest, `tests/agent/test_tools_live.py` builds its own registry instead of using the default, and the capability genuinely goes away. **(b) Make them dispatchable**, the way `define` made `search_knowledge` reachable — worth more than (a) the day a card is squeezed to its headline by 4.4's budget and the model needs the full one, which is exactly when the tables are numerous and the answer matters. `tests/agent/test_knowledge_in_the_loop.py::NOT_DISPATCHABLE` names both with their reason and fails if a third arrives, so this cannot quietly get worse while it waits. | P11 | P2 | open |
| B-078 | 2026-08-18 | WP10.2a live run | **A retrieved definition is prose, so nothing checks that the SQL kept it.** Seen live and it is the sharpest evidence yet that WP10.2b's critic rule is load-bearing rather than a nicety. The agent asked what an *anchor order* was, the document said *"a completed order of more than 40 pounds placed on a weekday… neither is anything placed on a Saturday or a Sunday"*, and iteration 2 wrote **exactly** that — `status = 'completed' AND total_amount > 40 AND EXTRACT(ISODOW …) BETWEEN 1 AND 5`, which no model could have guessed. Then iterations 3 and 4 talked it out of the weekday clause and the answer came back as **1,054 "across all days of the week, rather than the weekday-only count"** — against 747, which was right. The definition reached the model and the model discarded it, in the open, with a rationale. **Nothing in the system could object**, because a passage retrieved as prose carries no machine-readable filters for a critic to compare the AST against; plan WP10.2b's rule (*"if a used metric has a definition with required filters, the executed SQL must contain them"*) is written for **semantic definitions**, which are structured, and this was not one. Two ways to close it and they are not exclusive: have the semantic layer be where a *document* definition lands too — an admin blessing a retrieved passage into a structured definition, which is **B-059**'s import path pointed at prose — or give the critic a weaker, honest check it can run on prose: the draft must not contradict a definition the run was shown, judged by the LLM half rather than the deterministic one. The first is the real fix; the second is worth having anyway, because a run can be shown a definition nobody has blessed. | **P10** | **P1** | **accepted (owner, 2026-08-18) — covered by test, not demonstrated.** The critic rule shipped in WP10.2c and is asserted both ways in `tests/agent/test_required_filters.py`: it fires on a query that drops a required filter, and stays silent on `status = 'completed'`, which honours *“exclude cancelled orders”* without containing the word. What could not be produced is the **live** half this entry asked for — a run where a real model drops the filter and the critic catches it. Four attempts against the F&B warehouse, three outcomes, none of them that one: the model **complied**, writing `row_role <> 'parent_zero_qty'` into a CTE unprompted; told explicitly to include every row it **refused and named the rule** — *“the authoritative prep_quantity definition requires excluding fact_sale rows where row_role is parent_zero_qty”*; and asked about an item whose every row is excluded by that filter it **refused honestly** rather than reporting the 0.00 it used to report as fact. No blocking critic verdict was recorded on any run of the walk. **B-083 changed what this criterion is able to show, and that is the finding worth keeping.** Before it, a matched definition reached the critic and never the model, so the model dropped required filters *every time* — this criterion would have been met trivially and would have proved the opposite of its intent: not that a constraint binds a model that saw it, but that a model is punished for not reading minds. Now that the definition reaches the prompt, **compliance is the correct behaviour**, and a drop-and-catch may simply not be stageable against a competent model without rigging the run — which the owner ruled out, for the reason B-053 was accepted on: provoking a failure to film it is not evidence, it is set dressing. The rule's *value* is unchanged and is not conditional on being filmed: it runs on every answer, it is deterministic, and D-034 guarantees that a block which does stop a run reaches the reader as the answer's first limitation. Revisit if a weaker model is ever configured for the planner role, where a genuine drop is likelier |
| B-079 | 2026-08-18 | WP10.2c live run | **A critic block on the last permitted pass ships the answer anyway, and the objection is invisible.** Seen live, and it lands squarely on the claim this WP makes. The model wrote `status = 'completed'` for a metric defined as *none of cancelled, refunded*; the deterministic rule warned, the **LLM half blocked** — *"the query does not clearly exclude cancelled and refunded orders as required by the metric definition"* — the run re-entered once, wrote the same shape, and was blocked again: *"the draft overstates the query logic… that qualification should be removed or verified."* The final answer shipped as *"using completed orders and **explicitly excluding cancelled and refunded orders**"*, which is the overstatement the critic had just named. Two things are wrong and only the second is hard. `runner` re-enters while `_may_re_enter` allows it and otherwise keeps the draft — correct, since M9 caps the passes — but `composer.limitations_for` takes **only `verdict.warnings`**, so a `BLOCK` that could not be acted on is dropped on the floor: the one finding strong enough to stop a run is the one the reader never sees. The cheap half is to surface an unresolved block as a limitation in the answer, in the critic's own words. The harder half is what such an answer should *be*: shipping a draft the critic rejected with a caveat attached is defensible, and refusing outright may be righter for a block about the number itself rather than about its description. Whichever, it must not be silent — an answer that carries a confident sentence the platform knows to be false is the exact failure Phase 9 was built to prevent. **Owner's direction 2026-08-18: this blocks sign-off and is a WP10.2d gate criterion, not a backlog item.** The rule is **D-034** — *any critic finding strong enough to stop a run must reach the reader; if a run ships despite a block, the block becomes the loudest limitation on the answer, not a silent trace entry.* Settled with it: the answer **ships** rather than refusing, because the number is often right and only its description overstated, and refusing would turn 4.5's "violations become warnings in V1" into a refusal engine. | **P10** | **P1** | done (#72) — shipped with the gate. `composer.limitations_for` reads the verdict's **blocks** as well as its warnings, so an unresolved block becomes the answer's **first** limitation in the critic's own words, and the answer can no longer describe itself as highly confident. **D-034** is the rule it implements. The demo the entry asked for — a live run where a block survives the last pass — was not produced: see **B-078**, accepted as covered-by-test for the same reason and by the same argument, since after **B-083** the model sees the definition and complying is the correct behaviour. Asserted in `tests/agent/test_required_filters.py` (`test_an_unresolved_block_becomes_the_answers_first_limitation`, `test_the_block_outranks_the_budget_caveat`, `test_a_disputed_draft_cannot_call_itself_highly_confident`) |
| B-081 | 2026-08-18 | session end | **Nothing guards BACKLOG.md, and a row was silently merged into another.** Filing B-080 prepended its row to B-076's and lost the newline between them, so two entries became one line: every character of B-076 survived, but it no longer began a row, `grep '^\| B-076'` found nothing, and a later edit to B-076's own text landed inside what read as B-080's cell. Caught by an id audit at session end, not by anything in the repo. The file is **append-only, never renumbered, never deleted** (plan §1.5) and it is the only record of every deferred decision this project has made — losing a row loses the reason something was not done. **B-019** built exactly this guard for STATUS.md after #24 gutted it, and the argument transfers unchanged: a file only a careful human reads is one that eventually nobody reads carefully. Wanted, in `hygiene` beside `check_status.sh`: ids are unique, contiguous from B-001 with no gaps, every row starts a line and has the seven columns the header declares, no id present in the base branch has vanished, and `Prio`/`Status` hold values from the vocabulary §1.5 names. The id-and-gap check alone would have caught this in the commit that caused it. Worth a `--selftest` like check_status's, so a guard that has stopped matching fails instead of passing everything. | P11 | P2 | done (#72) — `scripts/check_backlog.sh`, run by `hygiene` before the check it tests, and by `make check.backlog` / `make preflight`. Three questions: every row begins a line and carries the seven columns the header declares, with `Prio` and `Status` from the vocabulary; the ids are unique and contiguous from B-001; and no id present on the base branch is missing here. **Replayed against `dc35e7a`, the commit that caused this entry, it reports the lost row four independent ways** — B-076 not beginning a line, a gap where B-076 should be, B-080's row carrying 15 columns, and B-076 present on the baseline and absent here — where CI reported success. It carries a `--selftest` of fifteen crafted files, run first in CI, and its merged-row case reproduces this defect exactly. **Its first run on the real file found two more.** B-013 and this row were both rendering wrong on GitHub: an unescaped `\|` inside a code span split them into 10 and 8 columns, and GFM drops what overflows the header — so B-013's Suggested phase, Prio and Status cells were being rendered away, on a public repo, unnoticed since Phase 3. Both are now escaped, and the rule is written down. Order is deliberately **not** checked: restoring B-076 by hand put it back at the end of the file, which is where an append lands and costs nothing. The Status vocabulary was widened to name `in progress` and `accepted`, which two rows already used — a guard enforcing a vocabulary the project does not use is one that gets switched off (§1.5, §2.3) |
| B-080 | 2026-08-18 | WP10.2c CI | **A CI job could hang for six hours on somebody else's apt mirror, and one did.** On #71 the `mssql` job's *Install the SQL Server ODBC driver* step sat at `apt-get update` against `azure.archive.ubuntu.com` for **32 minutes**, emitting `Ign:` lines and failing over to `archive.ubuntu.com`, with no end in sight — GitHub's default job timeout is **360 minutes**, so nothing would have stopped it before six hours of runner time. **Fixed in the same PR** because it was blocking that PR: `timeout-minutes: 20` on the job, `timeout-minutes: 8` on the step (so the failure names the step rather than the job), and `Acquire::Retries=3` with a 15-second timeout in `install-odbc.sh`, which the Docker image runs too. Filed rather than merely fixed because the **class** is not closed: `mssql` is the only job that installs from a third-party repository, but no job in this workflow has a timeout, and the next hang will be somewhere else. A sweep giving every job a ceiling proportionate to its honest runtime is small, and the argument for it is that a hung job is indistinguishable from a slow one until somebody looks. | P11 | P3 | done in part (#71) — the `mssql` job and the apt calls are bounded; the sweep across the other jobs is what remains |
| B-076 | 2026-08-17 | B-073 live run | **A call too cheap to round to a millionth of a dollar is recorded as costing nothing.** `meter.estimate_cost` quantizes to six decimal places, matching `usage_ledger.cost_usd`, and rounds half up — so a **7-token** query embedding at `text-embedding-3-small`'s $0.02/M costs $0.00000014 and is stored as **$0.000000**. Seen in the B-073 live run, where the row is `ok`, carries its token count, and reports a cost of zero. This was harmless while every metered call was a chat completion costing a fraction of a cent; it is not harmless now, because a query embedding is the cheapest and most frequent call the system makes and D-019's ceiling sums exactly this column. A run doing a thousand searches spends about $0.00014 and shows **$0.000000** against its cap, which is a ceiling that cannot see a whole category of spend — the same failure the unpriced-model rule exists to prevent, arriving through rounding rather than through a missing price. Note what is *not* wrong: `input_tokens` is exact, so the information is on the row and only the money column loses it. Options: widen `cost_usd` (a migration, and the natural moment is whenever the next one lands), keep an exact `Decimal` and quantize only at the point of display, or accumulate sub-cent spend per run rather than per row. **Owner's direction 2026-08-18: meter in tokens, not rounded dollars** — the ceiling should be computed from `input_tokens`/`output_tokens`, which are exact and already on every row, with the price applied at the comparison rather than stored per row. That makes a thousand tiny calls sum to something a cap can act on without a migration to widen `cost_usd`, and it leaves the money column as the human-readable report it already is. Ships with a test that a thousand 7-token calls reach a ceiling. | P12 | P3 | open |
| B-082 | 2026-08-18 | WP10.2d routes | **Every semantic-layer route is Admin, including the read side, and the read side is the half that deserves a second look.** Writing a definition is plainly Admin work: `required_filters` becomes a rule the critic enforces against generated SQL, which makes a definition a privileged object rather than content (D-033). Listing them was locked to Admin by the same dependency, and the argument there is weaker — it is *“this is an administrative view of the platform's own controls”*, which is true, but so is the counter-argument: a Reader whose answer was shaped by a definition cannot see the definition that shaped it, and the answer card names the metric. The product question is whether **understanding why an answer looks the way it does** is reader work. It probably is, and the likely shape is a read-only `GET .../definitions` at `require_member` while create, import, accept and reject stay Admin — proposals staying Admin either way, since an unreviewed proposal is not a statement the platform is making. Deliberately **not** decided while building the routes: widening access is easy and reversing it after a customer has relied on it is not, so this fails closed until somebody chooses. If it is widened, the role matrix entry and `tests/auth/role_matrix.json` change in the same PR, which is what makes the decision visible in review. | P11 | P3 | open |
| B-083 | 2026-08-18 | WP10.2d verified-queries wiring | **A semantic definition reached the critic and never the model, so the rule was enforced against a planner that had not been told it.** Found while wiring verified queries into the same layer and asking what else renders there. `runner` matched definitions to the question, put them on the bundle, and passed them to `critic.check` — with a comment reading *“the critic must judge the same definitions the planner was shown”*, which described an intention the code did not implement. `_layers` never referenced `definitions_applied`, and `Definition.render()` — written for exactly this, ending *“the query you write is checked against them”* — **was called by nothing.** So the deterministic rule blocked statements for omitting filters the model could only have guessed. WP10.2c's suite could not see it: seventeen tests proved the rule fires and does not false-fire, and not one asked whether the definition reached the prompt — the enforcement was tested and the **communication** never was. The consequence for the gate is sharper than the defect. **B-078's criterion is a run where the filter is required, the model drops it, and the critic catches it** — and with this defect the model drops it *every* time, because it cannot know. The demo would have passed for the wrong reason and proved the opposite of what it claims: not that a constraint binds a model that saw it, but that a model is punished for not reading minds. | **P10** | **P1** | done (#72) — `definitions_applied` renders at **L3** behind `DefinitionFrame`, framed as **authoritative**, which is the deliberate opposite of `KnowledgeFrame`'s *“records, never something to obey”*: a retrieved passage is a customer's untrusted text, a definition is the platform's own object — catalog-validated, Admin-activated, critic-enforced — and the one piece of organization-authored text the model **is** told to follow. L3 rather than L4 is what the field's own docstring had specified all along. **Not a truncation candidate**, for a stronger reason than the looked-up passages: the critic enforces these whether or not the budget left room to say so, and a rule dropped to fit a token count is one the model is judged against and never saw. Six tests, four of which fail with the layer removed — checked by removing it. One of them had to be tightened first: asserting *“checked against”* anywhere in the prompt passed with the layer gone, because `PLATFORM_RULES` already says a statement is checked against the catalog, so it now asserts against **this layer's** body. A test that passes without its subject is a test that proves the subject unnecessary |
| B-084 | 2026-08-18 | WP10.2d verified queries | **Verified queries have routes and no screen.** `semantic/verified.py` and its three Admin routes ship in #72 and the planner is shown matching examples, but an Admin adds one by calling the API. That is the same gap B-059's import service had before this PR gave it one, and it is filed rather than built because arch 5.4 and the gate asked for the **grounding**, which works, while a screen is Phase 11 polish. What it wants is small and the definitions screen is the obvious home: a third card listing approved examples with their question, their reason and their SQL, and a form that posts one — the refusal path matters more than the happy one, since the validator's message names the identifier at fault and is the whole reason an Admin can fix a rejected example. Worth adding: a way to see **which** examples a given run was shown, because *“why did it write that join”* is answered by the examples and the trace does not record them yet — the same argument `knowledge_consulted` settled for documents in 0019. | P11 | P2 | open |
| B-085 | 2026-08-18 | WP10.2d live F&B walk | **An imported definition is unreachable, so the whole import binds nothing.** Found in the gate's own live walk, after the import had worked perfectly. A definition is matched to a question by **name and synonym**, whole-word; an imported one answers only to its key and to whatever label its own table carried. Nobody asks a question in those words — and the proof is in the same database: the customer's own golden-question table pairs each metric with the question it answers, and **not one of those questions contains the metric's key or its label.** So eighteen metrics imported, one accepted with a filter that binds, and every real question sails straight past it. The failure is silent and reads as the feature not working rather than as the metric not being found: the run answers exactly as it did before, with no definition applied and nothing saying so. It is the same lesson **B-070** learned on the pizza fixture — *nobody types `net_revenue`* — arriving from the customer's side, where it is worse, because there the Admin did not write the definition and has no reason to suspect its name is the problem. | **P10** | **P1** | done (#72) — **`accept` takes `synonyms`**, because acceptance is the moment an Admin makes an imported metric reachable, and it was already the moment they make it bind. `None` keeps what the import found and a list replaces it: replacement rather than appending, since correcting a wrong label has to be possible. The distinction matters and has its own test — an Admin who only wanted to add a filter must not silently strip the metric's own label and make it unreachable, so an **absent** field and an **empty list** mean different things all the way from the screen to the row. The definitions screen grows an *“Also called”* field prefilled with what the import found. **What this does not fix**: nothing tells an Admin that a metric is unreachable, and the honest surface for that is the run — *“this question matched no definition”* is knowable and is not shown. Filed with B-084, which wants the same thing from the other end: which definitions and examples a given run was actually shown |
| B-086 | 2026-08-18 | gate demo prep | **The API container was never given the embedding model, so nothing embedded in the product.** `ops/docker-compose.yml` passes `LLM_PROVIDERS`, `LLM_MODELS`, `LLM_ROLE_MAP`, `LLM_PRICES` and the keys — under a comment explaining that runs happen *inside the API process*, so a value living only in the developer's shell reaches the smoke script and never the product — and then does not pass `EMBEDDINGS_PROVIDER`, `EMBEDDINGS_MODEL` or `EMBEDDINGS_DIMENSIONS`. The same argument applies to them word for word. Found while preparing the Phase 10 gate demo: `get_embedder()` returned **None** in the container while the identical question answered from the host had a working embedder, so every card and every document in the browser was **lexically searchable only** and D-031 quietly degraded the vector arm of every search. **This is B-073's defect wearing a config file instead of code** — there the application never *built* an embedder, here it builds one and is never told which model — and it lands on two of this phase's own claims, B-018's hybrid card search and B-073's hybrid document search, in the one environment a gate is walked in. It cost nothing to find and would have cost the demo its retrieval story. | **P10** | **P2** | done (#72) — the three variables are passed through, defaulted to empty like every other optional key, with the reason written beside them. Verified in the running container: `embedder built: True`. **The class is not closed** — nothing compares the environment a developer runs against the environment the container gets, and the next variable added to `.env` will go missing the same way. A test that asserts every `Settings` field with a `.env.example` entry is either passed through by compose or explicitly listed as host-only would close it. **Filed as B-090** and raised to P1 by the owner at the Phase 10 sign-off — *“this deserves a guard, not a note”* |
| B-087 | 2026-08-18 | gate walk, first attempt | **The import screen could not carry the customer's own names, so every UI import produced metrics no question could reach.** Found by the owner walking the gate script: eighteen metrics imported, one accepted, and the demo question answered exactly as it had before. The cause was three failures stacked, and the product half is this: the API's import accepts a `synonyms_column` and **the screen never offered one**, so the `metric_name` column — where that warehouse keeps the words people actually use — was left on the floor and every proposal arrived with `synonyms: []`. **B-085 fixed only half of this**: an Admin could add synonyms when accepting, but nothing brought in the ones the customer had already written, and the accept field prefills from what the import found — which was nothing. The other two failures were in the script: its mapping table omitted the names column, and step 4 never said to fill *“Also called”*, so a correctly-followed script produced a definition matching nothing. **The failure is silent and wears the wrong face** — the run answers as it always did, no definition applied and nothing saying so, which reads as the feature being broken rather than as the metric not being found. That is the third time this exact shape has appeared: **B-070** (nobody types `net_revenue`), **B-085** (an import answers only to its key), and now the screen that cannot carry the names at all. | **P10** | **P2** | done (#72) — a **Names column (optional)** field on the import form, sending `synonyms_column`, with two web tests: one that it is carried, one that the optional columns are **omitted** rather than sent empty, since an empty string is a column name the API would look for and never find. The script gains the field, an explicit *“Also called”* step, a warning that **Accept and enforce** is not **Accept as prose**, and a checkpoint on the green **enforced** badge. **And the half this entry called the one that would have caught all three is now built** (owner's direction, 2026-08-18 — *“three dead ends that looked like breakage is exactly the problem worth an hour”*). A run records `definitions_available` beside `applied_definitions`, both reach the run response and the trace event, and the answer card reads **“governed by prep_quantity”** or **“no definition matched this question (18 defined here)”**. **The two numbers only mean anything together**: empty beside **0** is an organization that has defined nothing and gets silence; empty beside **18** is the finding. That silence is deliberate and is the design's whole discipline — a caveat on every answer is how people learn to stop reading caveats, which is B-079's failure arriving from the other direction, so the line appears only in the state nobody could see. Proved on a live run in the owner's own organization: a question naming no metric recorded `applied=[] available=18`. Old runs carry neither field and render silently rather than breaking — the page whose whole job is explaining what happened must not be the page that breaks. Four web tests including the silence case and the older-run case, three API tests. Suites: API **1408**, web **113** |
| B-088 | 2026-08-18 | gate walk, second attempt | **An accepted definition cannot be corrected — there is no edit, no un-accept, and re-accepting is a 404.** Found by the owner mid-walk: having accepted the whole review queue rather than the single metric the script meant, there was no way through the product to give `prep_quantity` the filter it needed, because `accept` refuses anything that is not still `proposed`. The only route back was deleting the row in `psql` and importing again. **For a feature whose entire premise is that an Admin decides what binds, being unable to revise that decision is a hole in the premise** — and the likeliest moment to get a filter wrong is the first time you write one, which is exactly when you are locked out. It also makes B-078's own criterion awkward to rehearse: correcting a filter to provoke a critic block means a database round trip. Wanted: `PATCH .../definitions/{id}` for an **active** definition — `required_filters`, `synonyms`, `description` — validated against the catalog exactly as `accept` is, Admin-only, and **audited**, since it changes what the platform enforces on generated SQL. `status` belongs in the same route or a sibling: retiring an active definition has the same shape and the same absence. Worth deciding at the same time whether an edit should be **versioned** rather than overwriting — arch 5.4 says definitions are *“validated against the catalog at save time and versioned”*, and nothing here versions anything yet, so a run that cited a definition last month cannot show what it said at the time. | **P11** | **P1** | done (#75, and the web half in the PR that closes this) — **raised from P2 by the owner on 2026-08-18**, having hit it during the gate walk: *“a semantic layer whose definitions are write-once will not survive real use.”* Split in two because the whole of it is past the §1.1 size target. The **API half** (#75) shipped `PATCH`, `DELETE` (retire) and `GET …/versions`, audited all five decisions and versioned the row (**D-036**, migration 0022); the **web half** puts editing, retiring and the history on the Definitions screen, sending only the fields that changed because the API reads an absent one as *leave it alone*. Versioning was decided rather than deferred — see D-036 for why the day editing ships is the day overwrites start costing something |
| B-089 | 2026-08-18 | gate walk, third attempt | **A required filter applies to the whole metric, and a question that mixes two metrics has no way to scope it.** Seen live and it is not a defect so much as an unmodelled case. `prep_quantity` requires `fact_sale.row_role not_in (parent_zero_qty)`, which is right for counting units and **wrong for summing revenue** — the parent rows are precisely where the money is. Asked *“which item brings in the most sales revenue, and what is the prep quantity for it?”* the model has to decide where the filter stops, and both readings are defensible: apply it to the units half only and the top-revenue item turns out to have no countable rows at all, giving an honest refusal; apply it to the ranking too and a different item becomes “top”, giving real numbers for a subtly different question. Two runs produced one of each. **The deterministic critic cannot tell these apart** — its rule is *“the executed SQL must contain the filter”*, and both satisfy it, in one CTE out of several. So the rule is weaker than it reads: it proves the filter appeared, not that it governed the number the answer quotes. Worth deciding: whether a filter should carry the **grain it applies at**, whether a definition should be able to say *“this metric may not be combined with an unfiltered aggregate over the same table”*, or whether this is simply the honest limit of an AST check and belongs in the answer's limitations instead. Filed rather than guessed at, because the wrong fix here manufactures false blocks on legitimate multi-metric questions, which standing note 5 calls this component's characteristic failure. | P11 | **P2** | open |
| B-090 | 2026-08-18 | B-086, owner's direction at Phase 10 sign-off | **Nothing compares what a developer's environment has against what the container is given, so a missing variable is invisible exactly where the product runs.** **B-086** was one instance: `ops/docker-compose.yml` passed every `LLM_*` key under a comment explaining that runs happen *inside the API process*, and then did not pass `EMBEDDINGS_*` — so `get_embedder()` returned **None** in the container while the identical question answered from the host had a working embedder, and every card and document in the browser was lexically searchable only. Owner at sign-off: *“this deserves a guard, not a note. Nothing compares a developer's .env against what compose passes to the container, so the next variable added goes missing the same way — silently, and only in the container where the product actually runs.”* **The measurement, taken when this was filed**: `.env.example` documents **48** keys, `ops/docker-compose.yml` references **46**, and **12** documented keys are never passed to any service. Most of those twelve are legitimately host-only — `DATABASE_URL` and `APP_DATABASE_URL` are rebuilt for the compose network, `SEED_*_HOST` is `localhost` for tools run on the host, the `MSSQL_*` block belongs to an on-demand container — **which is the whole design constraint: a plain diff is useless because the legitimate absences outnumber the defects.** So the guard must be a **declaration**, in the idiom `TENANT_TABLES` already uses: an explicit `HOST_ONLY` list naming every key deliberately not passed and why, and a check that fails when a key is in `.env.example` and in neither the compose environment nor that list. Adding a variable then costs one deliberate line — pass it, or say why not — and forgetting costs a red build instead of a silently degraded product. Belongs in `hygiene` beside `check_status.sh` and `check_backlog.sh`, and wants their `--selftest` for the same reason: a guard that has stopped matching must fail the build rather than pass everything. **Worth extending to `Settings` in the same pass** — `config.py` reads **35** fields, and a field with no `.env.example` entry is undiscoverable to anyone setting the project up, which is the same defect facing the other way. | **P11** | **P1** | done (#76) — `scripts/check_env.sh` asks five questions, carries a `--selftest` and runs in `hygiene` before the check it tests. Run against the repo it found **six more instances of B-086's class**: `DAL_MAX_ROWS`, `DAL_TIMEOUT_SECONDS`, `ARTIFACT_RETENTION_DAYS`, `EMBEDDINGS_BATCH`, `LLM_REFUSE_UNPRICED_WHEN_CAPPED` and `OIDC_ISSUER`, all documented and passed to nothing. All six are passed now; fourteen genuinely host-only keys are declared with reasons. Passing them safely needed one rule — **an environment variable set to nothing is unset** — because `${VAR:-}` is how compose passes a key the developer has not set, and an integer field refused to parse it; that also fixes a latent break B-086 shipped, where a `.env` missing `EMBEDDINGS_DIMENSIONS` was a container that could not start |
| B-091 | 2026-08-18 | B-088, while versioning definitions | **A run records which definitions governed it by *name*, so a citation cannot be resolved to the version that was in force.** `state.applied_definitions` is a list of strings and the `definitions_applied` event carries the same, which was enough while a definition was write-once: the row said what it had always said. **B-088 ends that.** An Admin can now correct a filter, and from the moment they do, an answer from last week cites a rule that has since changed — the trace shows today's definition beside yesterday's SQL and nothing marks the seam. D-036 built the half that makes this fixable: `semantic_definition_versions` holds every state and `semantic_definitions.version` names the live one, so the run has something stable to point at. Wanted: the applied definitions recorded as `(id, version)` rather than as a name, the trace resolving a citation through `GET …/versions`, and the answer card saying *“this was checked against version 2, which has since been edited”* when the two differ. Worth doing before an eval regression is ever debugged across an edit, because the first symptom is a golden question that used to pass and a definition that reads as though it always said what it says now. | P11 | P2 | open |
| B-092 | 2026-08-18 | B-060 reproduction | **A catalog card describes an undocumented code column as an unranked list of examples taken from the first rows of the table, and a model reads it as the column's vocabulary.** Two defects in one sentence of `cards.py`. **The counts are measured and then discarded**: `ColumnProfile.top_values` is a list of `{value, count}`, already sorted by frequency, and the card renders `examples: A, B, C` — so a code on 0.01% of rows is typographically identical to one on 78%, and **B-060's smallest answer came from a filter matching 7 rows in 51,356** with nothing in the prompt to suggest that was odd. **And the vocabulary is incomplete without saying so**: the profile is the first `max_rows` rows rather than a random sample — deliberately, since ordering would sort a customer's table — so on the customer's warehouse the card advertised five codes for a column with eight, and the third-largest, on 10% of rows, never appeared. `distinct in sample` is honest about the count; the examples are not honest about the same thing. Wanted: the share beside each value, from counts the profile already holds; a phrase saying the list is the head of the table rather than a survey of it; and existing cards rebuilt in the same migration, as revision 0013 already did once for B-039. **Prerequisite for the critic rule B-060 asks for** — *a filter on an undocumented code column selecting a tiny share must be explained* needs the share to be known. | P11 | **P1** | done (#78) — the card now carries each value's share from counts the profile already held, and says the rows it read were the table's first. On the warehouse B-060 came from, `move_type` went from `examples: DO, PI, UC, CN, GR` to `DO 78%, PI 17%, UC 4%, CN 0.2%, GR 0.1%`. **No migration**: revision 0013 settled that a migration must not import application code to regenerate prose, and unlike 0013 this is not an exact string transformation — the new text needs the counts. Existing cards are rebuilt by the next profile run, which `refresh_cards` already does from the platform database alone |
| B-093 | 2026-08-18 | B-060 reproduction | **When a question has two defensible sources, the platform retrieves both and the answer names neither.** In every reproduction run `context_selected` lists both candidate fact tables; the statement uses one; `critic_verdict` is `pass`, `answer_composed` records `limitations: 0`, and the prose states the result with no hedge. **What is needed to say a choice was made is already in the run's own state**, which is what makes this cheap and its absence hard to defend. B-060 puts it exactly: the trace shows *what* was chosen and never that a choice existed. Wanted, in the smallest honest form: when context selected more than one candidate fact table and the executed statement used a strict subset, the answer says which source it came from and that another was available — a sentence, not a block. Deliberately weaker than asking the user to choose, which is a product decision the V1 plan does not carry, and weaker than a critic block, since a false block is this component's characteristic failure. **B-092** supplies the evidence; **B-060** is the observation. | P11 | **P1** | done (#78) — an answer now names the source it read and the comparable ones it did not, when the question matched more than one. Both halves come from the run's own record: `candidate_sources` is what context offered that had figures to aggregate, and each execution carries the tables the **validator** resolved, so the note compares what was read against what was available rather than against what the model said it read. **States the choice, does not judge it** — the run cannot know the other source would disagree without running it. Silent when one source was offered, when all of them were read, and when nothing was read, which is most runs |
| B-094 | 2026-08-19 | B-088's manual walk | **A retired definition cannot be brought back through the product, and its name stays taken, so re-importing cannot recover it either.** `DELETE` retires and nothing un-retires: `accept` refuses anything that is not `proposed`, `PATCH` refuses anything that is not `active`, and `propose_from_table` skips a name any row already holds — deliberately, so an import cannot silently re-propose what an Admin turned down. Together those three correct rules leave a mis-clicked **Retire** recoverable only in `psql`, which is the exact shape of the hole B-088 was filed for one verb earlier. Found by the owner walking B-088's web half: `waste_cost` went to `retired` and there is no way back on the screen that retired it. **The fixture this must recover is already in place**: on the local stack, `waste_cost` on the F&B demo source is `retired` at **version 5**, with a history reading v2 edited, v3 and v4 updated, v5 retired. The owner left it retired deliberately rather than restoring it in `psql` — *the first un-retire through the product is the proof this works*. Reinstating it should give version 6, `change` reading as its own act rather than as another edit, and the definition back in the list with the filters v4 left it holding. Wanted: a **Reinstate** on a retired definition — `PATCH …/definitions/{id}` accepting `status`, or a sibling route in the shape `accept` and `reject` already use, validated against the catalog exactly as an edit is, Admin-only, audited, and recorded as its own version so the history reads *retired → reinstated* rather than skipping the gap. Also decide whether the retired ones are visible at all: today they vanish from the screen, so an Admin cannot see there is anything to reinstate. | **P11** | **P1** | done (in the PR that closes this) — **raised from P2 by the owner on 2026-08-19**, on the same argument that raised B-088: *“a semantic layer whose definitions are write-once will not survive real use. Retire with no way back is the same sentence one verb later, and worse in one respect: retired definitions vanish from the screen, so nobody can see there is anything to bring back.”* Hit on the owner's first real use of the feature, as B-088 was. Shipped as `POST …/definitions/{id}/reinstate` — a POST for the reason accept and reject are, since it is a decision rather than a field — validated before it takes effect, Admin-only by the role matrix, audited, and recorded as `reinstated` in the history rather than as another edit (revision **0023**). `GET …/definitions?status=retired` and an **Out of force** card close the discoverability half. The second dead end is closed in the same act: a retired definition cannot be edited, so `reinstate` accepts optional filters and a definition whose catalog has moved on is repaired as it comes back |
| B-095 | 2026-08-19 | WP11.1 manual walk | **A run whose every query *failed* says nothing about it, and the answer describes the failure as an empty result.** Seen live: both executions ended `status = error` with `gaierror: getaddrinfo failed` — the platform never reached the database — and the answer read *“I couldn't show total net sales by month because **no data was returned** from the queries. The evidence does not establish why.”* with **`limitations: []`**. A reader is told their data is empty when in fact nothing was asked of it, which is the opposite of the distinction B-087 was built to draw and the same family as B-060: the run knew, and the answer did not say. **The gap is one predicate.** `composer.limitations_for` notes thin evidence only for executions that *succeeded* and returned nothing — `reference.ok and row_count == 0` — so a failed execution matches no rule and produces no note; the same `reference.ok` filter guards the method note and B-093's source line, correctly in both. Wanted: a limitation when any execution failed, naming how many and quoting the connector's own sanitized message, which already names what failed and never an address or a credential. Worth a second look at whether such a run should be `answered` at all — a refusal grounded in nothing is not a refusal grounded in the data. **The trigger here was a developer-environment quirk** (`scripts/agent_smoke.py` run on the host against a source registered with a compose hostname, so `seed-fnb-pg` does not resolve), but the defect it exposed is not: any connector failure in production reads the same way to the person asking. | P11 | **P1** | done (in the PR that closes this) — the fix is two halves and the second one was not in this entry. **The limitation**: `error` on `ExecutionRef` carries the connector's sanitized message, set **only** where rewriting could not have helped, and `limitations_for` notes how many of the run's queries failed and quotes it. Keyed on that rather than on `ok` deliberately — the loop records a *policy* refusal as a failed execution too, and the next planner routinely corrects it, so a note on `ok` alone would caveat a self-correction on a large share of healthy runs. **The ending**: the run is now **refused, not answered** (**D-038**, the owner's decision of 2026-08-20). Tracing it found the sharper half — the loop had *already written* the true sentence, quoting the connector, and the runner threw it away: the no-compose test asks whether anything **ran**, and a failed execution is still an execution, so the run fell through to `_compose` and a model with no evidence described the absence as a finding. Refusing is also a model call cheaper. One more thing surfaced: the existing test of this path asserted `answered` and never `status`, so it had been passing while the run ended **`failed`** — with a real model it does not fail, it confabulates, which is the live report. `method_note` fixed in the same pass, where *“Answered without running a query”* described two failures as a choice |
| B-096 | 2026-08-19 | WP11.1 manual walk | **The same claim was recorded twice, because the guard against it compared characters.** The Phase 7 rule is *one claim once*; `_write_ending` enforced it with `final.answer.strip() not in {existing statements}`, which holds only for **identical** text — so the composer doing its job, rephrasing a finding into an answer, defeated it. Seen by the owner on a live run: one query, and an answer card carrying two `high confidence` badges and two *“show the query behind this”* controls, over *“Monthly order counts are available for all 18 months…”* and *“Orders by month: February 2025: 3,624; …”* — both citing the same execution. **The fix keys on the evidence rather than the words**: two claims resting on exactly the same set of executions are one claim, whatever phrasing they use. That is the rule `mark_cited` already followed *one line below*, in as many words — *“matched by shared execution, not by text: the composer rephrases, and a match on wording would lose the link exactly when the answer was written well”* — so the codebase had learned this and one line had not. Equality of the citation set rather than overlap, deliberately: an answer that synthesises two findings has a union nobody else cites and is a new claim worth recording. Fixed in the WP11.1 PR, with a test that fails against the text-only guard. | P11 | **P1** | done (WP11.1) |
| B-097 | 2026-08-19 | WP11.1 walk, owner | **The prose enumerated all 18 months directly above a chart of the same 18 points** — one claim rendered twice, which is **B-096's defect from a different direction**: that one was redundancy between two findings, this is redundancy between the prose and the picture. Owner's rule: *when a chart is drawn, the prose should give the shape and let the chart carry the detail.* **Assessed as a composer-prompt change, not a structural one**, and keyed on **result size** rather than on whether a chart was drawn. The reasoning matters more than the verdict. Keying it on the chart is what looks obvious and is the trap: the model writes the prose and asks for the chart in the *same* `finalize` call, so at that moment nobody knows whether the chart will be drawn — `charts.decide` may refuse it for a category with thousands of values, a measure holding words, or a connector that never answered. A model told *“you are drawing a chart, so do not list the numbers”* would then produce a shapeless summary beside a refusal sentence, and the reader would have neither: a promise the platform might not keep, which is the B-087 failure again. Size is known at composing time and is independent of the outcome — the observation the model is given already reads *“18 rows over columns order_month, order_count”* — so a rule like *enumerate a handful, describe the shape and the notable points beyond that* is safe whatever happens to the chart. It also fixes the case that has nothing to do with charts: an answer listing eighteen rows in prose was a poor answer before charts existed, because the detail was already one click away in the evidence panel (B-034). **What would make this structural instead**: wanting the prose to *refer* to the picture (*“as the chart shows, the dip is in February”*) requires knowing the chart landed before the prose is written, which means deciding the chart before the composing call rather than in it — a real change to where the request rides, and one worth its own decision. | P11 | P2 | open |
| B-098 | 2026-08-19 | WP11.1 walk, owner | **A chart's axes are labelled with raw column names** — `order_month`, `order_count` — so the picture is captioned in the database's vocabulary rather than the reader's. Small, and it fits WP11.2's polish pass. The spec is assembled server-side from a closed vocabulary (`agent/charts.py`), so the title for each axis is one field to add to the encoding, not a client-side transformation. Two ways to get the words, and they are not exclusive: the catalog already holds a column's description where the customer wrote one, and a plain de-snake-casing (`order_month` → *Order month*) covers the rest. Worth doing at the same time as the chart's own title, which is currently absent — the model may supply one through `ChartAsk` but nothing asks it to. | P11 | P3 | done (WP11.2a) — `charts.axis_title` de-snake-cases the column into the encoding's `title`, so `order_month` is captioned *Order month*. **Deliberately a de-snake-casing and not a dictionary**: anything cleverer — expanding `qty`, deciding `dt` means date — would be the platform inventing meaning it does not have, which is the family of defect this codebase keeps filing. A name that already reads as a caption (`Total Revenue`, anything containing a space) is returned untouched, and a short list of abbreviations (`id`, `usd`, `vat`…) is not lowercased into nonsense. The catalog's own column description, which the entry also suggested, is **not** used: it is prose written for another purpose and is frequently a paragraph, which is not an axis label. The chart's own missing title stays open — the model may supply one through `ChartAsk` and nothing asks it to, which belongs with B-097's composer-prompt work |
| B-099 | 2026-08-19 | WP11.1 walk, owner | **Whether the web app adopts Tailwind + shadcn/ui is an open decision, deliberately not taken inside WP11.1.** The owner raised it while looking at the answer card and settled the immediate question — *polish within the current system* — while keeping the larger one open: *“Tailwind + shadcn stays a separate scoped decision. Don't start it inside this WP.”* Today every screen is CSS Modules over design tokens (`--fg`, `--surface`, `--space-*`), and adopting shadcn means Tailwind and Radix alongside that until each of data sources, catalog, definitions, members, documents and conversation is migrated — a WP of its own, with a period of two styling systems in one tree. **Charts are explicitly out of scope for it**: the shadcn sample is Recharts, which builds the picture client-side from data, and D-037 records why the server-built validated spec stays. Wants scoping before it is judged: what it buys (a component vocabulary, animation and empty/loading states the app writes by hand today), what it costs (bundle, a second system, a migration nobody can half-finish), and whether it lands before V1 or after. | P12 | P3 | open |
| B-100 | 2026-08-20 | B-095, while fixing `method_note` | **The answer's *method* line is computed on every run and read by nothing.** Architecture 4.2 makes an answer four things — the words, the evidence, the **method**, the limitations — and `composer.py`'s own docstring says why the third one is there: *“one line, so ‘how did you get this’ has an answer that is not the SQL”*. `assemble` duly builds it (`method_note`: how many queries, over how many steps, against which tables) and sets it on `ComposedAnswer.method` — and `runner._write_ending` then takes `composed.limitations` and `composed.text` and drops the rest. There is no column for it on `agent_runs`, no field on the run view, and nothing in the web app that renders one; a grep for consumers finds the assignment and nothing else. So the one part of the answer aimed at a reader who will not open the SQL is the part that never reaches them. **Found by grep while fixing B-095**, which corrected the sentence `method_note` produces for a run whose queries all failed — a correct sentence nobody can currently see, which is how the gap came to light. Small and self-contained: a column, a field on the view, and a line in the answer card near the evidence controls. Worth taking with **B-098** in WP11.2's polish pass, since both are about the card saying in the reader's words what it currently says in the database's or not at all. Worth confirming with the owner that the card should carry it at all before building it — the alternative reading is that `supported_by` plus B-034's evidence panel already answer *“how did you get this”* and the line is one the design outgrew, in which case the honest fix is to **delete** `method_note` rather than surface it. Either way the present state — computed, tested, discarded — is the one thing it should not stay. | P11 | P2 | done (WP11.2a) — **surfaced, not deleted** (owner, 2026-08-20): *“architecture 4.2 makes method one of an answer's four parts, and building it on every run while reading it nowhere is the same shape as the definition that reached the critic and never the model.”* `agent_runs.method` (revision **0025**), written by `record_answer` from what `assemble` already built, on the run view, on the API, and rendered in the answer card above the limitations and styled quieter — *“how did you get this”* is not a doubt about the answer, and a method line that read like a caveat would make every answer look qualified. **Stored rather than recomputed on read**, for the reason `_grounding` gives about definitions: what a reader is owed is what *this* run did, not what today's code would say about it. NULL for runs answered before the column existed — inventing a sentence for them would be the platform asserting something it cannot know |
| B-101 | 2026-08-20 | WP11.2a, owner's scoping call | **The app has never had a small-screen pass, and the catalog and members screens are still the sketch WP11.2 meant to round out.** Three `@media` rules exist in the whole web app — two in `globals.css`, one in `ui/page.module.css` — so every screen is a desktop layout that a phone renders by luck. The tables are the sharpest case: members and catalog both put actions in a final column, and a table wide enough to need horizontal scrolling puts the only control that matters off the edge of the screen. The conversation screen is the one that most needs to work on a phone and the one with the most to reflow — an answer card carrying prose, a chart, a method line, limitations, findings and a collapsed trace. **Split out of WP11.2a and deliberately kept out of the gate PR** (owner, 2026-08-20), on the same reasoning that dropped **B-020**: *“the gate PR already carries a new CI job, a scripted provider in the shipped image, and the sign-off walk — a layout pass touching every screen reviews badly next to that, and it's the one item where ‘done’ is my judgement in a browser rather than a test.”* That last clause is the load-bearing one and worth keeping: a responsive pass has almost no assertable surface, so it cannot ride along on a green build the way the rest of 11.2a did. It wants its own PR, walked at 375px on real screens, with the **catalog and members rounding** folded in since both are the same act of looking at a screen and finishing it. **The Phase 11 gate does not cover small screens**, and STATUS says so, so no sign-off can be read as having covered this. | P12 | P2 | open |
| B-102 | 2026-08-20 | WP11.2b, walking the README from a clean stack | **`make db.setup` — step 2 of the documented quickstart — crashed from a clean state, and had done for some time.** `db_setup.sh` needed three values and got them with `set -a; . ./.env; set +a`. A shell assignment removes quotes, so `LLM_ROLE_MAP={"compose":"small"}` entered the environment as `{compose:small}`; environment beats dotenv in pydantic's settings order, so the mangled value beat the correct one still sitting in the file, and `alembic upgrade head` died with *`SettingsError: error parsing value for field "llm_role_map" from source "EnvSettingsSource"`*. `.env.example` ships **three** JSON-valued keys uncommented — `LLM_ROLE_MAP`, `LLM_MODELS`, `LLM_PRICES` — so `make env` produced a `.env` that broke the next documented command. **The reason it stayed hidden is the part worth keeping**, and it is not the quoting bug. There were *two routes to the same outcome*: `make migrate` runs alembic directly, reads `.env` through pydantic's own dotenv parser, and is unaffected — and it is what everybody actually ran, because it is what you reach for mid-development. The documented path had a working shortcut beside it, so the broken one was never taken and never reported. That is a general hazard rather than a one-off: **a second route around a broken first route hides the breakage for exactly as long as the people who know the shortcut are the only ones walking it** — and the people the documented path exists for are, by definition, not those people. Worth a habit as much as a fix: **the quickstart is only tested by walking it from nothing**, which is how this surfaced and is now a standing condition on WP11.2b. Fixed in that PR by reading the three values the script uses rather than exporting the whole file, so no future JSON-valued key can break a migration merely by being present. **Still open**: nothing walks the documented path automatically, so the next divergence between what the README says and what works will hide the same way. A CI job that runs the quickstart's commands against a clean stack is the obvious candidate and is not in WP11.2b. **Walking the path is the only thing that has ever caught this class.** The evidence is now three-deep and consistent: this entry's crash, the four further defects the *second* walk found — three of them in prose written minutes earlier by someone who had just read the code — and **B-103**, where the gate's own sentence answers correctly and draws no chart. Not one was found by a test, a review or a re-reading, because every one of them lives in the gap between what the code can do and what the documented path actually does. A green build says nothing about that gap, and neither does an author checking their own steps: writing a step and walking it are different acts, and only the second one fails. | P12 | P2 | open (the quoting fix shipped in WP11.2b; the gap this entry is really about — nothing walks the documented path — is what stays open) |
| B-103 | 2026-08-20 | WP11.2b, the second clean-stack walk | **The gate's own question draws no chart on a fresh stack.** Asked *“show me the revenue trend by month”* against a freshly seeded pizza demo with a real model, the run answered correctly — eighteen months of revenue, peak and decline named — and the chart slot read *“No chart was drawn: 'revenue' does not hold numbers, so there is nothing to measure up the vertical axis.”* The **Phase 11 gate criterion is literally “show me the revenue trend by month → chart renders”**, so this is the gate failing on the gate's sentence, found by walking it rather than by any test. **Hypothesis confirmed 2026-08-20, and the mechanism is one line wider than expected.** The chart tool does not read the live frame: it reads the **stored artifact**, and `dal/artifacts.py::_encodable` keeps `str \| int \| float \| bool` and calls `str()` on everything else — so `Decimal("119558.51")` is stored as `"119558.51"`, comes back from `json.loads` a string, and `charts._is_number`, which is `isinstance(value, int \| float)`, says no. `_kind` falls through to `nominal` and `decide` refuses. An integer `count(*)` survives because `int` passes `_encodable` untouched. **The asymmetry is the tell**: `_is_temporal` *does* accept a stringified value — `isinstance(value, str) and _ISO_DATE.match(value)` — so the same author thought about dates surviving JSON as text and did not think about numbers. That would mean charts have only ever worked for the `count(*)` shape WP11.1 demoed, and would fail for every currency question, which is most of what this product is for. **Three converters share this seam and all three make the same choice**: `artifacts._encodable` for the stored blob the chart reads, `audit_hook._plain` for `result_artifacts.sample_rows` the evidence panel reads, and `tools/sql.py::_plain` for the rows the composing model is shown. Only the chart *computes* on them, so only the chart is broken today — the evidence panel renders `"119558.51"` identically to `119558.51`, and the model reads either as text — but the next consumer that does arithmetic inherits the same trap from whichever copy it happens to read. Worth care in the fix: coercing strings to numbers inside `charts.decide` would be the wrong layer and would silently draw a chart of something that is not a number; the honest options are for the DAL to preserve numeric types through the frame, or for the chart to key on the **column's catalog type** rather than on the rendered values. Blocks the WP11.2b gate as written. | P11 | **P1** | done (WP11.2b) — **fixed at the seam, and the criterion met live**: `show me the revenue trend by month`, clean stack, real model, `monthly_revenue` a *number* and a line chart drawn. A `Decimal` cannot cross JSON as a number — the stdlib will not emit one unquoted and `float` rounds money — so the value still travels as a string and `artifacts.encode` records **`column_types`** beside it, written where a Decimal is still a Decimal; the chart rebuilds from that declaration. It does **not** accept numeric-looking strings, and a test holds that line with an account number, because a wrong chart is worse than an absent one. Artifacts stored before the column existed carry no types, so those decline with `types_not_recorded` — blaming the platform rather than the customer's data — guarded so an old result that really holds words still gets `y_not_numeric`. All three converters now share one public `encodable`. **The fix moved the defect one seam later before it was done**: rebuilding Decimals put them into the spec's inline values, which are stored as JSONB, and the run died on `Object of type Decimal is not JSON serializable` — the round-trip tests written *for* the original stop at `decide` and never store what it returns. `_plottable` converts to float only where the spec is built, so the artifact keeps every digit for anything that computes and only the picture is approximate, as it always was: Vega renders in a browser whose only number type is a double |
| B-104 | 2026-08-20 | WP11.2b, writing the compose smoke | **A signed-in person is shown a sign-in form on every full page load.** `SessionProvider` starts with `who = null` and restores a dev session in an effect — read the remembered subject, mint a token, set `who` — so between the first paint and that round trip there is no way to tell *not signed in* from *not known yet*, and all nine screens gate on `session.who ? <Screen/> : <SignIn/>`. The server renders the same thing, because `who` is null there too. **This is the defect WP11.2a already fixed once**, for the conversations list that started at `[]` and told every visitor *“Nothing yet”* while its fetch was in flight — an empty state standing in for a loading one. This is the sharper version: it does not say *you have nothing*, it says *you are nobody*, and it offers a form that works if you use it, so the product looks broken and then heals. In a production bundle against a warm API it is a flash; against a cold or distant API it is a screen somebody will act on. **Found by the compose smoke, which could not get past it.** Jumping between screens with `page.goto` — a full load each time — the walk arrived at a conversations page rendering the sign-in card and waited thirty seconds for a heading that was never coming. The hermetic suite has never seen it: it signs in by writing `localStorage` and reloading once, and it serves a **built** page, so hydration is immediate. The smoke now navigates by clicking, which is both more faithful to how the product is used and steadier, and its one deliberate full reload carries a long timeout with this id beside it. **Wanted**: a third state on the session — `restoring`, true until the first mint settles — and one small gate component the nine screens share, so *“we do not know yet”* renders as nothing or as a quiet line rather than as a denial. Deliberately **not** fixed in WP11.2b, on the reasoning that moved B-101 and dropped B-020: the gate PR already carries a new CI job, a stub provider in the shipped image and the sign-off walk. Deliberately **not** to be fixed by seeding `who` from the remembered subject before the token exists — that would let screens fire authenticated calls with no token and turn one wrong render into a page of 401s. | P12 | P2 | open |
| B-105 | 2026-08-20 | WP11.2b, the owner's gate walk | **A monthly aggregate was drawn on a continuous day axis, so the chart was wrong rather than plain.** Four monthly bars — April to July 2026 — landed as four instants on a four-month temporal domain: Vega ticked it by week (`Apr 05`, `Apr 12`, `Apr 19`), the bars were drawn at their default width, and the space between them read as zero revenue. The owner's words: *“this is the Q1/Q2 rule with real dates — wrong is worse than absent, and `charts.decide` refuses the one and drew the other.”* Confirmed from the stored spec: `mark: bar`, `x: {"type": "temporal", "field": "month"}`, four values at `2026-04-01 … 2026-07-01`, **no `timeUnit`**. **The line chart shipped with the identical encoding** and only looked correct, because a line implies no width and so claims nothing about the space between two points — the same defect, one visible and one not. Fixed in WP11.2b by reading the grain **off the values**: the coarsest unit every present value sits exactly on becomes the encoding's `timeUnit` (`year`, `yearmonth`, `yearmonthdate`), for every mark. Read off the values and never off the column's name, which is the line this module holds everywhere — `_kind` judges `order_date` by what is in it, `axis_title` de-snake-cases rather than translating, `_is_number` will not parse a numeric-looking string. Conservative in one direction only: daily data that happens to hold nothing but firsts-of-month is banded by month, which places every point correctly and merely draws it coarser than it had to be; nothing can move a point to a time it is not. **Where no grain exists — dates carrying a time — a `bar` gets an `ordinal` axis instead**, which is not a compromise but what a bar chart's axis is: one band per thing compared. A `line` over the same values stays continuous, because spacing a working day's readings evenly would misstate when they happened. Seven tests, one of them the owner's frame verbatim. | P11 | **P1** | done (WP11.2b) |
| B-106 | 2026-08-20 | WP11.2b, the owner's gate walk | **Every answer but the newest loses its chart, its method line, its limitations, its findings, its evidence controls and its trace.** The owner saw the chart vanish on sending a second message and read it as the Phase 7 suppression rule outliving its reason — which is true of the card's *sentence* and understates the rest. `replied` only suppresses the duplicated answer sentence inside the card. The disappearance is one layer up: `ConversationThread` holds a single `run` in state and renders a single `<AnswerCard>` for it, so a previous run's card is not suppressed, it is **never rendered**. A reload restores only `conversation.last_run_id`. Everything else is durable rows that no longer have a route from this screen — including the trace, which is the one thing the product exists to be able to show. **The owner's question was whether the card stays or the chart moves into the bubble; the answer is the card stays, and it becomes the assistant turn.** Moving the chart fixes a quarter of the problem — architecture 4.2 makes an answer four things, and three of them still vanish — and it does not even save the work, because the thread's messages carry `content` and `run_id` and nothing else, so rendering a chart in a bubble means fetching the run anyway. It would also put the chart *outside* the card, which is what **B-048** refused: a chart beside the answer is a picture next to some prose. The sharper form is that the bubble and the card are two renderings of one thing, which is the only reason `replied` exists and was the root of the Phase 7 gate defect — so the thread should be a user bubble followed by the run's card, and `replied` should stop existing rather than be re-tuned. **Wants its own work package**, on the reasoning that moved B-101 and dropped B-020 (owner, 2026-08-20): a list endpoint so a twenty-turn thread is not twenty fetches (`GET …/conversations/{id}/runs`), the thread restructure, and lazy chart rendering so twenty Vega instances do not mount at once. Worth checking against the WP7.3b browser suite as it lands: its Phase 7 property — *after a single question, with no further interaction, the answer text is on the screen* — currently asserts against the bubble, and under this change the card can render the answer from the run before the message arrives, which is a better guarantee than the one being replaced. | **P11** | **P1** | done (WP11.2b) — **raised to P11 and made a gate blocker by the owner on 2026-08-20**, on the demo: *“a chart that disappears on the second question fails in front of an audience, and ‘chart renders’ is not met by one that survives only until the next message.”* Built as the answer above: `GET …/conversations/{id}/runs` returns every run in the thread oldest first, the screen holds them by id, and each assistant message renders as its run's card — words, chart, method, limitations, findings, evidence and trace — so nothing is lost when the next question is asked. **`replied` is gone rather than re-tuned**: it existed only because the bubble and the card were two renderings of one answer, and with the card as the turn there is one. The plain bubble remains as the fallback for a run that could not be fetched — the words are what the reader came for, and losing them because a second request failed would be a worse trade than losing the picture. **One request for the thread**, not one per assistant message: the cost of opening a conversation must not grow with how much somebody has used it. Both routes build their payload from one `_run_out`, with a test on the field set, because a field added to one and not the other would make an answer look different depending on which request fetched it — and the thread is exactly where two answers are compared side by side. The new route is in the cross-tenant isolation list, not beside it: a thread's runs are the answers in it. Three of the five new web tests fail against the old screen; the smoke asks a second question and asserts **two** evidence controls and **two** traces, keyed on the count because presence passed against the defect — the newest answer always had its card |
| B-107 | 2026-08-20 | WP11.2b, the owner's gate walk | **Two findings, one query, and B-096's rule was only ever applied to one of the two places that record a finding.** The owner expected one and got two, both citing the same single execution: *“Monthly revenue for the last four completed calendar months was $135,950.59 in April 2026, …”* and *“Revenue peaked in May 2026 and then declined in both June and July.”* **B-096's guard worked.** It lives in `runner._write_ending`, it keys on the citation set, and the composed answer for that run — a third wording, *“Revenue was $135,950.59 in April 2026, rose to $145,341.12 in May…”* — was correctly **not** added, because its set matched. Both findings came from the **loop**, and `state.add_finding` still dedupes on characters: `existing.statement.strip() == finding.statement.strip()`. That is the guard B-096 was filed against, one layer up, in the copy nobody changed. With the bubble's rewording the reader is shown one query's result three times. **Not simply B-096's rule copied across, and that is the open question.** Those two findings are arguably two claims — the numbers, and the trend — where the composer's case was one claim restated. Applying set-equality in `state.add_finding` would drop the second, and first-wins keeps the enumeration and discards the interpretation, which may be the wrong one to keep. Options worth weighing before building: keep both but have the card render them as one claim with its supporting detail; keep set-equality and choose deliberately which survives; or leave the loop alone and treat the redundancy as **B-097**'s composer-prompt problem, since *“enumerate a handful, describe the shape”* would have stopped the first finding being written at all. **The generalisation is the durable part**: a rule fixed at one of its call sites is fixed nowhere in particular, and B-096's entry already said the codebase had learned this and one line had not — it had two. | **P11** | P2 | done (WP11.2b) — the owner's call of 2026-08-20: *“copy the rule across, keying on the citation set as B-096 does — that was the Phase 7 rule and it did not have an exception for which layer wrote it”*, with the condition that if keying on citations alone would drop the better sentence, they be told what it keeps rather than shipped a rule that drops it. **It would have.** The two sentences arrived from **one** reflection — the event sequence is `finding_added → finding_added → reflection` — in the order enumeration-then-shape, so first-wins keeps the numbers and discards the trend, which is the worse of the two by **B-097**'s own rule that a chart should carry the detail while the prose gives the shape. Last-wins would be right here and wrong the moment a model emitted them the other way round. So the guard detects rather than chooses: `merge_by_evidence` **joins** findings from one reflection that rest on the same executions into one claim, with one citation and the weakest of their confidences, and nothing a model wrote is thrown away. Merged **before anything is persisted**, because a finding row and its `finding_added` event are written together and a later merge would have to rewrite a row whose event already said something else. `state.add_finding` gains the citation-set rejection for the other half — a *later* reflection restating on evidence the run has already concluded from, where merging is not available and the earlier finding stands; that iteration counts as barren, which is the honest reading since nothing new was asked of the data. **Only for findings that cite something**: an uncited finding shares the empty set with every other one, and keying on evidence there would collapse every unsupported sentence in a run into the first. Equality of the set and never overlap, so a synthesis over more evidence stays a new claim |
| B-108 | 2026-08-20 | WP11.2b, fixing web-e2e's first red run | **The local stack may not start for a Linux developer whose uid is not 1001, and nobody here can find out.** `ops/.secrets` and `ops/artifacts` are gitignored, so a fresh clone has neither, and Docker creates a missing bind-mount source **as root**. The api image runs as `app`, uid 1001. On a host where those directories do not already exist and the developer's uid differs, registering a data source dies with `PermissionError: '/app/ops/.secrets/secrets.json.N.tmp'` and every successful query dies writing its artifact — the second of which the compose file already documents as having happened once. **Found in CI, not here.** `web-e2e`'s first real run hit exactly this; the smoke stack now uses named volumes and the api image creates both directories before its `chown`, so a *named volume* inherits `app:app`. **That fix does nothing for a bind mount**, which never inherits image ownership — so the local stack still has the hole, and the only reason it has never been seen is that this project has been developed on Windows, where Docker Desktop's bind mounts ignore ownership entirely, and on a machine where both directories have existed for weeks. **Two conditions, each of which hides it**, which is why it needs somebody on Linux rather than more thought here. Candidate fixes, none verified: `make up` creating both directories (helps only where the uids happen to match — a GitHub runner is 1001, a typical Linux desktop user is 1000); a `user:` on the api service taken from the host; or the same named volumes the smoke uses, which would cost `make down` its promise that registered data sources survive. **The same family as B-102**: a precondition every existing machine already satisfies, so the path where it is missing is never walked. | P12 | P2 | open |
| B-109 | 2026-08-21 | Owner's walk of the Phase 11 gate | **Every chart is one flat colour, and the model apologised to the customer for it in the answer text.** Seen live: *“the chart format does not provide a separate color setting for each month”* — prose a customer reads as the product being broken, and which is really the model explaining our whitelist. **The colour channel is already built, already tested, and unreachable from the only live path.** `charts._spec` assembles a `color` encoding, `ChartRequest` carries `series`, `CreateChartIn` accepts it and `test_charts.py` asserts its axis title — while **`ChartAsk`**, the schema the model actually fills (**D-037**: the request rides on `FinalizeIn`, once per run), offers `of, mark, x, y` and nothing else, and `runner._chart_for` calls the tool with exactly those four. So the model read four fields, correctly inferred it could not colour anything, and said so where the data belongs. The flat purple is `chartTheme`'s `bar: { fill: primary }` — `--primary`, one token, every mark; the product has no categorical palette at all. **Scoped with the owner on 2026-08-21, three answers settled, one work package, no migration.** **(1) `series` on `ChartAsk`**, threaded through `_chart_for`, which makes the built channel reachable. It takes a *column name* checked against `frame.columns`, exactly like `x` and `y`, so it costs the security property nothing: the property is not *few channels*, it is *every value is a closed vocabulary or a validated column name*. Cost is roughly 40 tokens per run on the finalize call, against D-037's measured 339 for the whole closed schema. **(2) A separate categorical ramp in `globals.css`**, wired into `chartTheme` as `range.category` — **not** the five pastel accents, which stay semantic. The owner's reasoning: *“a hue that means two things means neither, and a rose bar reading as an error is worse than no colour.”* design.md's own rule is that a colour in a component file is a bug; a palette written into a spec is the same bug one layer along. **(3) Per-category colour is refused outright**, with a sentence naming why — colouring eighteen months in eighteen hues encodes nothing the x-axis does not already say, and design.md rule 2 settles it: *“a hue is a claim that this thing has a state or a category. Never colour for interest.”* A caveat beside a rainbow is a caveat nobody reads. **And the refusal must be said in the chart's own slot**: when `decide` overrides — series capped at `MAX_CATEGORIES`, or series equal to `x` — a chart that silently comes back mono is **B-060** in a picture. **The model stops narrating the chart at all.** The apology is structurally misplaced, and D-037 already says why: prose and chart request are written in the *same* `finalize` call, so at that moment nobody knows whether the chart will be drawn — `_chart_for` runs afterwards. The lever is `FinalizeIn.answer`'s field description, which today says nothing about it, plus a test that a refused chart leaves no apology in the prose. Deliberately **not** a string check on the answer: that is the wrong layer, on the same argument that stops `charts.decide` coercing numeric-looking strings. **Out of scope and named**: faceting (`row`/`column`) — no security cost, since those are column names too, but eighteen months facetted is eighteen tiny charts, which is **B-105**'s failure of being wrong rather than plain, and it needs `MAX_POINTS` to mean points-per-facet. Its own decision, later. And anything expression-bearing — Vega-Lite `transform`/`calculate` is a browser-side expression language, model-authored titles and tooltips are free text. **That is where the closed vocabulary earns its keep, and none of it is needed here**: column names and closed vocabularies are cheap, expressions and free strings are not. **The class, which is the fourth occurrence and the reason this entry is long.** *Something is built, is covered by tests, and no live path reaches it.* **B-083** — a definition reached the critic and never the model, and `Definition.render()`, written for exactly that, was called by nothing. **B-085** — an imported definition answers only to its key and its label, and not one real question is phrased in those words, so eighteen imports bound nothing. **B-100** — the method line computed on every run and read by nothing. **B-109** — a colour channel the model has no field to ask for. Every one was found by **reading or walking**, not one by CI, and the reason is worth stating: **line coverage cannot see this class by construction**, because a unit test is exactly the thing that makes unreachable code look exercised. `_spec`'s colour branch is covered today. Whether anything could catch it is assessed separately and deliberately not built on a guess — the four are not one shape (a dead function, a field assigned and never read, a path reachable in code and not in language, and a schema pair that diverged), and only some of those are mechanically visible. | P12 | **P1** | **open — three parts of four built and verified live (2026-08-21); the defect as reported is still happening.** `series` on `ChartAsk`, threaded through `_chart_for`, so the channel that was already built is reachable; a **fixed-order eight-hue categorical ramp** in `globals.css` and `docs/design.md`, separate from the semantic pastels, **validated as a set** in both modes against the card surface for lightness band, chroma floor, colour-vision separation on adjacent pairs and normal-vision separation — slot 1 stays in the primary's family, and light slots 3–5 fall under 3:1 on white, which the legend and the always-available result table relieve; `MAX_SERIES = 8`, the palette's own size, because a ninth series would repeat a hue and two series wearing one colour is a chart that lies; **`colour_would_repeat_an_axis`**, which refuses colouring by a column already on an axis and *says so in the chart's own slot* rather than dropping the channel silently, since a mono chart with nothing said is B-060 in a picture; and `FinalizeIn.answer`'s description now tells the model to write about the data and never about the chart. The test that carries it is **end to end, not on `decide`**: a unit test hands `decide` its request directly, which is the one thing the product could not do, so the run asserts the refusal — a code only reachable if the series survived `ChartAsk` → `_chart_for` → tool → `decide` → the stored run. **Verified live on 2026-08-21**, real model, owner's stack: *“show me monthly revenue split by channel as a bar chart”* stored `"color": {"type": "nominal", "field": "channel", "title": "Channel"}` beside B-105's `timeUnit`, and the served bundle carries the `chart-cat` tokens. **What is NOT done, and why this entry stays open: the model still narrates the chart in the answer**, which is the defect as reported. The same live run opened with *“A monthly revenue bar chart split by channel can be drawn from the 54-row result…”*. **One candidate is dead, measured rather than assumed**: the field description *does* reach the model — `FinalizeIn.model_json_schema()` carries *“never about the chart”*, `OpenAIProvider.supports_response_schema` is true, and `openai.py` sends that schema verbatim as `text.format.json_schema.schema`. The instruction is in the request and the model narrated anyway, so the lever is misplaced rather than missing: a field description describes a field's *content*, while the composing prompt is what the model is answering, and that prompt says nothing about charts. Next lever is the prompt, tried once. **Two failed levers would mean the prose is driven by something not yet found, which is worth knowing rather than papering over** (owner, 2026-08-21). **And a method note, which belongs beside B-110's coverage point.** Diagnosing that run produced a tidy explanation — the composer's 4,000-character preview truncated a 54-row result, so the model described the shape because it could not see the numbers — supported by the model's own words. It was wrong. It was then *measured* by rebuilding `RunSqlOut` with all 54 rows, which said 3,771 characters and no truncation, and that was wrong too: the real path sends `PREVIEW_ROWS = 20`, so the model saw 20 rows at 1,527 characters and was told `truncated: true`. **A measurement built on a reconstruction is a second guess wearing a number**, and it is more dangerous than the guess because it arrives with a figure attached. It was caught only because `PREVIEW_ROWS` happened to sit in a file being read for another reason. The check that would have caught it deliberately: measure the *stored artefact of the run in question*, never a rebuilt stand-in for it. **P1 was for the apology rather than for the palette**: a model explaining our whitelist inside an answer is a trust defect in a product whose whole claim is that its answers are evidenced, and it reads to a customer as breakage. Can run ahead of **WP12.1**, which is blocked on the USER INPUT batch, and does **not** reopen Phase 11 — that gate is signed and this is new work |
| B-110 | 2026-08-21 | B-109's scoping, and the owner's call on it | **Two guards against the *built, tested, unreachable* class — a schema-correspondence check and a dead-public-function pass.** Filed together because they are one idea at two scales, and filed rather than built at the owner's instruction (2026-08-21): B-109 is what changes something a customer sees. **The point a future session will need, because it is the part nobody rediscovers**: *carefully testing a capability is exactly what hides that nothing reaches it, so **coverage cannot see this class by construction**.* `charts._spec`'s colour branch was covered the whole time it was unreachable; a unit test hands the function its arguments directly, which is the one thing the product could not do. The more thoroughly a capability is tested at its own layer, the more convincingly it looks exercised. No coverage threshold, however high, changes that — which is why neither guard proposed here is a coverage rule. **Guard 1 — schema correspondence, in B-090's declaration idiom.** Where one layer's vocabulary must match another's, assert the field sets correspond and require every divergence to be *named with its reason*, exactly as `check_env.sh` does for the host and the container: *“a declaration rather than a diff — every key that stays on the host is named with its reason.”* `ChartAsk` against `CreateChartIn` would have failed the moment `series` existed on one and not the other, which is **B-109** caught on the day it was introduced rather than on a walk months later. Roughly thirty lines, a `--selftest` like its siblings, and a failure mode that costs a developer one line of justification. **The pairs worth declaring today**: the model's `ChartAsk` against the chart tool's `CreateChartIn`; and — worth a look while writing it — whatever the planner is shown against what the critic is given, which is **B-083**'s shape. **Guard 2 — a dead-public-function pass** over `agent/` and `dal/`, with an allowlist. Near-free, and it catches **B-083**'s other half directly: `Definition.render()` was written for exactly the job it never did, and was called by nothing. **Deliberately not built: a general *produced-but-never-consumed* detector.** That is **B-100**'s shape — a field assigned and never read across a seam — and the owner's reasoning is the reason it stays out: *“the allowlist becomes the artifact.”* Every model field, every attribute, every return value is a candidate; the false-positive rate would be high enough that nobody reads the output, and a guard that costs attention and buys suspicion is worse than none. **And one member of the class is undetectable in principle.** **B-085** — an imported definition answers only to its key and its label, and no real question is phrased in those words. That is a fact about language, not about code; nothing static can see it, and the live walk against the customer's own golden-question table is what found it. **Two of four, honestly**: these guards would have caught B-109 and half of B-083, and nothing here closes B-085 or B-100. The rest of the class lives in the gap between what the code can do and what the product actually does, which is the gap **B-102** names — and walking is still the only thing that has ever crossed it. | P12 | P2 | open — scoped, not built (owner, 2026-08-21) |
| B-111 | 2026-08-21 | B-109's live run | **An answer overstated what it could not see, which misleads a reader about what the answer rests on just as surely as overstating what it could.** Live, real model: the composer was handed **20 of 54 rows** — `PREVIEW_ROWS = 20` — with `truncated: true` beside them, and wrote *“The evidence provided here does not include the actual monthly revenue values, so I cannot state the month-by-month amounts.”* It **had** twenty rows of monthly revenue values, roughly seven of the eighteen months. The substance was true — it could not give a full month-by-month account — and the wording was not: *does not include* where the honest sentence is *includes part of*. **Filed narrow, and the wider version was withdrawn.** This began as a much stronger claim — *the model said its evidence lacked values that were in front of it*, an agent misreporting its own context, which would matter far beyond a caption. **That claim rested on a reconstruction rather than on the real path**: `RunSqlOut` was rebuilt with all 54 rows to measure the preview, which is not what the run sent. With `PREVIEW_ROWS` applied the model had 20 rows at 1,527 characters and an explicit truncation flag, and its answer is a wording defect rather than a misreading. The wider entry is not filed, deliberately, and the reason it is not is recorded here: it would have sent somebody hunting a hallucination that did not happen. **What to check first, and in this order.** (1) Does the composing prompt tell the model what `truncated: true` and a row preview *mean* — that it is seeing part of a result and should say *part* rather than *none*? The flag is in the payload; nothing in the prompt interprets it. (2) Are `row_count` and the preview length legible side by side in what the model is shown, so *20 of 54* is a fact it can state rather than infer? (3) Only then, whether the composer needs a rule for the shape of that sentence — because a prompt rule about phrasing, added before the model can see the numbers it is meant to phrase, would be teaching it to guess more fluently. **Relation to B-051**, which is the rule this is under: *a figure that can only come from part of the data is stated as such or not at all.* This answer did state it — it simply stated it too strongly, in the direction that makes the platform look blinder than it is. | P12 | P2 | done (2026-08-21, with **B-113**) — **the two were one seam and were fixed together**, which is what the check-first list above predicted: item (1) was *does the prompt tell the model what a preview and a truncation flag mean*, and it did not. `PARTIAL_RESULT_RULE` now says it, naming both numbers as the sentence's raw material — `row_count` is what the query returned, the rows given are what fitted — and forbidding the two failures directly: *do not describe a partial result as though it held nothing*, and *do not refuse a question the data answered merely because you were shown part of the answer*. Item (2) needed nothing: `row_count` and the rows were always legible side by side; what was missing was the instruction to read them as a pair. Item (3) — a rule about the *shape* of the sentence — is deliberately still not built, on its own reasoning: a phrasing rule added before the model can see the numbers it is meant to phrase would teach it to guess more fluently. **And B-113's budget removed most of the occasions for the sentence.** The case that produced this entry now arrives whole, so there is nothing partial to describe; the rule governs what is left, which is genuinely large results. Where it fires, the honest form is the one the owner asked for and the model now has the parts to build: *how many rows the query returned, how many it was shown, what those rows do establish, and what a narrower question would settle* — a limit named as ours, with a way past it |
| B-112 | 2026-08-21 | B-109's live run | **The 4,000-character preview cap marks the model's copy and reaches nothing else: no record, no card, no trace, and no flag.** `ToolResult.render()` truncates a rendered result at `MAX_RENDERED_CHARS` and appends `“… truncated”` to the text the model sees — and the module's own comment says why, that a large result should be *“truncated visibly rather than silently”*. Visibly **to the model**. Nothing downstream can know it happened: there is no field on `RunSqlOut`, nothing on the run, nothing on the answer card, nothing in the trace. An answer composed from a result cut at that boundary is an answer from part of the data, and the reader cannot learn it. **Row truncation, by contrast, is announced in all three places and is not what this is about.** `RunSqlOut.truncated` carries `execution.truncated or len(frame.rows) > PREVIEW_ROWS` to the model; the evidence panel renders a `truncated` badge to the reader; and `charts.decide` refuses to draw a truncated frame outright, which is **B-051** already enforced. That machinery is right. The character cap is the one truncation with no flag, and therefore the one nothing else can react to. **Found at the margin rather than by failing.** The run that prompted this rendered at **1,527 characters** against the 4,000 cap, so it did not fire; an earlier and wrong reconstruction of the same result put it at **3,771**, six per cent under. Neither is the point. The point is that a boundary nobody is told about is a boundary nobody can act on: no test asserts what happens at 4,001, and no reader would learn if it did. **Wanted, in the shape the row cap already has**: a flag on the tool result when the render was cut, carried the way `truncated` is — so the composer can be told, the run can record it, the card can say it, and `decide` can treat it the way it treats a truncated frame. Whether the cap should also *drop whole rows* rather than cut mid-JSON is worth deciding at the same time: a result sliced through the middle of a row is one the model has to parse around, and it is the sort of thing that shows up later as a number nobody can trace. | P12 | P2 | open |
| B-113 | 2026-08-21 | B-109's second live run | **The product refused a question its data answers completely, because the composer sees 20 rows of 54.** Live, real model, owner's stack: *“show me monthly revenue split by channel as a bar chart”*. The query ran, returned all 54 rows — 18 months × 3 channels — and the artefact holds every one of them. `PREVIEW_ROWS = 20` is what reached the model, and it declined: *“the available result contains monthly revenue split by channel, but it is truncated: 54 rows were returned and only 20 are shown.”* `answered = false`, and **no chart**, on a question the database had already answered in full. **Why this is P1 when every other refusal in this product is a feature.** WP7.2b's whole argument is that an honest refusal beats a confident guess, and B-095, B-087 and D-038 are all that argument being enforced. This refusal is not that. Nothing about the customer's data was missing; **the platform's own preview budget was**, and the sentence the reader gets is indistinguishable from one about their schema. A reader cannot tell *“your data cannot answer this”* from *“our prompt could not carry it”*, and the second one wearing the first one's clothes corrodes exactly the trust the first one earns. **This is B-060's shape.** The run knows the limit — `row_count` is 54, `truncated` is true, the artefact is complete — and the reader is told a conclusion instead of the choice behind it. B-060 was *the trace shows what was chosen and never that a choice existed*; here the answer shows a refusal and never that the refusal is ours. **The sequence is worth recording, because the order was not incidental.** The chart apology was sitting on top of this. While the model was narrating the whitelist (B-109), its prose was noise and this constraint had no room to speak. The moment the prose was cleaned — the instruction moved into the composing prompt — the model said plainly what it was short of, and what it was short of was rows. **Fixing the cosmetic defect is what let the structural one name itself**, which is an argument for cleaning prose before diagnosing behaviour rather than after. **What `PREVIEW_ROWS` was chosen for, and what it costs.** Its own comment: *“the DAL's own cap is far higher because the artifact keeps the whole result; what reaches a prompt is a sample big enough to reason about and small enough that raw values do not become the context.”* That is architecture 4.4 — summaries flow forward, raw rows do not accumulate — and it is the same budget `MAX_RENDERED_CHARS` guards from the other side. It is not an arbitrary number to raise: a bigger preview is more tokens on **every** run including the ones that need none, it pushes toward the 4,000-character cap (**B-112**, which nothing downstream can see), and more rows in front of a model is more rows enumerated back out in the prose — which is precisely what **B-097** exists to stop. **Options, with what each costs. No recommendation: this is a judgement about what the product is for.** **(a) Raise the constant.** Cheapest change, worst scaling — it pays on every run for the minority that need it, moves the char cap from theoretical to routine, and feeds B-097's enumeration problem. It also only moves the cliff: 54 rows is not a large result, and the next question has 500. **(b) Make it depend on the result's shape.** More rows when they are narrow, fewer when they are wide, budgeted in characters rather than rows — which is what the char cap is really measuring anyway. Fixes the common case with no fixed cost, and the cliff still exists, just further out and now at a boundary that varies by result, which is harder to reason about and to test. **(c) Let the model ask for more.** A tool that fetches the next page of a result it already has. Costs an extra call and an iteration when used, nothing when not, and it is the only option where the *run* decides rather than a constant chosen in advance. It is also the largest change: a new tool, a budget line, and a new way for a loop to spend its iterations — and D-024's lesson is that a loop given a new way to spend will find it. **(d) Aggregate rather than paginate.** The model is shown the whole result *summarised* — per-group totals, ranges, the extremes — and reaches for rows only to quote specifics. Best fit for architecture 4.4's *summaries flow forward*, and the hardest to get right: a summary is an opinion about what mattered, and choosing wrong is a silent choice, which is the family B-060 and B-092 are both in. **(e) Refuse honestly instead.** Leave the budget alone and make the *sentence* say whose limit it is — *“this result has 54 rows and I was shown 20; ask for a narrower range and I can answer it fully.”* Cheapest by far, fixes the trust defect and not the capability, and leaves the product unable to answer a question about eighteen months of data. **Not options**: raising the DAL's row cap (a different budget entirely, and the artefact is already complete), and anything that lets the model see unmasked values (masking happens before this, and is not negotiable). | P12 | **P1** | done (2026-08-21) — **(b) budget by shape + (e) refuse honestly**, the owner's choice: not (a), which moves the cliff rather than removing it; not (c), which hands the loop another way to spend; not (d), because a silent choice is the failure mode fixed everywhere else in this codebase. **The budget is derived, not chosen.** `PREVIEW_CHARS = MAX_RENDERED_CHARS - 100` — the ceiling `ToolResult.render` cuts at, less the frame it wraps the payload in — and `preview_rows` fills it with whole rows, dropping from the end. So a preview the budget governs cannot be cut by `render`, which narrows **B-112**'s exposure without closing it. A derived limit also cannot drift from the thing it was chosen against, and a test asserts that relation rather than the number. **Measured on the payload, after the first attempt measured a proxy for it.** That version counted `json.dumps(row)` compactly while the payload ships `indent=2` — 1,944 characters against 3,566 for the same 54 rows, an under-count of nearly two to one, which would have made the budget not budget. `preview_rows` now takes the renderer as an argument so the thing measured *is* the thing sent, and `_out` builds that payload once so the measurement and the result cannot differ. **One floor, and it is a stated trade rather than an oversight.** `PREVIEW_ROWS_MIN = 3` beats the budget for a result so wide that three rows exceed the ceiling; those are rendered anyway and cut by `render`. A model shown zero rows and a row count cannot tell an empty result from an expensive one, and the second invites it to invent what the result held. That is the single path by which this tool still reaches B-112, and a test asserts it so it stays known. **(e) is `PARTIAL_RESULT_RULE` in the composing prompt**, which names the owner of the limit and forbids both failures seen live: describing a partial result as though it held nothing (**B-111**), and refusing outright a question the data answered in full. **Verified live, real model, on the run that produced this entry.** Same question, same source: before, `answered=false`, no chart, 20 of 54 rows. After, `answered=true`, all 54 rows, a coloured bar chart, and an answer that names per-channel peaks and troughs across the full eighteen months — *“Delivery generated the most revenue in every month, ranging from $53,001.69 in July 2026 to $72,160.82 in May 2026.”* **Not fixed here**: B-112's missing flag, and the wide-result case above which is the one place a cut can still happen unannounced |
| B-114 | 2026-08-22 | Session-end migration audit | **The artifact store can lose a result and nothing anywhere says so.** Counted across the whole table on 2026-08-22: **411 `result_artifacts` rows, 408 files present, 2 files gone, 1 row with a NULL `storage_ref`.** **It is not retention.** Zero rows are past `expires_at` — the earliest expiry in the table is 2026-09-12 — and **B-021** records that nothing reads that column anyway, so there is no sweep that could have taken them. Both missing files belong to executions that **succeeded** (`status=ok`, 3 rows each, 2026-08-13); the query ran, the row was written, and the payload is not on disk. Cause undetermined and stated as such rather than guessed. The NULL-ref row (2026-08-15) is different and is **working as designed**: `StoredArtifactError` is deliberately never raised at the caller — *"losing the copy of an answer must not lose the answer"* — so a failed write degrades to a row with no reference. Correct for the answer, and invisible as an event. **What is not broken, checked before filing:** a citation does *not* open onto nothing. The evidence panel serves `result_artifacts.sample_rows` from the platform database (capped at 50 rows, masked on the way in), not the stored file, so every one of the 411 still shows its rows to a reader. The file is read by exactly two callers — `agent/tools/chart.py` and `knowledge/ingest.py` — and no route serves it. **The actual defect is a conflation.** `chart._frame_for` returns `None` for three unrelated causes: the execution is not part of this run, the reference is empty, and the payload is gone. All three collapse into one `no_result` refusal — *"the rows behind that query are not available to draw from"* — which is honest wording for the reader and is the same sentence whether the model named a query from another run (ordinary, expected) or the store lost a customer's result (an operational fault). Nothing logs it, counts it, or distinguishes it, so **silent data loss is presented as a routine model mistake** and the only reason these three rows were found at all is that somebody counted files against rows by hand. Wanted: separate the causes at the point they are known, emit an audited event when a reference exists and its payload does not, and give the reader a refusal that says the result is missing rather than unavailable. **Take it with WP12.2**, when the Blob backend lands and "the object is not there" becomes a network answer rather than a stat() — the same conflation over a remote store hides a transient outage as well as a lost file. | P12 | P2 | open |
| B-115 | 2026-08-23 | Bringing up a fresh machine, walking the README (B-102's standing condition) | **The only automated way to recreate an organization builds one the product cannot query.** A new machine with an empty platform database has exactly one target that offers to rebuild the lost state — `make evals.setup`, which `make help` describes generically as *"Create the org, register the seed source, build its catalog"*. It registers the pizza database at the address in `.env`, and those values are **host-side**: `SEED_PIZZA_HOST=localhost`, `SEED_PIZZA_PORT=6543`. Correct for the eval harness, which runs on the host. Wrong for everything else, because the API answers questions **inside the container**, where `localhost` is the API itself. Proven rather than reasoned: from `dataagent-api-1`, `localhost:6543` gives `ConnectionRefusedError: [Errno 111] Connection refused` while `seed-pizza-pg:5432` is reachable. So a developer who runs the one provisioning target the repo offers gets an organization, a verified read-only source and a full catalog — and every question asked of it in the browser fails at the connector. **This is B-102's shape, one layer in.** The path is not broken for the person who knows it belongs to the host-side harness; it is broken for exactly the person a provisioning target exists to serve, and nothing says which of the two you are. Fix by making the address explicit rather than ambient — a `--host/--port` pair, or a second target that registers the compose names — and by saying in `make help` which side of the network each one is for. Fix by making the address explicit and giving the browser its own target. | P12 | P2 | done (#95) — **fixed rather than filed, on the owner's call: “a provisioner that produces a broken org is worse than no provisioner.”** `make demo.setup` is the browser's path, and it runs `ops/seed/provision_demo.py` **inside the api container** — not a packaging choice, but the only honest place for it, because registering a source also *connects* to it and a host process cannot check `seed-pizza-pg`. It registers both seed databases against the compose names, discovers, profiles and embeds, is idempotent, and **skips rather than fails** a database that was never seeded, so a machine that only ran `make seed` still gets a working organization. `evals.setup` keeps the host address unchanged — CI has no container at all and `make evals` runs in-process on the host, so changing it would have broken the harness to fix the browser — but it now prints which network it registered for and points at `demo.setup`, and its `make help` line says HOST where it used to say something true of both and useful about neither. **The part that would have caught this** is the reachability report `demo.setup` ends with: for every source in every organization, can the API open a socket to the address it is registered with. It printed `NO  evals / Demo: localhost:6543/pizza` on its first run, which is this defect being reported by the thing built to report it. Only the organization the run provisioned can fail the command — a host-addressed eval org is unreachable *correctly*, and failing on it would make the target red everywhere and teach people to ignore it. Proved from clean on a fresh org: registered, catalogued, and *“How many orders were placed in July 2026?”* answered **3,718** through the container, matching `truths.json`. `ops/scripts/evals.sh`'s header, which flatly asserted the source *is* registered with the compose hostname, is corrected — it was describing the arrangement this entry created, two months early |
| B-116 | 2026-08-23 | Bringing up a fresh machine, walking the README (B-102's standing condition) | **The README's Prerequisites list is missing two things a fresh machine needs and misstates a third.** (1) **`gh` is absent.** Every merge is a PR into protected main and the session ritual (plan §7.1, CLAUDE.md) opens with `gh pr list`, so the GitHub CLI is as much a prerequisite as `make` is — and it is the one prerequisite whose absence you discover only after the work is done. Installed here with `winget install GitHub.cli`; note it also needs an interactive `gh auth login`, which no script can do for you. (2) **"Node 22 via pnpm" reads as though pnpm provides the runtime, and it does not** unless `manageNodeVersions` or `pnpm env use` is configured. This machine had Node **24.19.0** against `apps/web/package.json`'s `"node": ">=22 <23"`, so every pnpm invocation prints `WARN Unsupported engine`. Both web suites pass, so today it is noise rather than breakage — but it is noise on every command, and the day it stops being noise the README will still have said the tooling handles it. Say which Node and how to get it (`pnpm env use --global 22`, nvm-windows, or the `.node-version` file that is already there and unmentioned). (3) **Docker Desktop must be running**, and `make up` against a stopped daemon fails with `failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine`, which names a pipe rather than the thing to start. One line in the quickstart covers it. | P12 | P3 | open |
| B-117 | 2026-08-23 | Bringing up a fresh machine, walking the README (B-102's standing condition) | **The hermetic browser suite is flaky on a Windows host, with the exact symptom its own configuration was written to eliminate.** Two full runs of `make test.web.e2e`: **one failure each, a different test both times** — `conversation.spec.ts:197` then `conversation.spec.ts:77` — and an identical cause under both, `getByLabel('Ask a question')` never becoming visible inside the 20s expect timeout at `conversation.spec.ts:57`, in the shared `openConversation` helper. The named test passes **3/3** run on its own, so it is the suite and not the assertion. What makes this worth an entry rather than a shrug is that `playwright.config.ts` already chose `next build && next start` over `next dev` *for this*, and says so: Turbopack's compile-on-first-request "on Windows occasionally takes longer than any sensible assertion timeout — which shows up as a page that never signs in and has nothing to do with the code under test." The mitigation is in place and the symptom is back, so the diagnosis behind it is incomplete. **One concrete lead, not a proven cause**: `next.config` sets `output: "standalone"` while the webServer runs `next start`, and Next warns on every run — `⚠ "next start" does not work with "output: standalone" configuration. Use "node .next/standalone/server.js" instead.` That is the one combination in this path that the framework itself calls unsupported; whether it explains the hydration timeout was not established here. **It has a cost today**: `test.web.e2e` is in `preflight` (deliberately, after WP11.2b), so on this host `preflight` cannot go green in one pass and the developer learns to rerun it — which is how a real failure gets waved through. **Update 2026-08-24: the explanation in this entry is probably wrong, and saying so is worth more than the entry was.** Since it was written the suite has passed **four consecutive CI runs** and **9/9 locally on the very host it was filed against** — same host, same tests, no change to `playwright.config.ts` or to anything on the hydration path. Two things this entry did not account for happened in between. Docker Desktop on that machine was **broken for most of a day** — an `EIO` crash on the bind mount, published ports timing out while containers reported healthy, `docker exec` hanging, every connection answering `ConnectionResetError` — and clearing it took a reboot. And the two original failures were **a different test each time**, which reads as flakiness and reads equally as *whatever happened to run first while the machine was degraded*. **The `output: standalone` lead above is still unproven and now looks less likely**: nothing about it changed and the symptom stopped. What this entry can honestly claim is that the suite failed twice on a machine concurrently failing at everything else. What it did claim — a hydration flake specific to Windows — was an inference from two data points that the next thirteen contradict. Left **open** rather than closed, because a suite that failed twice deserves more than a few green runs before it is trusted; but nobody should spend an hour on the standalone lead on the strength of this text. **A wrong explanation nobody revisits is worse than an open question.** | P12 | P3 | open |
| B-118 | 2026-08-23 | The B-114 collision between #94 and #95 | **Two machines filing on the same day both allocated B-114, and the second one to push paid for it.** #94 (*a lost artifact is refused in the same words as a wrong query*) and #95 (*the provisioner builds an org the product cannot query*) were written on different hosts hours apart, each appending to what its own clone thought was the end of an append-only list. `scripts/check_backlog.sh` catches a duplicate id — `sort \| uniq -d`, with a selftest case — so the collision surfaced as a red `hygiene` job at merge rather than as two rows sharing a number, which is the guard working exactly as **B-081** intended. **The cost is renumbering, and it is larger than the table row suggests.** A backlog id does not stay in `BACKLOG.md`: #95's B-114 had reached `Makefile` help text, the README, `ops/evals/provision.py`, `ops/scripts/demo_setup.sh`, `ops/scripts/evals.sh`, `ops/seed/provision_demo.py`, `STATUS.md` and two commit messages — eight places, because this project deliberately cites entries where the code they explain lives. Renumbering *the row* and missing a citation would leave a comment pointing at somebody else's defect, which is worse than the collision. **Fixed as a rule rather than a mechanism**, in CLAUDE.md beside the branch rules: fetch `origin/main` before picking a number, and expect to renumber every citation if you lose the race. **A mechanism was considered and is not built**, deliberately: reserving ids centrally, or deriving them from a PR number, would trade a rare hour against a permanent coordination step on every entry — and the guard already makes the failure loud and safe rather than silent. Worth revisiting only if it happens a third time, which is the signal that two-machine work has stopped being occasional. One artefact survives on the dev host: the throwaway org proving #95's fix is still named `Demo B-114 check`, named before the renumber, and STATUS says so rather than rewriting a fact about the machine to match a number. | P12 | P3 | done (this entry is the record; the rule is in CLAUDE.md) |
| B-119 | 2026-08-23 | The owner's browser walk on the new machine, F&B sample | **The model invented a platform constraint and refused half a question the data answered.** Live, real model, owner's stack, F&B sample: *"which outlet wastes the most, and what does it cost?"* → `answerable=false`, **no query executed at all**, and the sentence *"The available reference also prohibits combining `fact_waste` with `dim_outlet`, so the outlet name cannot be returned."* **No such prohibition exists.** `fact_waste.outlet_key → dim_outlet.outlet_key` is a **declared foreign key at confidence 1.00** in the active catalog; the run's own `capability_checked` event lists **20** unreachable pairs and this pair is **not among them**, nor among the 20 chasms — it was `joinable`, a direct child→parent join, the safest kind the graph has. Run by hand, it answers: **Outlet C, 3.398**. The other half of the question was refused honestly — `est_cost` is NULL on both of `fact_waste`'s 2 rows — and that half is not the defect. **This is B-113's family and not B-113's cause.** B-113 was the platform's *preview budget* refusing a question and sounding like the schema; here nothing was short of anything — the model asserted a rule that no deterministic component imposed. A reader cannot tell *"your data cannot answer this"* from *"our prompt could not carry it"* (B-113) or, now, from *"the model believed a rule that does not exist"*. P1 for the same reason B-113 was: WP7.2b's whole argument is that an honest refusal beats a confident guess, and a **fabricated** refusal spends the credibility that argument earns. **D-026's mitigation was correct and insufficient, which is the part worth keeping.** `runner.py` already phrases the chasm note as an instruction rather than a prohibition, and says why in a comment: *"these pairs have a correct query, and a model told only 'do not' would refuse a question it could have answered."* That guards **over-refusal of a listed pair**. This is **over-refusal of an unlisted pair whose table names appear in the list**: the note is one sentence — *"These tables are related only through a shared parent, so joining them directly multiplies their rows together instead of matching them:"* — followed by 20 pairs, in which `dim_outlet` appears 7 times and `fact_waste` 13, including the entry `fact_member_visit ↔ fact_waste via dim_outlet` that places both names adjacent inside the prohibition. Co-occurrence in a "do not join directly" wall was collapsed into a pairwise ban. **It only surfaces on a wide schema, which is why the pizza fixture never produced it**: 6 tables yield few chasms and a short note, while F&B's 35 tables yield 20 chasms and 20 gaps — 40 pairs of table names under two prohibition-flavoured headings. Every eval and every gate demo to date has run against the narrow fixture, so the note has never before been long enough to be misread. **What to check first, in this order.** (1) Does the chasm note need to *scope* each pair rather than list them flat — naming the two children and the parent in a form that cannot be read as three prohibited tables? (2) Should the planner be told the pairs it **may** join for the tables it actually named, rather than the whole cross-product of pairs it must avoid? The gaps are handed over up front as fact (architecture 4.3), and at 40 pairs "up front" has become "buried". (3) Only then, whether a claimed constraint should be *checked* before it reaches a reader: the run named two tables and a prohibition, and `capability.py` holds the truth of that pair deterministically — a composed answer asserting a link is forbidden could be contradicted by the same graph that permitted it, which is the deterministic-enforcer shape architecture Part 7 uses everywhere else. | P12 | **P1** | open |
| B-120 | 2026-08-23 | WP12.2, wiring the API to the Bicep WP12.1 compiled | **"Built, tested, unreachable" at deployment scale — the fifth instance, and the first where the unreachable thing was infrastructure rather than code.** WP12.1 shipped nine Bicep modules that compile, pass `check.infra`, and were reviewed and merged green. Wiring the application to them found that `apps.bicep` set three environment variables on the API container that `Settings` has never read: `AZURE_KEY_VAULT_URI` (the field is `KEY_VAULT_URL`), `ARTIFACTS_BLOB_ENDPOINT` (the field is `ARTIFACTS_ACCOUNT_URL`), and `APP_DB_PASSWORD`, which no field read at all — so the template's deliberately password-less `APP_DATABASE_URL` would have reached asyncpg with nothing to authenticate with. A fourth was an *absence*: `ARTIFACTS_BACKEND` was never set, so a deployed API would have defaulted to `local` and written every result to a container filesystem that vanishes on the next revision. **Same lesson as B-083, B-085, B-100 and B-109, and it is worth stating in the form that generalises: a green build proves the parts work, never that they meet.** Coverage cannot see this class by construction — there is no unit test that could fail, because both halves are individually correct and the defect is only in the correspondence between them. What is new here is the *scale*: the previous four were a function nothing called, a definition nothing matched, a line nothing read and a field nothing filled. This is an entire deployment target wired to names that do not exist, and the only reason it was caught is that somebody wrote the other half by hand and looked. **Fixed in the same PR**, because a provisioner that produces a broken deployment is worse than none: the two names are aligned on the `Settings` fields (which the guards can see), `ARTIFACTS_BACKEND: blob` is set, and `app_db_password` is a real field joined into the DSN at `require_app_database_url` — honouring the template's stated intent that the password is the only part from Key Vault and the rest stays readable. **The load-bearing half is the guard.** `scripts/check_env.sh` compared `.env.example` with `ops/docker-compose.yml` and **never looked at `infra/`** — `grep -c infra scripts/check_env.sh` returned 0 — so no check in this repository could have caught any of it. It now asks two more questions: does every environment variable a deployment template sets name something `Settings` reads (or a declared `PLATFORM_ENV` key like `AZURE_CLIENT_ID`, which the Azure SDK reads rather than this application), and does a template that selects a non-default backend also set what that backend needs. Run against the unfixed tree it reported all four defects; three selftest cases hold it, including a control so the two failure cases cannot be satisfied by a check that fails everything. **One thing the guard still cannot see, stated rather than left to be discovered:** a variable the template *omits*. `ARTIFACTS_BACKEND` was found by reading, not by the guard, and no correspondence check can flag an absence, because most settings are absent on purpose and correctly defaulted. Question 7 covers the one knowable shape of it — a backend selected without its companion address — and the general case still needs a person. **A second defect was found inside the guard while extending it**, and it is the worse kind: an unmatched glob handed `sed` a literal path, `sed` exited non-zero, `pipefail` failed the pipeline and `set -e` ended the run — **exit 1 with no message at all**, which in CI is a red build that says nothing and locally is a guard that appears to have an opinion it never formed. Both readers now build the file list first and return cleanly on an empty one. | P12 | P2 | done (WP12.2) |
| B-121 | 2026-08-23 | WP12.2, closing the first-deploy gaps | **The API authenticates to Postgres with a password, where the design says managed identity — deferred deliberately, and this entry is the record that it was a choice.** `ops/sql/app_role.sql` has promised since Phase 1 that *"in Phase 12 this step is replaced by Key Vault and a managed identity"*. WP12.2 delivers the Key Vault half: `dataagent_app`'s password is generated once by `ops/scripts/deploy_seed_secrets.sh`, stored in the vault, read by the migration job (`dataagent.db.grant_app_login`) to grant the login, and read by the API as its own connection secret. No migration contains a credential and no password appears in a template, which is what that promise was mostly about. **What is not delivered is the second half**: the API still presents a password rather than a token proving it is `id-dataagent-dev`. **The owner chose this on 2026-08-23**, asked directly, on the reasoning that it is the smaller change, it uses a migration job that already runs as the owner role, and Entra-to-Postgres is a work package rather than a step. **What the real thing costs**, so the next person is not deciding blind: Azure Database for PostgreSQL Flexible Server supports Microsoft Entra authentication, where the "password" is a short-lived access token fetched from the platform. That means `postgres.bicep` enabling Entra auth and naming an Entra administrator; the managed identity created as a Postgres role rather than `dataagent_app` being created by a migration; **token refresh inside asyncpg's connection lifecycle**, which is the genuinely fiddly part — a pooled connection outlives a token, so the DSN's password cannot be computed once at startup the way `_with_password` computes it now; and a local story, because developers have no managed identity and `make db.setup` must keep working. **What deferring costs, stated plainly**: a password exists that could be exfiltrated, it is shared between the migration job and the API, and rotating it is a vault edit plus a redeploy rather than nothing at all. Contained by the vault, the managed identity that reads it, and a server with no public endpoint. **Take it in WP12.3 or WP12.4** — 12.4's ASVS-lite checklist has an authentication section this belongs under, and the restore drill is the natural moment to prove the new path works against a fresh server. | P12 | P2 | open |
| B-122 | 2026-08-24 | WP12.2, the first real deployment | **`what-if` skips the modules most worth checking, says `Succeeded`, and the only place that is written down is a warning nobody reads.** Every `what-if` run against `rg-dataagent-dev` returned `status: Succeeded, error: null` — and each also returned three `NestedDeploymentShortCircuited` diagnostics naming `apps`, `roles` and `postgres`. Those are the three modules whose parameters come from earlier modules' `reference()` outputs, so ARM cannot evaluate them before those modules run and skips them entirely. **Three of ten modules were never validated, and they are the app, its permissions and its database.** The gap is structural: any template composed through module outputs — which is how this one is built, deliberately — has the same hole in the same places. **What it cost, concretely.** `apps.bicep` declared its Container Apps environment with `destination: 'log-analytics'` and deliberately no `sharedKey`, on the stated reasoning that "the environment writes with its own identity". It does not: that destination requires `customerId` **and** `sharedKey`, and Azure refuses at preflight with `LogAnalyticsConfiguration is invalid`. The template compiled, `check.infra` passed, `what-if` passed three times, and the **first real deployment** failed on it — after creating ten resources including a running Postgres server. Fixed in the same PR by moving to `destination: 'azure-monitor'` plus a diagnostic setting, which is keyless and is what the original comment was reaching for; the owner declined the one-line `listKeys()` alternative because it would have reinstated the single secret WP12.1 designed out while looking like it had not. **Wanted, and none of it is free.** (1) The deploy workflow should **fail, or at least say so loudly, when `what-if` reports a short-circuit** — today the step prints the diagnostics into a log nobody reads and exits 0, which is the worst of both. (2) Consider whether the modules that short-circuit could take fewer `reference()` parameters — passing a workspace's resource id rather than a value read from it, for instance, is often enough and evaluates statically. That is a real refactor with a real cost in template legibility, so it is a judgement rather than an obvious win. (3) At minimum, `infra/README.md` now tells a reader to read the `diagnostics` array rather than `status`, which is the cheapest half and is already done. **Same family as B-120**, one level out: parts individually correct, never checked against each other, and the only thing that closes it is running the real thing. | P12 | P2 | open |
| B-123 | 2026-08-24 | The first deployed request, WP12.2 | **The API has connected to Postgres as the migration owner on every developer machine since Phase 1, and CLAUDE.md's hard rule says it must not.** The rule is unambiguous — *"The API connects as `dataagent_app` (no superuser, no BYPASSRLS, owns nothing). Migrations run as the owner. Never collapse the two."* `system_session()` is the **owner** connection (`DATABASE_URL`), and its own docstring says it is *"for migrations, bootstrap and admin jobs only. Every use is a deliberate step outside tenant isolation."* Eight call sites in six request-path modules use it: `auth/context.py` twice (this is `/v1/me`, so **every authenticated request**), `orgs/service.py` twice, `invitations/service.py`, `orgs/recovery.py`, `db/security_events.py`, and `agent/scheduler.py`. **Sixth instance of this project's characteristic defect, and the first where the thing never reached was a security guarantee rather than a feature.** B-083 was a function nothing called, B-085 a definition nothing matched, B-100 a line nothing read, B-109 a field nothing filled, B-120 a template naming variables nothing read. This one is the *absence* of a separation the documentation asserts and the tests do not check: `rls_proof` proves `dataagent_app` cannot cross tenants, and proves nothing about which role the API actually connects as. **Only a real deployment could reveal it, and the mechanism is worth keeping.** Every developer `.env` sets `DATABASE_URL` and `APP_DATABASE_URL`, so the owner DSN was always present and `system_session()` always worked. Azure is the first environment that gave the API only the unprivileged DSN — `apps.bicep` sets `APP_DATABASE_URL` and not `DATABASE_URL`, which was itself an oversight — and the API immediately failed with `RuntimeError: DATABASE_URL is not set` inside the startup sweep and inside `/v1/me`. **A configuration omission surfaced a design violation**; had the Bicep been "correct" the rule would still be false and nothing would have said so. **The fix is not the obvious one, and the analysis matters more than the entry.** Of the eight sites, three touch only non-tenant tables (`users`, `security_events`) and move to the app engine unchanged. The other five — `resolve_context` and `memberships_for` on `org_memberships`, `accept_invitation` on `invitations`, the recovery claim on `org_recovery_grants`, and `sweep_orphaned_runs` on `agent_runs` — read **genuine tenant tables** either across every organization or *before* the organization is known, which is the authorization bootstrap: you cannot set `app.org_id` until you have discovered which org the caller belongs to. Under the app role with RLS those five return nothing, which `rls_proof`'s *"a session without an org sees nothing and says so"* asserts as correct behaviour. **Candidates, none free:** `SECURITY DEFINER` functions owned by the owner and granted `EXECUTE` to `dataagent_app`, which keeps one connection and makes the bypass surface an enumerable list in a migration; a user-scoped RLS policy keyed on a new `app.user_id` setting, which changes the tenancy model; or a third role that owns nothing and may bypass RLS on exactly those five tables, which is a second credential to hold and rotate. **Adding `DATABASE_URL` to `apps.bicep` is explicitly not the fix** — the owner was offered it on 2026-08-24 and refused it, on the grounds that it makes the hard rule false in the one environment where it matters and that a temporary owner credential is never revisited. **dev stays broken until this is fixed**, which is the owner's stated price. | P12 | **P1** | done (WP12.2) — **SECURITY DEFINER, the owner's choice on 2026-08-24**, so the bypass becomes an enumerable list in a migration rather than an ambient privilege. Revision **0028** creates five functions, each answering exactly one question with the narrowest query that answers it: no parameter is a table name, a column name, or any fragment of SQL, because a SECURITY DEFINER function that assembles a statement is the same hole with more steps. Each is `STABLE`, pins `SET search_path = pg_catalog, public` — the textbook escalation is a caller prepending their own schema so an unqualified name in the body runs as the owner — and is `REVOKE ALL ... FROM PUBLIC` before `GRANT EXECUTE` to `dataagent_app` alone, because a definer function is executable by PUBLIC from the moment it exists. **All eight call sites moved.** Three needed nothing but the application role (`users` and `security_events` carry no RLS); five took a function. Both token flows already did every *write* inside `org_session`, so only the lookups needed anything. `tests/db/test_security_definer.py` enumerates every definer function in the database and fails on any not in `SECURITY_DEFINER_FUNCTIONS`, plus unpinned search_path, PUBLIC executability, a missing grant, volatility, and that `ops_orphaned_runs`'s hard-coded statuses still equal `ORPHANABLE`. **And the test that should have caught this all along was worse than its absence.** `test_the_api_connects_as_the_application_role` opened a connection *with the app DSN* and asserted it connected as the app role — true by construction, and occupying the space where the real check would have gone. Renamed to what it checks; the guarantee is now `test_the_api_never_needs_the_owner_connection`, which makes `get_engine` **raise** and then drives `ensure_user`, `resolve_user_id` and `memberships_for` — reproducing the Azure failure, and unlike a grep it cannot be satisfied by a wrapper around `system_session` |
| B-124 | 2026-08-24 | WP12.2, the fourth one in a row | **Four checks in this phase have passed or failed for reasons unrelated to the thing they were checking, and the lesson is the same every time: a check has to be verified in both directions before it is trusted in either.** The instances, because the pattern is only visible as a list. **(1) The smoke that lied.** Checks 2 and 3 ran `az containerapp exec`, which prints `INFO: Connecting to the container 'api'...` on stdout ahead of any output, so both compared that banner to what they expected and reported a *working* deployment as broken — twice in one run. **(2) The check that could not fail.** The same smoke's check 3 asked the *web* origin for a proxy path that does not exist, got a 404 and called it "inconclusive". A check with no failing case is decoration that reads as coverage, and nothing would ever have drawn attention to it. **(3) The test that was true by construction.** `test_the_api_connects_as_the_application_role` opened a connection *with the app DSN* and asserted the app DSN connects as the app role. It passed for eleven phases while the API used the **owner** connection on every authenticated request (**B-123**) — a test whose name stated the guarantee, whose body proved something adjacent, and which occupied the space the real check would have gone in. **(4) The timing failure on a healthy revision.** The rewritten smoke retried the web app ten times over a minute and gave the API a single 30-second attempt. `minReplicas: 0` means the API is cold when the smoke arrives, so a good deploy went red — and a check that fails on a cold start is one people re-run rather than read, which is instance (1)'s damage arriving by the opposite route. **What actually distinguishes the checks in this repository that have worked.** `check_env.sh`'s infra questions were run against the unfixed tree first and reported all four real defects before anything was fixed; `test_both_parameters_actually_bind` was verified by reintroducing each of the two broken spellings by hand; `test_security_definer.py` ships a control case so its two failure cases cannot be satisfied by a check that fails everything; and B-109's regression test asserts a code only reachable if a field survives the whole path. Every one of those was made to fail on purpose before being trusted to pass. **Wanted: make that the standing condition rather than a habit.** A new check earns its place by demonstrating a red — in the PR description, by reintroducing the defect, or by a control case beside it — and a check that has never been seen to fail is recorded as unproven. Cheap, and it is the only thing that separates the four above from the four that worked. | P12 | P2 | open |
| B-125 | 2026-08-24 | Registering a customer database on the deployed app | **The API's managed identity could read secrets and not write them, so the product's central action failed on a deployment that was otherwise working.** Registering a data source writes a customer's database credential to Key Vault (`SecretsProvider.put`, architecture 7.3), and `roles.bicep` granted the app identity **Key Vault Secrets User** — read-only. The register form answered *"Could not reach the API"*, which is the browser's rendering of an unhandled 500 with no CORS headers; the API was healthy throughout, and the log said `ForbiddenByRbac — Microsoft.KeyVault/vaults/secrets/setSecret/action, Assignment: (not found)`. **The comment above the role is the interesting part**, because it argued for the wrong thing convincingly: *"Deliberately not Secrets Officer, which can also write and delete: the application reads the OpenAI key and has no business rotating it."* True of the configuration secrets, and it quietly redefined the application as the thing that reads its own config rather than the thing that stores customers' credentials — the narrowest role for the job the author had in mind, not for the job the product performs. B-120's family, one level up from names to permissions. Fixed by granting `Key Vault Secrets Officer` (`b86a8fe4-…`), still the narrowest **built-in** role that can set a secret; a custom definition holding exactly get/set/delete would be narrower and needs a larger permission to create than this pipeline holds, which the comment records as the next step if Officer's extra verbs ever matter. **Two things this leaves open.** The assignment name is `guid(vault.id, principalId, role)`, so a redeploy creates a new assignment and **leaves the old Secrets User one in place** — harmless, since Officer is a superset, and worth deleting by hand so the vault's access list says what is true. And nothing tests it: no check here exercises a write to a real vault, and the first thing that does is a person registering a database. A post-deploy smoke step that stores and deletes a throwaway secret under the app identity would have caught this in the pipeline rather than in a browser — the same argument as **B-124**, that a permission nobody has seen exercised is a permission nobody has verified. | P12 | P2 | done (WP12.2 the role; #111 the smoke step, which now runs as the *app's* identity rather than the pipeline's) / open (the stale Secrets User assignment, still to be deleted by hand) |
| B-126 | 2026-08-24 | Asking a question on the deployed app | **The deployment shipped the model credential and none of the configuration that makes it usable, so every question failed and nothing said why.** `apps.bicep` set `OPENAI_API_KEY` and not one of `LLM_PROVIDERS`, `LLM_MODELS`, `LLM_ROLE_MAP` or `LLM_PRICES`. `llm_providers` falls back to `('openai',)` and `llm_models` to `{}`, so `registry.resolve` raised *"LLM_MODELS names no models for provider 'openai'"* at the first model call of every run. **`ops/docker-compose.yml` already carries that exact sentence**, recorded when the same thing happened in Phase 7 — *"a key that exists only in the developer's shell reaches the smoke script and never the product"*. Compose was fixed then; this template was written afterwards and repeated it. B-120's family: a mode configured without what it requires. **Three things let it through, and they are the part worth reading.** **(1) The guard was right and its coverage was the gap** — the third time those two have needed separating (B-124 is the second). `MODE_REQUIREMENTS` and the `degraded` probe shipped *for exactly this shape of failure*, and its own docstring says so: *"a configuration that promises a mode it cannot serve"*. It covered `auth_mode`, `secrets_backend` and `artifacts_backend` and said nothing about the LLM, so `/healthz` reported `ok` on a deployment where every question failed. Nothing about the mechanism was wrong; its list was short. The distinction matters because the instinct on finding a defect past a new guard is to distrust the guard, and that is the wrong repair — the fix here adds two names, not a different design. **(2) `check_env.sh` passed**, for the same reason: its couplings are `KEY=VALUE → COMPANION`, and this is not a mode being selected. Check 9 (`RUN_REQUIRED`) is the new shape — what a *run* needs in every mode — and it was run against the unfixed tree first and reported both missing names before anything was changed, per B-124. **(3) The failure was silent where an operator looks.** `_record_failure` wrote the reason to `agent_events` and called no logger, so 300 lines of API log held no error at all: the person asking saw `failed` and *"The run could not be completed."*, the operator saw a healthy service, and the actual sentence sat in a database column needing the platform DSN to read. Neither could tell a broken platform from a bad question. Fixed in the same pass at the owner's instruction rather than deferred to WP12.3. **And the reason nobody noticed the missing logging for eleven phases: the test suite could not see a log line.** Alembic's `env.py` called `fileConfig(...)`, whose `disable_existing_loggers` defaults to **True**, so migrating at session start switched off every `dataagent.*` logger for the rest of the process — a `caplog` assertion captured nothing, silently, and would have passed against a runner that logged nothing anyway. `disable_existing_loggers=False` now. That is a fourth instance of a check that cannot fail. Fixed: the eight variables in `apps.bicep` (`LLM_RUN_COST_LIMIT_USD` at **$1.00** in dev, half a developer's, because dev is externally reachable and unwatched); `RUN_REQUIRED` in `config.py` with its shell copy and the test that keeps them equal; `/healthz` degraded when a provider named in `LLM_PROVIDERS` has no models — note it is **not** a presence check, since `{}` is set and `{"anthropic": …}` is non-empty, and both are the failure; and a runner test that drives `execute_run` with that configuration and asserts the log names the run and the variable, with a control that a successful run logs nothing. `EMBEDDINGS_*` was absent too and is set here — not the cause, since `get_embedder` returns None and retrieval degrades to lexical by design, but it made dev quietly worse at retrieval than local. | P12 | **P1** | done (#108) |
| B-127 | 2026-08-24 | B-126, writing the first `caplog` assertion this repository has ever had | **No test in this repository could observe a log line, and none had ever tried — so every "does it log the right thing" assertion has been unfalsifiable since Phase 1.** `alembic/env.py` called `fileConfig(config.config_file_name)`, whose `disable_existing_loggers` argument defaults to **True**. Every `dataagent.*` logger is created at import; `conftest` migrates once at session start; so from that moment every application logger in the process had `disabled = True` and emitted nothing at all. **The asymmetry is what makes it a check that cannot fail, and it is the fourth instance** (after B-124's three). An assertion that a line *is* logged fails loudly — that is exactly how this was found, when B-126's first `caplog` assertion captured an empty string and looked like a bug in the fix. An assertion that *nothing* was logged passes **vacuously**, and "nothing was logged" is precisely the shape a **control** case takes: B-126's own `test_a_run_that_succeeds_logs_no_error`, written to prove the two positive tests are not satisfied by a runner that logs on every path, would have passed against a runner that logged nothing anywhere. The control and the fix have to ship together or the control is decoration. **What was actually measured, rather than assumed.** Searching the suite for `caplog`, `LogCaptureFixture`, `assertLogs`, `capsys`/`capfd` and logger patching returns **one file — the one added by B-126**. So no test was quietly passing: the capability had never been used at all, and the unfalsifiability was latent until the first person tried. That is a weaker claim than "assertions were silently green" and it is the true one. **Thirteen `logger.` call sites across nine modules were unobservable**, and the sharp ones are the records of last resort — each writes somewhere durable first and logs *only when that write itself failed*: `auth/audit.py:99` when an audit row cannot be written, `db/security_events.py:97` when a security event cannot be written, `auth/jwt_validator.py:122`, `main.py:49` when interrupted runs cannot be reconciled at startup. When the durable record fails, the log line **is** the record, and nothing could verify it exists. **Scope, stated rather than implied: this was never a production outage.** Nothing under `src/dataagent` runs Alembic in-process — the deployed migration job is a separate container command — so the deployed API's own logs were always real. What was disabled was the ability to *test* logging, which in a repository whose characteristic defect is "built, tested, unreachable" is the more expensive of the two. Fixed: `disable_existing_loggers=False` in `alembic/env.py`, plus `tests/test_logging_is_observable.py` — one test that no `dataagent.*` logger is disabled *after requesting the migration fixture* (without that fixture the test passes against the defect, which would make the guard itself the fifth instance), and one per last-resort module asserting a record actually reaches a handler. | P12 | P2 | done (#108) |
| B-128 | 2026-08-24 | Re-dispatching the deploy after #108 | **The step that checks the deployment has its values could never pass, and failed on a deployment where all eight values were present.** `deploy.yml`'s preflight loop read `eval "value=${${$name}}"`. `${${...}}` is not indirection in any shell — it is a **parse** error, so bash rejected the line before `eval` was reached and the step exited 1 with `bad substitution`. The run's own env block lists every one of the eight variables with a value. **Written in #107, whose title is "the guard becomes a detector", and merged without ever executing.** A workflow step is the one kind of code this repository has no way to run before pushing it: `check_env.sh` has a `--selftest`, `check_status.sh` has one, the Python has pytest — and a `run:` block is exercised for the first time in the environment it gates. It is therefore the easiest place in the repo to ship something that has never run, which is this project's characteristic defect wearing a different hat. **The class the owner has been counting**: a check that fails for a reason unrelated to the thing it checks. That is now instance five, after the `az containerapp exec` banner (twice), `test_the_application_role` proving something adjacent, and the asymmetric smoke retry on a cold start. Same lesson each time — **a check has to be verified in both directions before it is trusted in either** — and the same remedy B-124 asks for: demonstrate the red. Fixed with bash's `${!name}`, and demonstrated before pushing rather than after: run in the same shell with all values set (passes), with one **empty** (fails naming it), with one **unset** (fails naming it), and the old spelling reproducing `bad substitution` verbatim. **What is still owed, and it is the real fix**: nothing runs `deploy.yml`'s shell before a dispatch does. A `shellcheck` pass over the `run:` blocks, or extracting them into `ops/scripts/` where they can be selftested like every other script here, would have caught this at the same cost as reading it. Filed as the follow-up rather than done here, because unblocking a deploy and restructuring a workflow are two different reviews. | P12 | P2 | done (#TBD, the expansion) / open (nothing exercises a `run:` block before a dispatch) |
| B-129 | 2026-08-25 | Listing what the deployed app cannot do that the local one can | **`storage.bicep` creates a `documents` blob container that nothing writes to, reads from, or names.** Knowledge documents and their chunks store their text in Postgres — revision **0016** creates `knowledge_documents` and `knowledge_chunks` with text columns, and `knowledge/` contains no blob code at all. `ARTIFACTS_CONTAINER` defaults to `artifacts` and is the only container any setting names. So the resource exists, is empty, and its name states a purpose the system does not have. **Why this is worth an entry rather than a shrug.** An empty container costs nothing; a *name* that asserts something false costs whatever the next person builds on it. Someone adding document upload will find a container called `documents`, reasonably conclude it is the intended home, and write there — at which point they inherit two things nobody decided: it is **outside the retention policy** (the lifecycle rule filters `prefixMatch: ['artifacts/']`, so anything under `documents/` is kept forever), and it has no tenant-prefix convention, where `BlobArtifactStore` enforces `{org_id}/...` and `_check_tenant` refuses a reference belonging to another organization. A container with no code behind it also has no isolation rule behind it. This is the same family as **B-120** — a name that reads correctly and that nothing honours — one level out, in infrastructure rather than in configuration. **Two ways to resolve it, and the choice is the owner's.** Delete the container and let whoever needs document storage create it with its rules in the same PR; or keep it and write down, in `storage.bicep`, that it is reserved, unused, outside the lifecycle policy, and what a writer would owe. Deleting is preferred here — a comment saying "unused" is a promise about the future that the next person is free to disagree with, and the container is trivially recreatable. Not done in this PR because deleting a storage resource, even an empty one, is not something to slip into a PR about smoke checks. | P12 | P3 | open |
| B-130 | 2026-08-25 | The first deploy after #111, which failed on it | **A role definition GUID was fabricated rather than looked up, and nothing between writing it and deploying it could tell.** `roles.bicep` set `keyVaultSecretsOfficer = 'b86a8fe4-44ce-4948-aff7-8adaef4a4c62'`. The real id is `b86a8fe4-44ce-4948-**aee5-eccb2c155cd7**` — the first three groups match and the last two were invented. The deployment failed three minutes in with `RoleDefinitionDoesNotExist`, after the resource group had already been touched, and the nested `roles` deployment was left `Failed`. **Written by Claude in #107 from recall instead of from `az role definition list`.** That is the same failure as the fabricated `fnb_readonly` password earlier in this phase: a plausible-looking value produced where a lookup was required. The password was caught immediately because the user tried it; this one survived review and a merge, because a GUID cannot be checked by reading. **Why every existing guard was blind to it, and this is the part worth keeping.** A wrong *name* reads wrong — `AZURE_KEY_VAULT_URI` for `KEY_VAULT_URL` (B-120) is visible to anyone who knows the field list, and `check_env.sh` catches it now. A wrong *expansion* reads wrong once you know the idiom, and shellcheck catches it (B-128). A wrong **GUID reads exactly like a right one** — to a reviewer, to `bicep build`, which type-checks the template and cannot know what the constant means, and to **`what-if`**, which reports the assignment as a resource it would create without resolving the role it points at. That is another entry in the what-if blind-spot list, and a sharper one than the nested-deployment case: here what-if had the value in hand and no reason to question it. Only Azure can distinguish the two, so only Azure can be the check. **Two of the three ids in the file were correct** (`AcrPull`, `Storage Blob Data Contributor`), verified against Azure while fixing this — worth stating, because "one was wrong" and "the file was unreliable" are different findings and the second would have justified a much larger change. Fixed with the id Azure reports, and `ops/scripts/check_role_definitions.sh` now runs in `deploy.yml` **after the OIDC sign-in and before `what-if`** — one API call per distinct id, turning a deployment that fails partway into one that refuses to start. Verified in both directions against the real committed files: red on `roles.bicep` at **49e4082** naming the bad id, green on the corrected tree naming all three roles. It also fails when it extracts *no* ids at all, because a guard that silently checks nothing is the defect this repository has now shipped twice. **Scope stated rather than implied**: it reads `roles.bicep` only, where every GUID literal is a role id by construction. A role assigned from another module would not be covered, and widening it should be deliberate rather than a glob. **Then the fix went red on its own PR, which turned out to be the same defect one file over.** `hygiene`'s gitleaks step flagged the *corrected* id as `generic-api-key`: a public role id is indistinguishable from a leaked key to an entropy heuristic, which is why `.gitleaks.toml` allowlists each one by value with the role named beside it. That list had drifted — **#107 swapped the vault role from Secrets User to Secrets Officer and never touched the allowlist**, which went on exempting `4633458b-…`, a value the template no longer used. Nothing failed at the time *because the id #107 introduced was invented and happened not to trip the heuristic*: the same fabrication hid the drift it caused. Correcting the id made both visible at once. So `--allowlist-only` was added to the same script and runs in `hygiene`, comparing the two lists **in both directions** and needing no Azure — an id the template uses and the allowlist omits fails the scanner on a public value, and an id the allowlist keeps after the template drops it is a standing exemption a real secret could inherit. Verified against the real files: run with `main`'s allowlist and the corrected template it reports both problems and exits 1; on the fixed tree it agrees. The `az` resolution half stays in `deploy.yml`, where there is a signed-in session. | P12 | P2 | done (#112) |
| B-131 | 2026-08-25 | The deploy after #112, which the role-id fix unblocked and this then blocked | **A role assignment made by hand to work around B-125 now blocks the template that fixes it.** With the corrected Officer id, `check_role_definitions.sh` passed and `what-if` passed, and the `roles` module failed with `RoleAssignmentExists — The ID of the existing role assignment is fe5ff098052c4b6ca8bb72762c332524`. The vault currently carries **two** assignments for the app identity: `3be8d90b-…` **Key Vault Secrets User**, the deterministic `guid()` name the pre-#107 template created and B-125 already records as needing removal, and `fe5ff098-…` **Key Vault Secrets Officer**, a random name — the signature of `az role assignment create`, which is the command the owner ran by hand on 2026-08-24 so that registering a data source would work. **Why Bicep cannot reconcile it, and this is the generalisable part.** `roles.bicep` names its assignment `guid(vault.id, principalId, role)` deliberately, so a redeploy *updates* rather than colliding on a name that already exists. But Azure's uniqueness constraint is on the **(scope, principal, role) triple**, not on the name — so an assignment of the same triple under a *different* name is not something the template can adopt, update or overwrite. It can only fail. A deterministic name protects against redeploying your own assignment and gives no protection at all against somebody else's, and the error names an opaque GUID rather than the role, so the first reading of it looks like the template colliding with itself. **The wider point worth keeping: a manual workaround for a template defect becomes a blocker for the template's fix.** The workaround was correct and necessary at the time — it is what made the product usable while B-125 was open — and nothing recorded that the environment had drifted from what `infra/` describes. That is the same class as the stale gitleaks allowlist in B-130 and the orphaned secrets in B-014: state changed outside the thing that is supposed to describe it. **The fix is two commands and one re-dispatch**, and it belongs to a person because it removes the identity's ability to write secrets for the seconds between the delete and the redeploy: delete `fe5ff098-052c-4b6c-a8bb-72762c332524` so the template can create its own, and delete `3be8d90b-1990-5a8f-9305-bdef2270589a` because Secrets User is a subset of Officer and a vault access list should say what is true — which closes B-125's remaining open half. The exact commands are in STATUS under *"What the next session must do first"*. **Worth considering rather than assumed**: a pre-deploy check that the assignments Azure holds for the app identity match the ones `roles.bicep` declares would have named this before the deploy started, in the same shape as `check_role_definitions.sh`. Not built here — it needs a decision about what to do with an assignment somebody added deliberately, and refusing a deploy over one may be worse than the drift. | P12 | P2 | open — blocks the deploy |
| B-132 | 2026-08-25 | Checking why `main` was red while every PR was green | **The `docker` job has failed on every merge to `main` and never once on a pull request.** `apps/web/Dockerfile` refuses to build without `NEXT_PUBLIC_API_URL` — the value is inlined into the browser bundle, so an image built without it can never reach an API, and the guard was added after exactly that shipped. The guard is right. `ci.yml`'s `docker` job passed **no build args at all**, so the build died with `test -n "${NEXT_PUBLIC_API_URL}"` returning 1. **Why nobody noticed: the job is `if: github.ref == 'refs/heads/main'`.** On a PR it is SKIPPED, so it goes green through review and red the instant it merges — and it is not a required context, so nothing blocks and nobody is told. Eight consecutive red runs on `main` before anyone looked. **Same shape as B-128**, and worth saying so in as many words: *a job whose first real execution is somewhere nobody is looking*. There the code was a `run:` block that no tool could exercise before a dispatch; here it is a job that no PR can exercise before a merge. Both are the repository's characteristic defect — built, tested, unreachable — moved out of the product and into the pipeline, and in both cases the green everyone was reading came from a step that had not run. **Nothing was actually broken**, which is why it survived: `deploy.yml` builds the real web image with the real values from the `dev` environment, so every deployed bundle has always been correct. What was broken was the signal. Fixed by passing `NEXT_PUBLIC_API_URL=https://api.ci.invalid` — **RFC 2606's reserved TLD**, guaranteed never to resolve anywhere, chosen so the resulting image cannot be mistaken for a deployable one or accidentally point at something real. The job now proves exactly one thing, and its comment says so: **the image builds**. Verified by running that exact command locally before pushing. **The honest fix is different and is deliberately deferred.** Running `docker` on pull requests too is what would put the failure where somebody can act on it, at the cost of an image build per PR — and CI already takes 30+ minutes with a required `coverage` job gated on the slowest shard, so adding to it needs the CI-duration question answered first rather than worked around. The owner's call on 2026-08-25 was the cheap option now, main-only, with this note. | P12 | P2 | done (#114, the placeholder) / open (running it where a PR can see it) |
