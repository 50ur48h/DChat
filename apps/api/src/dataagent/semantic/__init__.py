"""The semantic layer: what a metric means here (architecture Part 5.4).

One module today. `definitions.py` holds the objects a metric is made of and the
two things the rest of the system does with them — render them into a prompt, and
check a statement against them. `verified.py`, the admin-blessed query patterns
5.4 also calls for, is WP10.2d's.
"""
