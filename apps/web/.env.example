# Web app configuration. Copy to `.env.local` for `pnpm dev`.
# NEXT_PUBLIC_* values are inlined into the browser bundle at build time —
# never put a secret behind that prefix.

NEXT_PUBLIC_API_URL=http://localhost:8000
